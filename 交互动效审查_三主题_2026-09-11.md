# 「上课 Shangke」交互动效审查 · 柔绘 / 书卷 / 通透（第二轮 · 代码级）

> **审查范围**：全 App 交互动效。仅做动效分析，不含配色、排版、字体、界面绘制。
> **审查基线**：`d330bcb`（2026-09-11 11:20，`feat(theme): 新增第三主题「柔绘」(SOFT) v3.42.0`）。
> **上一轮**：同目录文档 08:14 版。本轮逐行复核，**已修正 2 条上一轮的误判，新增 13 项发现**。

---

## 0. 与上一轮的差异（先说清哪些已变）

| 上一轮结论 | 本轮复核结果 |
|---|---|
| 「柔绘主题完全没有动效分流，周扫光原样命中」 | **已修复，误判需撤回**。柔绘于 11:20 提交，`WeeklyScheduleScreen.kt:957-1008` 已加动效级分支：柔绘下扫光改为「整屏换气」。但该新实现自身存在 2 处缺陷（见柔绘问题点 1） |
| 「sheet/dialog 走 M3 默认 spec、不读动画令牌」 | **依然成立**，但需精确化：柔绘已分支 `scrimColor` 与模糊参数（`AppBasicComponents.kt:343-373`，视觉层），**动效 spec 仍未分支、仍不读 `LocalAppMotion`** |
| 「Tab 选中瞬切」「加载/空态零动效」「无下拉刷新」 | 全部依然成立，且本轮补全了量化证据 |

**新增发现 13 项**（按严重度）：今日页课程卡按压与入场动画落在**不可达分支**；110 处 `clickable` 全部使用 Material 默认涟漪；`TouchFeedbackStyle` 是死设置；拖拽落位无吸附；当前节次高亮瞬切；列表增删无动画；顶栏无滚动动效；无减弱动效降级；入场错峰总延迟可达 2.1s；三处入场记忆方式不一致；预览只覆盖 2/5 分组。

---

## 1. 事实清点（本轮结论的全部依据）

### 1.1 动效体系结构

- `AppMotion.kt:61-87`：3 档风格 `GLASS`（默认）/ `GENTLE` / `SNAPPY`；`AppSettingsModel.kt:160` 默认 `GLASS`。
- `AppMotion.kt:223-238`：`GlassTokens` 的弹簧 = SwiftUI `.smooth/.snappy/.bouncy`（dampingRatio 1.0 / 0.86 / 0.62），曲线 = iOS `cubic-bezier(0.32,0.72,0,1)`。
- `Theme.kt:62-71`：注入 `LocalAppMotion` 时**只读动画风格与分组开关，不读 `themePreset`**。
- 全库仅 11 个文件 import `androidx.compose.animation`。

### 1.2 主题 × 动效 交叉矩阵（本轮核心）

柔绘（SOFT）在全库有 **50+ 处 `isSoft` 分支**，逐条归类后：

| 分支类别 | 处数 | 是否动效级 |
|---|---|---|
| 材质（`softSurface` / `softFeatherRim` / `softGlow` / `softShadow` / `softTexture`） | 20+ | 否 |
| 圆角 / 间距 / 字重 / 字号 / 透明度 / 色值 | 25+ | 否 |
| 内容分支（`SoftTodayContent` 等） | 4 | 否 |
| **动效**（周换气时长与曲线） | **1**（`WeeklyScheduleScreen.kt:974-975, 987-990`） | **是** |

**结论：柔绘主题已"进场"，但只进了材质层，动效层只进了 1 处。** `SoftStyle.kt:44-46` 的注释自称「功能位完全一致……只替换材质、圆角、配色**与动效**」——**「动效」这半句与实现不符**。书卷、通透同理：全库**没有任何一处按 `isClaude` / `isIos` 分流的动效**。

### 1.3 动画原语使用统计（全库 grep，已排除 build/）

| 原语 | 处数 | 说明 |
|---|---|---|
| `Animatable(` | 4 | 触摸反馈 ×2、周换气 ×1、动效预览 ×1 |
| `animateColorAsState` | 1 | 仅「开发者模式 App 图标」背景（`DeveloperAndIconComponents.kt:70`） |
| `animateFloatAsState` | 8 | 底栏隐藏、圆钮隐藏、按压缩放、入场 ×4 |
| `AnimatedVisibility` | 4 | 挂起条、圆钮、开发者选项展开、动效预览 |
| `animateContentSize` | 1 | 外观设置页预览容器 |
| **`AnimatedContent`** | **0** | 无内容替换动画 |
| **`Crossfade`** | **0** | 无交叉淡入 |
| **`animateDpAsState`** | **0** | 无尺寸/位置补间 |
| **`updateTransition`** | **0** | 无多属性联合动画 |
| **`animateItem`** | **0** | 列表增删/重排**无动画** |

### 1.4 触碰反馈覆盖面（本轮最重的发现）

- `LocalIndication` / `ProvideLocalIndication` 全库**从未覆盖**（grep 结果为空）。
- 因此 `Modifier.clickable` / `combinedClickable` / `selectable` 全部落到 **Material 默认涟漪**。
- 全库此类调用点共 **110 处**（`TodayScheduleScreen.kt` 23、`ManageCourseTablesScreen.kt` 10、`AgendaScreen.kt` 9 ……）。
- 自研 `Modifier.touchFeedback`（`StyleComponents.kt:198`）全库调用点 **1 处**：`ScheduleGrid.kt:304`（周课表课程块）。

**覆盖率 1 : 110。** 三套主题共用同一套 Material 涟漪。

### 1.5 触摸反馈是死设置

- `TouchFeedbackStyle`（`AppMotion.kt:93-111`）含 `RIPPLE` / `HALO` / `NONE`，并带 `fromString`（`:108`）——形似可持久化设置。
- 但 `AppSettingsModel.kt` **无对应字段**，设置页**无入口**，`fromString` **无调用方**。
- 唯一调用点 `ScheduleGrid.kt:240` 硬编码：`val touchFeedbackStyle = remember { TouchFeedbackStyle.RIPPLE }`。

**结论：HALO 与 NONE 不可达；全 App 实际生效的触摸反馈恒为 Material 式涟漪。** 且时长曲线硬编码（`StyleComponents.kt:145-151` 400ms 扩散、`:156-164` 220ms 淡出、`:175` 缓动 `(0.32,0.72,0,1)`），不随主题、不随动画风格变化。

### 1.6 今日页课程卡动效断链（本轮新增·严重）

`TodayContent` 的三套主题分支**全部提前 `return@Column`**：

- `TodayScheduleScreen.kt:454-471` 柔绘 → `return@Column`
- `:474-491` 通透 → `return@Column`
- `:494-509` 书卷 → `return@Column`

而带按压反馈 + 入场错峰的课程卡组件 `CourseTimelineItem`（`:2352`，动效在 `:2405-2480`）**只在兜底分支被调用**（`:612`，位于 `TodayContent` 第 512 行之后的不可达区）。

三套主题各自真正使用的课程卡，动效命中数均为 **1**，且是同一个**二值切换**：

| 组件 | 行号 | 全部动效 |
|---|---|---|
| `SoftTimelineCard` | `:3298` | `graphicsLayer(alpha = if (isFinished) 0.6f else 1f)` |
| `Ios26TimelineCard` | `:4510` | 同上 |
| `ClaudeTimelineCard` | `:1072` | 同上 |

**结论：今日页的课程卡在全部三套主题下都没有按压反馈、没有入场动画；「已结束课程降透明」是硬跳。** 同时 `TodayScheduleScreen.kt:556` 读取的 `todayEntranceMotion` 也在不可达分支内。

### 1.7 其它量化事实

- 入场错峰延迟 = `stagger × 序号`（`ScheduleGrid.kt:249`、`WeeklyScheduleScreen.kt:1083`）。满周约 30 块时：GLASS `40ms × 30 ≈ 1.2s`，GENTLE `70ms × 30 ≈ 2.1s`；期间 `alpha = entranceFraction`（`ScheduleGrid.kt:301`）**完全不可见**。
- 入场记忆方式不一致：`ScheduleGrid.kt:246` 用 `remember`（旋转/主题切换会重播），`WeeklyScheduleScreen.kt:1138` 与 `TodayScheduleScreen.kt:2422` 用 `rememberSaveable`。
- 拖拽落位无吸附：`ScheduleGrid.kt:411-412` 直接赋值 `bodyDragOffsetX/Y`，由 `placeRelative`（`:599`）落位 → **硬跳**；尺寸调整（`topHandleDragOffsetY`，`:493/:499`）同理。
- 当前节次高亮瞬切：`ScheduleGridComponents.kt:320-334` 由 `produceState` + 小时流驱动，跨整点直接换色。
- 顶栏无滚动动效：`AppTopAppBar.kt:90-145` 的 `AppNavigationBar` 是固定 48dp，无大标题折叠、无标题淡出/缩放。
- 无减弱动效降级：全库无 `MotionDurationScale` / `reduceMotion` 读取，应用内无「减弱动态效果」开关。
- 动效预览仅覆盖 2/5 分组：`AnimationSettingsScreen.kt:218-275` 只演示「错峰入场」与「按压胶囊」，导航转场 / 周翻页 / 底栏隐藏三组无预览。
- 全库无下拉刷新（`PullToRefresh` / `SwipeRefresh` 均为 0 命中）。

---

【现有动画问题清单】

## 柔绘主题（SOFT）

> 气质：柔和晕染、虚化大圆角、无锐利硬边缘、低对比薄涂、朦胧肌理、漫射柔光、干净留白、**流畅色彩过渡（明文写入规格：节点 4~5 个、过渡连续无折点）**。关键词「化开、慢、无声」。

- **问题点 1 · 周换气是「整屏白蒙版」，且被双重缓动压扁**
  - 现象：`WeeklyScheduleScreen.kt:974-990`。落定后 `sin(fraction × π)` 驱动 `drawRect(color = Color.White.copy(alpha = 0.03f * breathe))`，即给**整个课程区叠一层白色**。同时 `fraction` 本身已被 `CubicBezierEasing(0.45, 0, 0.55, 1)`（对称 ease-in-out）缓动过一次。
  - 冲突原因：两条。① **深色模式下白蒙版 = 闪白**，而柔绘深色是暖炭薄涂底，"换气"被读成"打闪"，与「漫射柔光、无声」相反；② **缓动叠加**——`sin(π·f)` 本身已是单峰曲线，外面再套一条 ease-in-out，使亮度峰值被压缩到中段极窄区间、两端几乎不动，观感是"停顿—突亮—停顿"三段，而非一次柔和的起伏。这也正是柔绘自己禁止的"折点"。
  - 修改建议：① 去掉 `sin` 与缓动二选一——保留 `sin(π·f)` 则 `fraction` 走**线性**；② 亮度调制改为**随主题方向性**——浅色向 `#FFFFFF` 调、深色改用 `Color.Black.copy(alpha = 0.02f)` 或 `blendMode` 提亮而非叠白，避免深色闪白；③ 幅度从 0.03 降到 0.015–0.02（柔绘底色已接近白，白上加白的容忍度更低，`SoftStyle.kt:442-443` 的注释已警示过这一点）。

- **问题点 2 · 全站 110 处交互元素都是 Material 涟漪，而柔绘只有 1 处自研反馈**
  - 现象：`LocalIndication` 全库从未覆盖（§1.4），110 处 `clickable/selectable` 全部使用 Material 默认涟漪：硬边圆形/矩形波纹 + 高亮层扩散。自研 `touchFeedback` 仅覆盖周课表课程块 1 处。
  - 冲突原因：涟漪具有**清晰外缘 + 中心扩散方向 + 瞬时高亮**，与柔绘「无锐利硬边缘 / 化开 / 薄涂」正面冲突。柔绘把「无硬边」贯彻到了描边、投影、圆角、纹理，却在这一层完全失守——而这是用户每次点击都会看到的反馈。
  - 修改建议：在 `ShangKeScheduleTheme` 内按 `themePreset` provide `LocalIndication`：柔绘用**软边径向浓度渗开**（无环边、无中心高光点、500ms 扩散 / 320ms 淡出 / ease-in-out）；同时把自研 `touchFeedback` 从「课程块专用」提升为可选全局方案，避免两套反馈并存。

- **问题点 3 · 按压缩放会带着「双层软投影」与「羽化描边环」一起抖动**
  - 现象：`ScheduleGrid.kt:293-302` 的 `graphicsLayer { scaleX/scaleY = pressScale; translationY = ... }` 包住整个课程块；块内 `CourseBlock.kt:236` 施 `softShadow(elevation = 12.dp)`（**双层** `Modifier.shadow`，`SoftStyle.kt:417-433`），`:250` 施 `softFeatherRim`（1.2dp 外层 + 2.2dp 内缩层的渐变描边，`SoftStyle.kt:482-525`）。
  - 冲突原因：`graphicsLayer` 缩放是**整层光栅缩放**，投影的模糊半径与偏移量、描边的 1.2dp 线宽会**随缩放一起变化**。柔绘的投影是刻意做"软边"的双层扩散影、描边是做"化开"的两道渐变环——它们在 3% 缩放下的形变，恰恰是柔绘最不能容忍的边缘抖动。且 `cellLiftDp` 1dp 的上抬让投影偏移量也在变。
  - 修改建议：柔绘档**禁用缩放与位移**，改为**透明度/浓度响应**（按下时卡片薄涂底浓度 +8%、或 `softGlow` 光斑强度提亮，180ms 起、500ms 指数回落）；这样 `softShadow` 与 `softFeatherRim` 全程零形变，材质保持静止。

- **问题点 4 · 入场错峰的总延迟最高约 2.1s，且期间卡片不可见**
  - 现象：`ScheduleGrid.kt:249` 与 `WeeklyScheduleScreen.kt:1083` 的延迟为 `stagger × 序号`；`ScheduleGrid.kt:301` 用 `alpha = entranceFraction`。GLASS 40ms×30 ≈ 1.2s，GENTLE 70ms×30 ≈ 2.1s。
  - 冲突原因：柔绘是"慢"的主题，但**慢不等于让内容延迟出现**。满周课时，最后几块课程在 1–2 秒内完全不可见，用户看到的是逐格"长出来"的网格，而不是一次柔和的浮现；这与柔绘"低对比（不靠动效吸引注意）+ 干净留白"的诉求相反——动效本身变成了主角。
  - 修改建议：① 错峰改为**按行（天）而非按块**，或对错峰总量设上限（如 `min(stagger × idx, 240ms)`）；② 入场位移改为不改变可见性起点——起始 `alpha` 提到 0.35 而非 0；③ 柔绘档推荐纯淡入、无位移、错峰上限 300ms。

- **问题点 5 · 触摸/按压时长全部硬编码，柔绘拿不到慢节奏**
  - 现象：`StyleComponents.kt:145-151`（400ms）、`:156-164`（220ms）、`:175`（缓动常量）全部写死；数值不读 `LocalAppMotion`。
  - 冲突原因：柔绘的节奏锚点（间距 22/18/56、圆角 24/28）比另两套主题整体慢一档，但触摸反馈是全 App 唯一一处**完全不受主题与动画风格影响**的时长。用户即使把动画风格切到最舒缓的 `GENTLE`，涟漪仍是 400ms 的 iOS 节奏。
  - 修改建议：把扩散/淡出时长与缓动改为读 `LocalAppMotion` 令牌（复用 `expandDurationMs` / `colorDurationMs` 或新增 `touchExpandMs`），并把柔绘档的默认值设为 500 / 320ms、对称缓动。

- **问题点 6 · Tab 选中态瞬切——柔绘规格里明文要求的那一条没有实现**
  - 现象：`NavigationComponents.kt:351-353` 选中胶囊底色 `if (isSelected) resolvedIndicatorColor else Color.Transparent` 直接切换；`:365-371` 图标瞬时替换；`:376` 字号 11sp↔12sp 瞬时跳。全库 `animateColorAsState` 仅 1 处（开发者图标），此处不在其中。
  - 冲突原因：`SoftStyle.kt` 规格明文写了「**流畅色彩过渡** —— 渐变节点 4~5 个、色相跨度 ≤30°，过渡连续无折点」。底栏是全屏触发最频繁的元素，却恰好是全站唯一完全没有色彩过渡的地方；且由于柔绘选中胶囊是**低对比薄涂色**，瞬切在薄涂底上会读成一次"闪"，折点感反而比另两套主题更明显。
  - 修改建议：选中胶囊用 `animateColorAsState` + 位置/宽度补间（柔绘 420ms 对称缓动）；图标 220ms `Crossfade`；字号**不参与**动画（避免文本重排），选中态改由字重与颜色承载。

- **问题点 7 · 加载态是 Material 硬边圆弧 spinner**
  - 现象：`AppBasicComponents.kt:143-147` `AppLoading` 直接 `CircularProgressIndicator()`，无主题分支；被导入、备份、校选择等流程复用。
  - 冲突原因：硬边圆弧 + 匀速旋转是几何语言，柔绘没有提供任何替代；`SoftStyle.kt` 里做了 `softFeatherRim` 这一类"化开的边"，却在唯一的加载指示器上用了最锐利的圆弧。
  - 修改建议：柔绘档改「三点柔光呼吸」（三枚低 alpha 圆点依次亮度起伏，1800ms/轮）或「羽化环慢转」（角速度非线性、边缘羽化 ≥6dp）。

- **问题点 8 · 空状态零动画，且未使用已有的柔光母题**
  - 现象：`AppBasicComponents.kt:112-137` `AppEmptyState` 无任何动画与主题分支；`TodayScheduleScreen.kt:2826-2903` 的 `EmptyStateView` 动效命中数为 **0**；`AgendaScreen.kt:316` 空态同样是静态的 `AppEmptyState`。
  - 冲突原因：柔绘定义里有「漫射柔光」母题且已实现 `AppHeroMotif` 呼吸光块（`StyleComponents.kt:472-505`），空态却完全没用；用户在一片空的页面上看到一个静止胶囊，材质语言在此断档。
  - 修改建议：空态整体 700ms 淡入 + 内部柔光斑**单次** 1.4s 呼吸（不循环），静止后保留极微微光。

- **问题点 9 · 悬浮件进出仍是 iOS 弹窗式缩放；今日页课程卡按压反馈丢失**
  - 现象：① `emphasisInitialScale` 默认 0.88（`AppMotion.kt:228`）／灵动档 0.72（`:264`），用于挂起条（`FloatingCourseBar.kt:74-83`）与「回到本周」圆钮（`WeeklyScheduleScreen.kt:881-893`）。② 今日页三套主题的课程卡都只有二值 alpha（§1.6），柔绘的 `SoftTimelineCard`（`:3298`）同样没有按压反馈。
  - 冲突原因：① 0.88–0.72 的居中缩放是"从屏幕中心弹出"的空间语义，柔绘的悬浮件应当"从底部渗上来"；且缩放会强调硬轮廓，与虚化边缘冲突。② 柔绘的课程卡是薄涂柔光材质，**浓度响应**才是它的天然语言，但这一层完全没做，用户在今日页点课程卡没有任何视觉回落。
  - 修改建议：① 柔绘档悬浮件改为纯位移淡入（`translationY` +12dp → 0），取消缩放；② 为 `SoftTimelineCard` 补浓度响应（同问题点 3 的方案），与周课表保持一致。

## 书卷主题（CLAUDE）

> 气质：暖砂纸底（`#FAF9F5`）+ 白卡 + 赤陶（`#C96442`）、圆角克制（16 卡 / 12 胶囊）、留白 20dp、Poppins/Newsreader/Lora 衬线阅读字体、**不透明分组卡（`claudeGroupBg()` + 0.5dp 实色描边）**。关键词「纸、静、端庄」。

- **问题点 1 · 全站 Material 涟漪落在纸上**
  - 现象：同柔绘问题点 2——110 处交互元素使用 Material 默认涟漪，书卷无任何覆盖；自研 `touchFeedback` 同样只覆盖课程块 1 处。
  - 冲突原因：书卷的卡片是**不透明纸 + 0.5dp 实色描边**（`StyleComponents.kt:353-355`），涟漪的扩散波纹会在卡面上"游动"，与纸的静态质感冲突；而且涟漪是 Android 系统语言，与书卷精心维持的 Anthropic 设计系统语言无关。
  - 修改建议：书卷档的 `LocalIndication` 改为**底色加深**（无波纹、无扩散），180ms 加深一档、240ms 回落；这也是纸质界面的通用做法。

- **问题点 2 · 导航转场仍是 iOS 整屏横推 + 1/3 视差**
  - 现象：`App.kt:167-170` 新页 `slideInHorizontally(initialOffsetX = { it })` 整屏推入、旧页 `targetOffsetX = { -it / 3 }` 左移淡出，350ms；`popTransitionSpec`（`:181-184`）与 `predictivePopTransitionSpec`（`:192-195`）同口径。`transitionSpec` 内**只判断 `navAnimEnabled` 与是否主屏**，无任何主题分支。
  - 冲突原因：书卷是 inset grouped 布局 + 20dp 宽松留白 + 衬线阅读语境，需要"沉静"。整屏横推会让分组卡的左右留白在滑动过程中反复"呼吸"，破坏书卷刻意经营的稳定边距；1/3 视差是移动端手势语言，与"纸页"无关。
  - 修改建议：书卷档改为**层叠轻推**——新页从右侧 12–16dp 处淡入（不整屏位移），旧页原地仅降 6% 不透明度；380–420ms 对称缓动。保留"进入下一层"的方向感，不让纸面移动。

- **问题点 3 · 弹簧过冲施加在纸卡上**
  - 现象：`AppMotion.kt:216-219` `IosBouncySpring`（dampingRatio 0.62）为默认 `emphasisScaleSpec`；`:212-215` `IosSnappySpring`（0.86）为默认 `pressSpec` / `cellPressSpec`。书卷全部沿用。
  - 冲突原因：过冲是**能量与活泼**的表达；书卷的静态感（暖砂底 + 衬线字 + 克制圆角）与之相反。且书中卡片带 0.5dp **对称实色描边**，任何缩放都会让描边在动画中呈现厚度不均。
  - 修改建议：书卷档强制 `dampingRatio = 1.0`（临界阻尼）、`stiffness = Medium`，取消全部过冲。

- **问题点 4 · 课程块按压是「把纸卡捏小」，今日页更是毫无反馈**
  - 现象：`cellPressScale` 默认 0.97 / 灵动档 0.94（`AppMotion.kt:232, 268`）、`cellLiftDp` 1–3dp；周课表生效于 `ScheduleGrid.kt:293-302`。今日页 `ClaudeTimelineCard`（`:1072`）**连按压反馈都没有**，只有二值 alpha。
  - 冲突原因：书卷课程块是**不透明色卡 + 左侧色条 + 0.5dp 描边**（`CourseBlock.kt`），按压缩放会让色条、描边、圆角三者同时位移，在静止纸面上形成一块"会动的色斑"；而今日页干脆没有任何按下反馈，等于同一张卡在两个页面有/无反馈两套行为。
  - 修改建议：书卷档统一改为**底色加深一档 + 描边加深**（`animateColorAsState` 160ms / 抬起 240ms），不缩放不位移；并把今日页课程卡纳入同一套反馈。

- **问题点 5 · 底部面板与对话框走 M3 默认 spec，且绕过动画开关**
  - 现象：`AppBasicComponents.kt:335-381` `AppGlassBottomSheet` 用 `rememberModalBottomSheetState()` + `ModalBottomSheet`，柔绘仅分支了 `scrimColor`（`:346`）与 haze 参数（`:371-373`，均为视觉），**未传任何 `enter/exit` spec**；`CourseDetailBottomSheet`、`WeekSelectorBottomSheet`（`:66`）均经它实现。全部 `AlertDialog`（`App.kt:301`、`CourseTablePickerDialog.kt:126`、`AppDangerDialog`）同为 M3 默认。
  - 冲突原因：M3 默认面板是 snappy spring + 明显 scrim 渐入，节奏偏"工具化"，与书卷的阅读节奏不符；更关键的是——这是**全 App 唯一不受「动画效果」设置控制的一类动效**，用户在设置里关掉全部动画分组，面板与弹窗照旧弹跳。
  - 修改建议：书卷档指定 sheet spec——进入 420ms ease-out 并伴随 24dp 位移（"纸页从下方抽出"）、退场 300ms、scrim 380ms 淡入至 ≤0.32；对话框改 0.98→1 + 280ms 淡入（取消缩放回弹）。同时把 sheet / dialog 纳入 `AnimationGroup` 体系。

- **问题点 6 · 加载与空态完全没有书卷语言**
  - 现象：`AppLoading`（`AppBasicComponents.kt:143-147`）= M3 圆弧，无主题分支；`AppEmptyState`（`:112-137`）静态无主题分支。
  - 冲突原因：书卷自带「课程格纸母题」`AppHeroMotif`（`StyleComponents.kt:472-505`，含呼吸块）与衬线字阶，却在空态与加载里一个都没用；在 Newsreader / Lora 的衬线语境下，几何圆弧 spinner 尤其突兀。
  - 修改建议：加载改「墨点扩散」（低 alpha 圆自中心向外做 1200ms 浓度波）；空态用 `AppHeroMotif` 单次 1.2s 淡入 + 一次呼吸后静止。

- **问题点 7 · Tab 选中瞬切，让不透明胶囊「闪」一下**
  - 现象：`NavigationComponents.kt:351-384` 底色、图标、字号全部瞬切。
  - 冲突原因：书卷底栏选中胶囊是**不透明分组底色**（非半透明），从透明到实色的瞬切在暖砂静态底上会被读成一次闪烁；这与主题"静态、端庄"的定位冲突。
  - 修改建议：选中胶囊 280ms ease-out 平移 + 底色渐变；图标 160ms `Crossfade`；页面内容 260ms 淡入。**节奏比柔绘紧一档**（书卷是"克制"，不是"缓慢"）。

- **问题点 8 · Snackbar 是 M3 默认上滑**
  - 现象：`StyleComponents.kt:790-805` `AppSnackbarHost` 只替换了形状与配色，动效未改。
  - 冲突原因：M3 snackbar 从底部上滑、固定时长自动消失，节奏偏急促；书卷的"沉静"没有体现在这个频繁出现的反馈上。
  - 修改建议：改为底部 12dp 位移 + 淡入 300ms、停留时长随文案自适应（下限 2.4s）、退场 220ms。

## 通透主题（IOS · iOS 26 Liquid Glass）

> 气质：Apple 系统色、Liquid Glass（半透明 + 背景模糊 + 镜面高光 + 边缘折射环）、连续圆角 26/22/16/12/10、SF 字阶、**物理弹簧（`.smooth` / `.snappy` / `.bouncy`）**。关键词「跟手、流体、精确」。

- **问题点 1 · 全站涟漪——iOS 主题里最显性的「伪 iOS」**
  - 现象：110 处交互元素使用 Material 默认涟漪（§1.4）。自研反馈仅在 `ScheduleGrid.kt:240` 硬编码 `RIPPLE`，且其注释自称「类似 iOS 列表 cell 按压」（`AppMotion.kt:98`）。
  - 冲突原因：两条。① **iOS 完全没有涟漪**——iOS 列表 cell 按压是底色 highlight + 极轻微 scale，波纹环是 Material 语言；通透的规格是"完全参照 iOS 26"，这条与主题的核心主张正面冲突。② 自研 `touchFeedback` 的 `RIPPLE` 画的是**环形径向渐变**（`StyleComponents.kt:233-269`），它不是 iOS 的 highlight；`HALO`（`:275-323`）更是"梦幻光晕"，离 iOS 更远。
  - 修改建议：通透档 provide `LocalIndication` 为 **HIG highlight**——140ms 底色加深一档 + 0.98 scale，抬起 240ms 用 `.smooth` 弹簧回位，**不画波纹**；并在 iOS 档把自研 `touchFeedback` 从课程块上撤下（避免叠加两层反馈）。

- **问题点 2 · 导航转场是固定 tween，不跟手、不可打断**
  - 现象：`App.kt:147-151` 用 `tween<IntOffset>(navDurationMs, IosSheetEase)`；`predictivePopTransitionSpec`（`:187-197`）把同一段 tween 再写一遍，**并未接入预测性返回的手势分数**。
  - 冲突原因：iOS 26 的 push/pop 是**跟手 + 松手弹簧收束**的交互式转场，位移与手势进度绑定、可中途反向。当前是一段不可打断的固定 350ms 曲线——iOS 最核心的"跟手"手感在通透主题里缺位，且 `predictivePop` 名不副实（只换了曲线，没换机制）。
  - 修改建议：nav 转场由 `tween` 改为 `spring<IntOffset>(dampingRatio = 1.0, stiffness = StiffnessMediumLow)`；`popTransitionSpec` / `predictivePopTransitionSpec` 接入手势进度（`NavDisplay` 的 predictive 分数或 `AnchoredDraggable`），实现"松手按剩余距离决定完成或取消"。

- **问题点 3 · Tab 选中无过渡，且缺 SF Symbol 变体切换**
  - 现象：`NavigationComponents.kt:351-384` 背景、图标（两套 drawable 直接替换）、字号全部瞬切。全库 `Crossfade` / `AnimatedContent` 均为 0。
  - 冲突原因：iOS Tab Bar 选中态是有过渡的（图标符号变体切换、选中色渐变），iOS 26 更进一步带胶囊 morph。当前实现让底栏成为全 App 唯一零动效的核心控件，与通透"流体、跟手"的定位冲突。
  - 修改建议：图标用 `Crossfade` 做 180–220ms `.snappy` 变体切换；选中色 `animateColorAsState` `.smooth`；若保留选中胶囊则用弹簧滑动。

- **问题点 4 · 顶部导航栏没有任何滚动动效**
  - 现象：`AppTopAppBar.kt:90-145` 的 `AppNavigationBar` 是固定 48dp 行 + 固定字重，随滚动**零变化**；无大标题折叠、无标题淡出/缩放。
  - 冲突原因：iOS 26 的 large title → inline title 折叠是签名行为，靠连续插值完成。当前顶栏是一个永远不动的条——"层级靠留白与分组卡分层"的 iOS 语言，在滚动这个最高频交互上完全没有落实。
  - 修改建议：通透档接入滚动进度：滚动 0→56dp 区间内，标题从 `largeTitle` 字号与左对齐连续插值到 17sp 居中小标题，同时透明度交叉；顶部玻璃底衬的着色随进度渐显（不是阈值切换）。

- **问题点 5 · 底部面板未使用 iOS detent 与弹簧；对话框绕过动画开关**
  - 现象：同书卷问题点 5——`AppGlassBottomSheet`（`AppBasicComponents.kt:335-381`）与全部 `AlertDialog` 走 M3 默认 spec，不受 `LocalAppMotion` 控制；`WeekSelectorBottomSheet` 亦经它实现。
  - 冲突原因：iOS 26 sheet 是**detent 分档 + 弹簧 + 可拖拽中途停靠**；M3 默认是固定 expand/collapse 的 snappy spring，量程、超程阻尼、scrim 曲线都不是 iOS 手感。且与主题无关的"绕过动画开关"这一缺陷在通透下同样存在。
  - 修改建议：`SheetState` 设 `skipPartiallyExpanded = false` 并自定量程/或自绘 detent；进入用 `.smooth` 弹簧、scrim 320ms 淡入；把手拖拽区加大并加超程阻尼；`Alert` 用 0.96→1 + 260ms `.snappy`、退场 180ms。并把 sheet / dialog 纳入动画分组开关。

- **问题点 6 · 悬浮件用「居中弹出」而非「从下方升起」**
  - 现象：`emphasisInitialScale` 0.88（默认）/ 0.72（灵动），用于挂起条与圆钮（`FloatingCourseBar.kt:74-83`、`WeeklyScheduleScreen.kt:881-893`）。
  - 冲突原因：0.88 的居中缩放是旧版 iOS Alert 的空间语义；iOS 26 的浮动控件惯例是从底部方向 scale + fade 升起，或沿边缘滑入。用居中缩放表达浮动件，空间关系与 iOS 26 不符。
  - 修改建议：改为 `scaleIn(initialScale = 0.9) + slideInVertically(initialOffsetY = { it / 4 })`，阻尼 1.0；右下角圆钮沿最小弧线升起（`translationY` +8dp）。

- **问题点 7 · 加载 spinner 是 Material 圆弧，空状态静止**
  - 现象：`AppLoading` = M3 `CircularProgressIndicator`（`AppBasicComponents.kt:143-147`）；`AppEmptyState`（`:112-137`）与今日页 `EmptyStateView`（`:2826-2903`，动效命中 0）均无动画与主题分支。
  - 冲突原因：iOS 用 8 段线段明暗循环（`UIActivityIndicator`）或 iOS 26 圆环进度，M3 弧线是 Android 语言；空状态在 iOS 惯例中是 SF Symbol + 一次性 spring 显现，静止出现让"层级靠留白"的语言失去节奏。
  - 修改建议：加载改 8 段线条依次明暗（1200ms / 每段 150ms 相位差）或圆环进度带 spring 起始；空态 `scaleIn(0.92) + fadeIn` 400ms `.smooth`，图标先于文字 60ms 错峰，**静止后不加循环动画**（HIG 明确反对常驻装饰动效）。

- **问题点 8 · 周扫光在 iOS 语境下是多余装饰**
  - 现象：`WeekPagerGlassSheen`（`WeeklyScheduleScreen.kt:991-1008`）在非柔绘档画一道 28% 屏宽、峰值 alpha 0.07 的白色斜光带。
  - 冲突原因：iOS 26 的周切换就是 Pager 原生位移 + 落定收束，**没有扫光**。扫光是装饰性特效，与 HIG「动效只服务于空间关系、不做无意义装饰」相悖；同时它抢走了本该给橡皮筋边界与落定收束的注意力。
  - 修改建议：通透档关闭扫光（把该分组在 iOS 下映射为"仅橡皮筋 + 弹簧收束"），预算转移到 Pager 边界阻尼与落定 `.smooth`。

---

【缺失动画清单】

> 覆盖 9 个典型场景：页面转场 / 卡片点击 / 弹窗弹出收起 / 列表滚动 / Tab 切换 / 下拉刷新 / 加载动画 / 空状态 / 数据状态变更。
> 全主题空白项：**下拉刷新**、**列表增删补位（`animateItem` 零使用）**、**拖拽落位吸附**、**顶栏滚动折叠**、**减弱动效降级**。

## 柔绘主题（SOFT）

- **页面转场**：「化开」式转场——新页 600ms 对称缓动淡入 + 6dp 上浮，旧页慢速降至 0.85 不透明度后淡出。禁止整屏横推（横向位移会打断晕染画面的连续性）。
- **卡片点击**：浓度响应——按下时该卡薄涂底浓度 +8%（180ms 起、500ms 指数回落），或 `softGlow` 光斑提亮。**零缩放、零位移**，保证 `softShadow` 双层投影与 `softFeatherRim` 羽化环全程零形变（这是柔绘问题点 3 的根治方案）。
- **弹窗弹出/收起**：弹窗 0.97→1 + 620ms 淡入（ease-in-out）；scrim 500ms 淡入、终值 ≤0.28（低对比，不压死底层晕染）；收起 380ms 反向更快。落地时需同时把 `AppGlassBottomSheet` 的 `enter/exit` spec 真正接上令牌。
- **列表滚动**：卡片首次进入视口 520ms 淡入 + ≤4dp 上浮，错峰取 `min(90ms × 序号, 300ms)` 并**设总量上限**（解决满周 2.1s 延迟）；滚动过程不做视差；到顶/到底 12dp 柔性回弹（`dampingRatio = 1.0`，无过冲）。
- **Tab 切换**：选中胶囊 420ms 色相 + 位置平滑迁移（对齐「过渡连续无折点」规格）；图标 220ms `Crossfade`；页面内容 300ms 淡入，不做横滑。
- **下拉刷新**：顶部出现**羽化柔光环**（非箭头、非硬环），半径与浓度随下拉距离连续增大；松手后环心 900ms 向内聚拢并减速旋转；完成 500ms 淡出。
- **加载动画**：三点柔光呼吸（三枚低 alpha 圆点依次亮度起伏，1800ms/轮），或单圈羽化环慢转（角速度非线性、边缘羽化 ≥6dp）。
- **空状态**：700ms 整体淡入 + 内部柔光斑**单次** 1.4s 呼吸后静止（不循环），静止后保留极微微光。
- **数据状态变更**：① 课程块新增从 0.4 浓度渐显至目标浓度（360ms）；② 删除时浓度降至 0 后回收（300ms），其余卡片 260ms 浓度回升补位；③ 同一课程改时间/节次**仅重新晕染颜色（260ms）不移动位置**（避免纸面"跳一下"）；④ 当前节次高亮跨整点时用 400ms 浓度渐变替代瞬切；⑤ 拖拽落位改用 320ms 临界阻尼弹簧吸附到目标格并伴随一次浓度回落。

## 书卷主题（CLAUDE）

- **页面转场**：「纸页层叠」——新页从右侧 14dp 处淡入（380ms ease-out），旧页原地仅降 6% 不透明度并轻微压暗；返回反向 300ms。禁止 1/3 视差与整屏横移（留白边距必须稳定）。
- **卡片点击**：描边与底色加深——0.5dp 描边与卡片底色在 160ms 内各加深一档，抬起 240ms 回落；不缩放不位移。今日页 `ClaudeTimelineCard` 同步补上（当前完全没有反馈）。
- **弹窗弹出/收起**：面板从下方 24dp 升起 + 380ms 淡入（"抽出纸页"），scrim 320ms 淡入至 ≤0.32，收起 280ms；对话框 0.98→1 + 280ms 淡入，**不用缩放回弹**。
- **列表滚动**：仅首次进入时 400ms / 60ms 错峰淡入；滚动过程完全静态（阅读流不应被动效干扰）；到达顶部时分组卡做一次 ≤4dp 微下沉。列表增删补位用 260ms 位置补间（需先引入 `animateItem`）。
- **Tab 切换**：选中胶囊 280ms ease-out 平移 + 底色渐变；图标 160ms 淡换；页面内容 260ms 淡入。节奏比柔绘紧一档（克制而非缓慢）。
- **下拉刷新**：顶部一道细横线（0.5dp、赤陶 60% alpha）从中心向两侧拉出，长度随下拉比例；松手后横线收成一点并 500ms 淡出。用「一条线」替代 spinner，贴合纸面克制的版式语言。
- **加载动画**：「墨点扩散」——一点低 alpha 圆自中心向外做 1200ms 浓度波（α 0.35→0.05），背景保持纸色。
- **空状态**：静态分组卡式空态 + 一次性 1.2s 淡入；配 `AppHeroMotif`（课程格纸母题）单次呼吸后静止。
- **数据状态变更**：① 新增色卡 200ms 淡入 + 一次 1.0→1.02→1.0 极微缩放（`dampingRatio = 1.0`，提示"已加入"）；② 删除 240ms 淡出、其余卡片 260ms 位移补位；③ **「已结束课程降透明」由二值跳变改为 400ms 不透明度渐变**（当前 `graphicsLayer(alpha = if (isFinished) 0.6f else 1f)` 是硬跳）；④ 当前节次高亮跨整点用 400ms 底色渐变；⑤ 拖拽落位 300ms 临界阻尼弹簧吸附。

## 通透主题（IOS · iOS 26）

- **页面转场**：真 iOS push/pop——新页位移**跟手**，松手用 `.smooth` 弹簧（`dampingRatio = 1.0`、`StiffnessMediumLow`）收束；返回支持中途反向手势（按剩余距离决定完成或取消）；Tab 之间保持 `None`（iOS 不做 Tab 横滑）。
- **卡片点击**：HIG highlight——140ms 内底色加深一档 + 0.98 scale（`.snappy`），抬起 240ms `.smooth` 回位，**不画波纹**。今日页 `Ios26TimelineCard` 同步补上。
- **弹窗弹出/收起**：sheet 从底部升起用 `.smooth` 弹簧、scrim 320ms 淡入，支持 mid-detent 停靠与拖拽超程阻尼；`Alert` 0.96→1 + 260ms（`.snappy`），退场 180ms。
- **列表滚动**：**不做**逐项错峰入场（iOS 列表默认无此项，逐项入场本身即伪 iOS）；改为滚动边缘橡皮筋拉伸/回弹（`dampingRatio ≈ 0.9`）、滚动条 240ms 淡入淡出、**大标题随滚动连续插值折叠**（替代当前完全静止的顶栏）。
- **Tab 切换**：图标 SF 变体 morph（180ms `.snappy`）+ 选中色 `.smooth`；若有选中胶囊则弹簧滑动；点击已选中 Tab 回到该页顶部（`animateScrollToItem`）。
- **下拉刷新**：`UIRefreshControl` 式——环形进度随下拉位移从 0 填充至 100%，松手后旋转加速 + 400ms `.smooth` 收束回顶；完成 200ms 淡出。
- **加载动画**：8 段线段依次明暗（1200ms，每段 150ms 相位差），或 iOS 26 圆环进度带 spring 起始。
- **空状态**：SF Symbol 式图标 + 文字，`scaleIn(0.92) + fadeIn` 400ms `.smooth`，图标先于文字 60ms 错峰；静止后无循环动画。
- **数据状态变更**：① 新增课程卡从 0.94 scale + 淡入（320ms `.bouncy`）；② 删除后列表用 `.smooth` 弹簧补位（260ms）；③ **「已结束课程降透明」改 400ms 不透明度渐变**；④ 开关/分段控件用 `.snappy` 弹簧（当前 `AppSegmentedControl` 选中胶囊是**瞬移**，`StyleComponents.kt:866-886`）；⑤ 当前节次/当前小时高亮跨点时 400ms 底色渐变；⑥ 数字与时长变化用 `AnimatedContent` 做 200ms 数值滚动；⑦ 拖拽落位 280ms `.smooth` 弹簧吸附。

---

## 附 A：修复优先级（按代码事实排序）

| 优先级 | 事项 | 影响主题 | 依据 |
|---|---|---|---|
| **P0** | 按主题 provide `LocalIndication`（柔绘=软边渗开 / 书卷=底色加深 / 通透=HIG highlight） | 全部 | 110:1 的覆盖率差，是用户每次点击都会感知的落差 |
| **P0** | sheet / dialog 接入 `LocalAppMotion` 并纳入分组开关 | 全部 | 用户关掉全部动画后仍弹跳，属功能缺陷 |
| **P0** | 今日页课程卡动效断链修复（三套 `*TimelineCard` 补按压 + 入场） | 全部 | §1.6，动效落在不可达分支，等于今日页丢失全部课程卡反馈 |
| **P0** | 「已结束课程降透明」由二值跳变改渐变 | 全部 | 三处 `graphicsLayer(alpha = if (isFinished) 0.6f else 1f)` |
| **P1** | 为 `LocalAppMotion` 增加 themePreset 覆盖层（阻尼 / 时长 / 曲线按主题分档） | 全部 | 总纲性修复；当前动效级主题分支仅 1 处 |
| **P1** | 修正柔绘周换气（去双重缓动、深色不叠白、幅度下调） | 柔绘 | §柔绘问题点 1 |
| **P1** | Tab 选中态补齐过渡（位置 + 颜色 + 图标） | 全部 | 三套主题均缺；柔绘规格已明文要求 |
| **P1** | 导航转场按主题分档（通透改弹簧 + 交互式返回） | 通透 · 书卷 | 与主题核心主张冲突 |
| **P1** | 入场错峰设总量上限 + 提高起始 alpha | 全部 | 满周总延迟 1.2–2.1s 且期间不可见 |
| **P2** | 拖拽落位吸附、列表增删补位（引入 `animateItem`） | 全部 | 场景缺口 |
| **P2** | 加载 / 空状态 / 下拉刷新三处主题化 | 全部 | 场景缺口 |
| **P2** | 顶栏滚动折叠、当前节次高亮渐变 | 通透 · 全部 | 场景缺口 |
| **P2** | `TouchFeedbackStyle` 要么接入设置并持久化，要么从代码中删除 | 全部 | 当前是含 `fromString` 的死设置，HALO/NONE 不可达 |
| **P3** | 触摸反馈时长改读令牌；三处入场记忆统一为 `rememberSaveable`；动效预览补齐另外 3 个分组 | 全部 | 一致性 |
| **P3** | 补「减弱动态效果」降级路径（Reduce Motion 用溶解替代位移/缩放） | 全部 | 无障碍缺口 |

## 附 B：结论一句话

三套主题的**材质分化已经做得很细**（柔绘 50+ 处分支、通透/书卷各成体系），但**动效分化只做了 1 处**；同时全站 110 处交互元素共用 Material 涟漪，而精心实现的自研反馈只覆盖 1 处、且其中两种样式不可达。因此三套主题目前的观感差异主要来自"静态皮肤"，一旦发生交互（点击、切 Tab、开弹窗、拖拽），差异立刻收敛到同一套 iOS 26 + Material 混合语言上。

> 说明：本文仅为动效层面分析，不涉及配色、排版、字体与界面结构的任何改动。

---

## 附 C：修复状态（v3.43.0(177) · 16 项全量已修复）

> 本节由修复实施方补写，逐项对齐「附 A」的 16 条。基线 `d330bcb`（v3.42.0）→ 修复提交 v3.43.0(177)。

| 优先级 | 事项 | 状态 | 落地位置 |
|---|---|---|---|
| P0 | 按主题 provide `LocalIndication` | ✅ 已修复 | 新增 `ui/theme/ThemeIndication.kt`；`Theme.kt` 注入 `LocalIndication` |
| P0 | sheet / dialog 接入 `LocalAppMotion` 并纳入分组开关 | ✅ 已修复 | `AppBasicComponents.kt`（`AppGlassBottomSheet` + 新增 `AppAlertDialog` / `rememberDialogEnterMotion()`）+ 30 处对话框迁移 |
| P0 | 今日页课程卡动效断链修复 | ✅ 已修复 | `TodayScheduleScreen.kt`（`rememberTodayCardMotion` 接入三套 `*TimelineCard`） |
| P0 | 「已结束课程降透明」二值跳变改渐变 | ✅ 已修复 | 三处 `graphicsLayer` 改按 `MotionTokens.statusFadeMs` 补间 |
| P1 | 为 `LocalAppMotion` 增加 themePreset 覆盖层 | ✅ 已修复 | `AppMotion.kt`（`ThemeMotionProfile` / `MotionPressMode` / `NavMotionMode` / `MotionTokens.withThemePreset`） |
| P1 | 修正柔绘周换气 | ✅ 已修复 | `WeeklyScheduleScreen.kt`（去双重缓动、深色不叠白、幅度下调） |
| P1 | Tab 选中态补齐过渡 | ✅ 已修复 | `NavigationComponents.kt` 共享胶囊指示器；`StyleComponents.kt` `AppSegmentedControl` 同构 |
| P1 | 导航转场按主题分档 | ✅ 已修复（交互式返回见下） | `App.kt`（SLIDE 弹簧横推 / FADE_UP 淡入上浮 / LAYER_PUSH 纸页层叠） |
| P1 | 入场错峰设总量上限 + 提高起始 alpha | ✅ 已修复 | `AppMotion.kt`（`entranceStaggerCapMs` 240–300ms、`entranceInitialAlpha = 0.35f`） |
| P2 | 拖拽落位吸附、列表增删补位 | ✅ 已修复 | `ScheduleGrid.kt`（`animateSpringSettle`）；`QuickDeleteScreen.kt`（`Modifier.animateItem()`） |
| P2 | 加载 / 空状态 / 下拉刷新三处主题化 | ✅ 已修复 | `AppBasicComponents.kt`（`ThemedLoadingIndicator`、`AppEmptyState` 入场）；`TodayScheduleScreen.kt`（`PullToRefreshBox`）+ `TodayScheduleViewModel.refresh()` |
| P2 | 顶栏滚动折叠、当前节次高亮渐变 | ✅ 已修复 | `AppTopAppBar.kt`（玻璃分支按 `collapsedFraction` 折叠）；`ScheduleGridComponents.kt`（高亮四要素同步补间） |
| P2 | `TouchFeedbackStyle` 死设置清理 | ✅ 已删除 | `StyleComponents.kt` 移除 `TouchFeedbackState` / `drawRipple` / `drawHalo` / `TouchEaseOut` 及死导入（−218 行） |
| P3 | 触摸时长读令牌；入场记忆统一 `rememberSaveable`；预览补齐 3 组 | ✅ 已修复 | `AppMotion.kt`（`touchExpandMs/touchFadeMs/touchEasing`）；`TodayScheduleScreen.kt`；`AnimationSettingsScreen.kt`（新增「转场 / 滚动预览」） |
| P3 | 补「减弱动态效果」降级路径 | ✅ 已修复 | `AppMotion.kt`（`MotionTokens.reduced()` / `ThemeMotionProfile.reduced()`）；`AppSettingsModel/Repository/ViewModel`；设置页开关 |

### 装机验证记录（真机 Redmi 22041216UC · versionCode 177）

- **交互动效专项 19 项全过**（`tools/verify_motion_v343.py`）：下拉刷新、课程详情面板、动效设置页新预览 + 3 个新分组
  + 减弱动效开关、30 处 `AppAlertDialog` 迁移后的主题切换确认框，均可正常交互；**全程 FATAL 0 条、进程未重启**。
- **掉帧实测**（`tools/verify_soft_jank.py`，同一滚动负载）：
  | 主题 | 课表页卡顿率 | 今日页卡顿率 | p50 | p99（课表） |
  |---|---|---|---|---|
  | 柔绘 | 6.31% | 4.15% | 8ms | 117ms |
  | 书卷 | 4.55% | 2.76% | 6–7ms | 73ms |
  | 通透 | 4.63% | 1.99% | 6–7ms | 65ms |
  ⇒ 柔绘已回到与两套基线主题**同量级**（1.37× / 1.75×）；残留：柔绘课表 p99 = 117ms 为最差单帧，待单独排查。
- **静态像素互斥复核通过**：跨主题签名色 ≈ 0，柔绘我的页 `soft_card` 101.1 万 px 与 v3.42.0 基线吻合 ⇒ 本轮零视觉回归。

### 本轮明确未做（遗留）

1. **交互式返回手势（swipe-back）**：「导航转场按主题分档」已落地三种形态，但 P1 附注的「交互式返回」需自建手势层
   （Compose Navigation 无原生可交互转场），未在本轮范围。
2. **顶栏折叠的接入面**：`AppTopAppBar` 玻璃分支已支持折叠，但当前仅 `AnimationSettingsScreen` 显式接入
   （`exitUntilCollapsedScrollBehavior` + `Modifier.nestedScroll`）；其余 29 处 `AppTopAppBar` 调用点可按同一 3 行模式接入。
   另注：本次核查发现 **0 处调用点向 `AppTopAppBar` 传 `hazeState`**，即玻璃分支实际未被使用——该分支的折叠此前属「参数被静默丢弃」，现已修正。
3. **`animateItem` 覆盖面**：目前接入 `QuickDeleteScreen`（删除场景最典型）；今日页待办、课程表管理等其余列表可按需接入。
4. **真机手感走查**：三套主题的动效差异需装机主观验收（编译三端已通过，静态像素不变）。
