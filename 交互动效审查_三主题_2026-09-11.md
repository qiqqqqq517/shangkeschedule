# 「上课 Shangke」交互动效审查 · 柔绘 / 书卷 / 通透

> 审查范围：全 App 交互动效。仅做动效分析，不涉及配色、排版、字体、界面绘制。
> 代码基线：`shared/src/commonMain`（v3.41.x）。审查日期：2026-09-11。

---

## 0. 总纲：当前动效与主题的关系（一切问题的根源）

三套主题共用**同一套**全局动效令牌 `LocalAppMotion`，默认档位为 `GLASS`（= iOS 26 物理弹簧语言）：

- `AppMotion.kt:208-238` —— `GlassTokens` 的弹簧取自 SwiftUI `.smooth/.snappy/.bouncy`（dampingRatio 1.0 / 0.86 / 0.62），缓动取 iOS 的 `cubic-bezier(0.32,0.72,0,1)`。
- `AppSettingsModel.kt:160` —— `animationStyle` 默认 `GLASS`，全局唯一，**与 `themePreset` 无任何绑定**。
- `Theme.kt:70` —— 注入时只读动画风格与分组开关，不读主题。
- 全库仅 `LocalIsSoftTheme` 做主题分流，且只用于顶栏透明度（`AppTopAppBar.kt:95`）与材质，**完全不参与动效**。

结论：本质问题是「**一套 iOS 26 弹簧语言被无差别套用到柔绘与书卷两种非 iOS 气质上**」，外加若干场景根本没有任何动画。因此每套主题的问题，一半来自「错的语言」，一半来自「缺失的语言」。修复总纲是为 `LocalAppMotion` 增加**主题覆盖层**（themePreset → 令牌覆盖），而不是继续加用户可选项。

另有两处系统性缺口，三套主题共同受害：

1. **底部面板与对话框未接入动效令牌**：`AppGlassBottomSheet`（`AppBasicComponents.kt:307-342`）、全部 `AlertDialog`（`App.kt:301`、`CourseTablePickerDialog.kt:126`、`AppDangerDialog` 等）均使用 M3 默认 spec。后果是用户在「动画效果」里关掉全部动画后，弹窗与面板**照旧弹跳**。
2. **下拉刷新完全不存在**：全库检索不到 `PullToRefresh` / `SwipeRefresh`，刷新语义只能靠切页或重进。

---

【现有动画问题清单】

## 柔绘主题（SOFT）

> 主题气质：柔和晕染渐变、虚化大圆角、无锐利硬边缘、低对比薄涂、朦胧肌理、漫射柔光、干净留白、**流畅色彩过渡（明确写入规格：节点 4~5 个、过渡连续无折点）**。关键词是「化开、慢、无声」。

- **问题点 1 · 周切换扫过一道镜面玻璃光带**
  - 现象：`WeeklyScheduleScreen.kt:938-991` 的 `WeekPagerGlassSheen`，翻页落定时一道 28% 屏宽的白色斜光带（alpha 0.07）扫过整个课程区，320ms。该层只受 `AnimationGroup.WEEK_PAGER` 门控，**与主题无关，柔绘同样命中**。
  - 冲突原因：柔绘全站「禁用 1dp 实色描边、改用羽化渐变环」、材质是薄涂雾面 + 朦胧噪点肌理。而扫光具备**明确方向、明确边界、瞬时高亮**三要素，是玻璃/金属的镜面语言；斜向光带落在雾面薄涂底上会切出一条可见的「锋面」，与「化开」的材质定义直接对立，是全 App 里与柔绘气质最冲突的一处。
  - 修改建议：柔绘档禁用扫光位移。改为「整屏换气」——翻页落定后课程区做一次 600–800ms、幅度 ≤3% 的整体明度正弦起伏（无方向性）；或最简方案：柔绘下把 `WEEK_PAGER` 组映射为「内容重播淡入」，彻底不画光带。

- **问题点 2 · 按压是「几何硬缩放 + 上抬」**
  - 现象：课程格 `cellPressScale` 0.97 / `cellLiftDp` 1dp（`AppMotion.kt:231-232`），切到灵动档为 0.94 / 3dp（`:267-268`）；FAB 与悬浮件 `pressScale` 0.97。今日页课程卡同样走这套（`TodayScheduleScreen.kt:2382-2389`）。
  - 冲突原因：柔绘的边缘是「羽化渐变环 + 扩散投影」，本身带模糊；任何 2–6% 的缩放都会让**羽化边缘在 1–2 帧内位移**，模糊边缘一动就是闪。再叠 1–3dp 的纵向抬升，等于承认有一条硬边在移动。柔绘需要的是「颜色洇开」，不是「卡片被捏紧」。
  - 修改建议：柔绘档改为**零缩放 + 浓度响应**：按下时课程块整体不透明度或内部漫射光斑强度 +8%（180ms），抬起 500ms 指数回落；若必须保留触觉暗示，缩放不超过 0.995；`cellLiftDp` 归 0（柔绘无「上抬」语义）。

- **问题点 3 · 入场节奏偏快、位移偏大、错峰偏密**
  - 现象：默认入场 320ms / 位移 10dp / 错峰 40ms（`AppMotion.kt:233`），施加于网格课程块、列表块、今日页卡片（`ScheduleGrid.kt:244-257`、`WeeklyScheduleScreen.kt:1108-1126`、`TodayScheduleScreen.kt:2392-2408`）。
  - 冲突原因：柔绘的间距被整体放宽一档（页边距 22 / 卡距 18 / 行高 56），是「慢下来」的节奏基准；320ms + ease-out 是 iOS 的果决节奏。在宽松留白里，10dp 的上移反而会被读成「跳了一格」而不是「浮现」，错峰 40ms 也让卡片显得拥挤。
  - 修改建议：柔绘档入场 520–600ms、位移压到 4–6dp（或纯淡入）、错峰 90–110ms，缓动改为**对称 ease-in-out**，让卡片「浮现」。

- **问题点 4 · 弹簧过冲用在低对比薄涂面上**
  - 现象：`emphasisScaleSpec = IosBouncySpring`（dampingRatio 0.62，`AppMotion.kt:216-219`），悬浮件从 0.88 弹到 1.0；部分档位还会让 `hideFraction` 过冲（代码注释已记录 `WeeklyScheduleScreen.kt:871`、`NavigationComponents.kt:309-310` 曾因此崩溃或需钳制）。
  - 冲突原因：过冲是「能量感」。柔绘规格是低对比、安静、漫射，一次 6% 的过冲在薄涂底上会被放大为边缘闪烁；且过冲溢出需要 `coerceIn` 兜底，说明该曲线与柔绘的稳定诉求不合。
  - 修改建议：柔绘档全档位 `dampingRatio = 1.0`（临界阻尼，无过冲），`stiffness` 取 `StiffnessLow`/`VeryLow`；悬浮件初始缩放抬到 0.96–0.97（几乎不缩），主要靠淡入表达出现。

- **问题点 5 · 触摸反馈是 iOS 涟漪/光晕，且时长曲线写死**
  - 现象：`TouchFeedbackStyle.RIPPLE`（注释自称「类似 iOS 列表 cell 按压」）与 `HALO`（注释自称「更梦幻更玻璃」），绘制为**环形径向渐变 + 中心高光点**（`StyleComponents.kt:233-323`）；时长 400ms 扩散 / 220ms 淡出、缓动 `(0.32,0.72,0,1)` **全部硬编码**（`:145-151, 156-164, 175`），既不读主题也不读 `LocalAppMotion`。
  - 冲突原因：涟漪环与光晕都是玻璃/系统语言，柔绘没有对应项；环形有清晰内外边界，而柔绘要求「无锐利硬边缘」；硬编码意味着柔绘即使被指定为舒缓节奏，触摸反馈也不会跟着变慢。
  - 修改建议：新增柔绘专属样式 `BLEED`（晕染）：以触点为圆心的**软边径向浓度渐变**，无环形边界、无中心高光点，扩散 500ms / 淡出 320ms / ease-in-out；并把时长与缓动改为读 `LocalAppMotion`，不再写死。

- **问题点 6 · Tab 选中态瞬切，色彩无过渡**
  - 现象：`NavigationComponents.kt:351-353` 选中胶囊底色 `if (isSelected) resolvedIndicatorColor else Color.Transparent` 直接换；图标 `:365-371` 瞬时替换；选中字号 11sp↔12sp 瞬时跳变（`:376`）。
  - 冲突原因：柔绘规格里**明文写了「流畅色彩过渡 —— 节点 4~5 个、色相跨度 ≤30°、过渡连续无折点」**。底栏是全屏触发最频繁的元素，却恰好是全站唯一完全没有过渡的地方，等于规格写了一条、实现漏了一条。
  - 修改建议：选中胶囊用 `animateColorAsState` + 位置/宽度 `animateDpAsState` 平滑迁移（柔绘 420ms 对称缓动）；图标 220ms 交叉淡入；字号**不参与动画**（避免文本回流抖动），选中态改由字重 + 颜色承载。

- **问题点 7 · 加载态是 Material 硬边圆弧 spinner**
  - 现象：`AppLoading` 直接使用 M3 `CircularProgressIndicator`（`AppBasicComponents.kt:130-134`），被导入流程、备份、校选择列表等复用。
  - 冲突原因：硬边圆弧 + 匀速旋转是几何玻璃语言，柔绘没有提供任何替代材质；匀速旋转也缺乏柔绘应有的「非匀速柔感」。
  - 修改建议：柔绘档改「三点柔光呼吸」（三个低 alpha 圆点依次亮度起伏，1800ms 一轮）或「羽化环慢转」（角速度非线性、边缘羽化 ≥6dp）。

- **问题点 8 · 空状态零动画，浪费了已有柔光母题**
  - 现象：`AppEmptyState` 是静态胶囊 + 文案（`AppBasicComponents.kt:100-124`），无任何进入或呼吸动效。
  - 冲突原因：柔绘定义里有「漫射柔光」母题，且 `AppHeroMotif` 已实现呼吸光块（`StyleComponents.kt:472-505`），但空态完全没用上——材质语言在空态处断档。
  - 修改建议：空态整体 700ms 淡入 + 内部柔光斑 **1 次** 1.4s 亮度呼吸（只播一次不循环），静止后保留极微微光，避免常驻动画打扰。

- **问题点 9 · 悬浮件进出是 iOS 弹窗式缩放**
  - 现象：`emphasisInitialScale = 0.88`（默认档）→ 0.72（灵动档），挂起条与「回到本周」圆钮都用它（`FloatingCourseBar.kt:74-83`、`WeeklyScheduleScreen.kt:877-888`）。
  - 冲突原因：0.72–0.88 的居中缩放是「弹窗从屏幕中心弹出」的空间语义；柔绘的悬浮件应当「从底部渗上来」，而大幅缩放会强调硬边轮廓。
  - 修改建议：柔绘档悬浮件进出改为**位移淡入**（`translationY` +12dp → 0，alpha 0 → 1），取消缩放；若保留则 ≤0.97 且不带弹簧。

---

## 书卷主题（CLAUDE）

> 主题气质：暖砂纸底（`#FAF9F5`）+ 白卡 + 赤陶（`#C96442`）、圆角克制（16 卡 / 12 胶囊）、留白宽松 20dp、Poppins / Newsreader / Lora 衬线阅读字体、**不透明分组卡（`claudeGroupBg()` + 0.5dp 实色描边）**、「靠留白与分组卡分层，不用线框」。关键词是「纸、静、端庄」。

- **问题点 1 · 纸面上扫过一道玻璃高光**
  - 现象：同柔绘问题点 1，`WeekPagerGlassSheen` 全主题生效（`WeeklyScheduleScreen.kt:938-991`）。
  - 冲突原因：书卷的卡片是**不透明**的（`StyleComponents.kt:338-359` 用 `claudeGroupBg()` + `claudeGroupBorder()` 实色描边；`FloatingCourseBar.kt:90-101` 同样）。在一个声明为「不透明纸」的表面上出现镜面反光，是材质层面的自相矛盾——纸不反光。
  - 修改建议：书卷档移除扫光层；翻页改为「纸页轻推」——新旧周内容各做 8–10dp 的水平微位移 + 交叉淡入（**不要** 1/3 视差），保留方向感而不破坏纸面稳定。

- **问题点 2 · 导航转场是 iOS 整屏横推 + 1/3 视差**
  - 现象：`App.kt:167-170` 新页 `slideInHorizontally(initialOffsetX = { it })` 整屏推入，旧页左移 1/3 并淡出，350ms。
  - 冲突原因：书卷是 inset grouped 布局 + 宽松留白 + 阅读语境，需要「沉静」。整屏横推是移动端手势语言；更糟的是整屏滑动会让分组卡的左右留白在滑动中反复「呼吸」，破坏书卷刻意营造的稳定边距。
  - 修改建议：书卷档改为**层叠轻推**——新页从右侧 12–16dp 处淡入（不做整屏位移），旧页原地仅降 6% 不透明度；时长 380–420ms 对称缓动。既表达「进入下一层」，又不让纸面移动。

- **问题点 3 · 弹簧过冲施加在纸卡上**
  - 现象：默认 `emphasisScaleSpec = IosBouncySpring`（0.62）、`pressSpec = IosSnappySpring`（0.86）。
  - 冲突原因：书卷的气质是「谦逊的纸」。弹跳是活泼/拟物的表达，与暖砂纸底 + 衬线字体的沉静语境冲突；且书卷的卡片带 0.5dp 对称描边，任何缩放都会让描边厚度在动画中不均。
  - 修改建议：书卷档强制 `dampingRatio = 1.0`，`stiffness = Medium`，取消全部过冲。

- **问题点 4 · 课程块按压是「把纸卡捏小」**
  - 现象：`cellPressScale` 0.97 / 0.94 + `cellLiftDp` 1–3dp。
  - 冲突原因：书卷课程块是**不透明色卡 + 左侧色条 + 0.5dp 描边**，按压缩放会让「色条 + 描边 + 圆角」三者同时位移，在静止的纸面上产生一块会动的色斑。
  - 修改建议：书卷档改为**底色加深一档 + 描边加深**（`animateColorAsState` 160ms），不缩放、不位移。这也与「靠留白与分组卡分层」的静态语言一致。

- **问题点 5 · 底部面板与对话框走 M3 默认动效，既不是书卷语言，也绕过了动画开关**
  - 现象：`AppGlassBottomSheet` 用 `rememberModalBottomSheetState()` + `ModalBottomSheet` 默认 spec（`AppBasicComponents.kt:307-342`）；`CourseDetailBottomSheet.kt` 经它实现；全部 `AlertDialog` 亦为 M3 默认。
  - 冲突原因：M3 默认面板是 snappy spring + 明显的 scrim 渐入，节奏偏「工具化」，与书卷的阅读节奏不符；且这是**全 App 唯一不受「动画效果」设置控制的一类动效**——用户关掉全部动画，面板与弹窗照旧弹跳。
  - 修改建议：书卷档单独指定 sheet spec——进入 420ms ease-out（伴随 24dp 位移，模拟「纸页从下方抽出」）、退场 300ms、scrim 380ms 淡入至 ≤0.32；对话框改为 0.98→1 + 280ms 淡入。同时把 sheet/dialog 纳入 `AnimationGroup` 开关体系。

- **问题点 6 · 加载 / 空态 / 展开等次要动效完全没有书卷语言**
  - 现象：`AppLoading` = M3 圆弧；`AppEmptyState` 静态；`AnimatedVisibility` 展开（`DeveloperAndIconComponents.kt:112-120`）用 `expandDurationMs` tween，但仅服务「开发者选项」一处。
  - 冲突原因：书卷自带「课程格纸母题」（`AppHeroMotif`，`StyleComponents.kt:472-505`）与衬线字阶，却在空态/加载里一个都没用；在衬线字体语境下，几何圆弧 spinner 尤其突兀。
  - 修改建议：加载改「墨点扩散」（低 alpha 圆点自中心向外做 1200ms 浓度波，更贴近纸墨语境）；空态用 `AppHeroMotif` 单次 1.2s 淡入 + 一次呼吸后静止。

- **问题点 7 · Tab 选中瞬切，让不透明胶囊「闪一下」**
  - 现象：`NavigationComponents.kt:351-384` 全瞬切（底色、图标、字号）。
  - 冲突原因：书卷底栏胶囊是不透明分组底（`claudeGroupBg()`），选中态瞬间从透明变实色，在暖砂纸静态底上会被读成一次闪烁，与主题的静态感冲突。
  - 修改建议：选中胶囊 280ms ease-out 平移 + 底色渐变；图标 160ms 淡换；页面内容 260ms 淡入。时长比柔绘紧一档（书卷是「克制」不是「缓慢」）。

- **问题点 8 · Snackbar 是 M3 默认上滑**
  - 现象：`AppSnackbarHost` 只替换了形状与配色，未改动效（`StyleComponents.kt:790-805`）。
  - 冲突原因：M3 snackbar 从底部上滑弹出、固定时长自动消失，节奏偏急促，与书卷的沉静语境不符。
  - 修改建议：书卷档改为底部 12dp 位移 + 淡入 300ms，停留时间随文案长度自适应（下限 2.4s），退场 220ms 淡出。

---

## 通透主题（IOS · iOS 26 Liquid Glass）

> 主题气质：Apple 系统色、Liquid Glass（半透明 + 背景模糊 + 镜面高光 + 边缘折射环）、连续圆角 26/22/16/12/10、SF 字阶、**物理弹簧（`.smooth` / `.snappy` / `.bouncy`）**、层级靠留白。关键词是「跟手、流体、精确」。

- **问题点 1 · 触摸反馈自称 iOS，实则是 Android 涟漪**
  - 现象：`TouchFeedbackStyle.RIPPLE` 注释写「类似 iOS 列表 cell 按压」（`AppMotion.kt:98`），实际绘制是**环形径向渐变**（`StyleComponents.kt:233-269`）；且它是全局默认。
  - 冲突原因：iOS 列表 cell 按压的实际表现是 **highlight（底色瞬时加深）+ 极轻微 scale，没有涟漪环**；涟漪环是 Material 语言。通透的规格是「完全参照 iOS 26」，这条是最显性的「伪 iOS」，与主题的核心主张（HIG 一致性）正面冲突。
  - 修改建议：通透默认反馈改为 `HIGHLIGHT`——140ms 内 cell 底色加深一档 + 0.98 scale，抬起 240ms 用 `.smooth` 弹簧回落；`RIPPLE`/`HALO` 保留为可选但不再作为 iOS 主题默认（或让默认反馈跟随主题）。

- **问题点 2 · 导航转场是固定 tween，既非弹簧也非交互式**
  - 现象：`App.kt:147-151` 用 `tween<IntOffset>(navDurationMs, IosSheetEase)`；`predictivePopTransitionSpec`（`:187-197`）只是把同一段 tween 再写一遍。
  - 冲突原因：iOS 26 的 push/pop 是**跟手 + 松手弹簧收束**的交互式转场，位移与手势进度绑定；当前实现是一段不可打断的固定 350ms 曲线，中途反向手势无法跟随，`predictivePop` 也没有真正接入预测分数。iOS 最核心的「跟手」手感在通透主题里缺位。
  - 修改建议：把 nav 转场由 `tween` 改为 `spring<IntOffset>(dampingRatio = 1.0, stiffness = StiffnessMediumLow)`；`popTransitionSpec` / `predictivePopTransitionSpec` 接入手势分数（`AnchoredDraggable` 或 NavDisplay 的 predictive 进度），实现「松手后按剩余距离决定完成或取消」。

- **问题点 3 · 悬浮件是「居中弹出」，iOS 的浮动控件是「从下方升起」**
  - 现象：`emphasisInitialScale = 0.88` + `IosBouncySpring`，用于挂起条与「回到本周」圆钮。
  - 冲突原因：0.88 的居中缩放是旧版 iOS Alert 的空间语义；iOS 26 的浮动控件（玻璃胶囊、FAB）惯例是从底部方向 scale + fade 升起，或直接沿边缘滑入。用居中缩放表达浮动件，空间关系与 iOS 26 不符。
  - 修改建议：改为 `scaleIn(initialScale = 0.9) + slideInVertically(initialOffsetY = { it / 4 })`（从下方约 1/4 高度升起），阻尼 1.0；右下角圆钮沿最小弧线升起（`translationY` +8dp）。

- **问题点 4 · Tab 选中无任何过渡，缺 SF Symbol 变体切换**
  - 现象：`NavigationComponents.kt:351-384` 背景、图标（两套 drawable 直接替换）、字号全部瞬切。
  - 冲突原因：iOS Tab Bar 的选中态是有过渡的（图标变体切换、选中色渐变），iOS 26 更进一步有胶囊 morph；当前实现让底栏成为全 App 唯一零动效的核心控件，与通透「流体、跟手」的主张冲突。
  - 修改建议：图标用 `Crossfade` / `AnimatedContent` 做 180–220ms `.snappy` 变体切换；选中色 `animateColorAsState` `.smooth` 渐变；若保留选中胶囊则用弹簧滑动。

- **问题点 5 · 底部面板未使用 iOS detent 与弹簧**
  - 现象：M3 `ModalBottomSheet` 默认 spec。
  - 冲突原因：iOS 26 sheet 是 **detent 分档 + 弹簧 + 可拖拽中途停靠**，M3 默认是固定 expand/collapse 的 snappy spring；手势量程、超程阻尼、scrim 曲线都不是 iOS 手感。
  - 修改建议：`SheetState` 设 `skipPartiallyExpanded = false` 并自定量程，或自绘 detent；进入用 `.smooth` 弹簧、scrim 320ms 淡入；把手拖拽区加大并加超程阻尼。

- **问题点 6 · 加载 spinner 是 Material 圆弧，不是 iOS 指示器**
  - 现象：`AppLoading` = M3 `CircularProgressIndicator`。
  - 冲突原因：iOS 用 8 段线段明暗循环（`UIActivityIndicator`）或 iOS 26 圆环进度；M3 弧线是 Android 语言，在纯 iOS 主题里是材质断点。
  - 修改建议：通透档做 8 段线条依次明暗的 1200ms 循环（每段 150ms 相位差），或 iOS 26 `ProgressView` 圆环（带 spring 起始）。

- **问题点 7 · 空状态完全静态**
  - 现象：`AppEmptyState` 无进入动效。
  - 冲突原因：iOS 26 空状态惯例是 SF Symbol + 一次性 spring 显现（`scaleIn + fadeIn`，不循环）。静态出现让「层级靠留白」的 iOS 语言在空态处失去节奏。
  - 修改建议：入场 `scaleIn(0.92) + fadeIn` 400ms `.smooth`，图标先于文字 60ms 错峰；静止后**不加循环动画**（HIG 明确反对常驻装饰动效）。

- **问题点 8 · 周切换扫光在 iOS 语境下是多余装饰**
  - 现象：`WeekPagerGlassSheen` 白色 0.07 斜光带。
  - 冲突原因：iOS 26 的周切换就是 Pager 原生位移 + 落定收束，**没有扫光**。扫光是装饰性特效，与 HIG「动效只服务于空间关系、不做无意义装饰」的原则相悖——在通透主题里它既不是 iOS 语言，也抢走了本该给橡皮筋边界与落定收束的注意力。
  - 修改建议：通透档关闭扫光，把预算转移到 Pager 的橡皮筋边界阻尼与落定 `.smooth` 收束上。

---

【缺失动画清单】

> 覆盖典型交互场景 9 项：页面转场 / 卡片点击 / 弹窗弹出收起 / 列表滚动 / Tab 切换 / 下拉刷新 / 加载动画 / 空状态 / 数据状态变更。其中**下拉刷新为全主题空白**；`弹窗弹出收起` 目前全主题为 M3 默认且不受动画开关控制。

## 柔绘主题（SOFT）

- **页面转场**：「化开」式转场——新页 600ms 对称缓动淡入 + 6dp 上浮，旧页慢速降至 0.85 不透明度后淡出。禁止整屏横推（横向位移会打断晕染画面的连续性）。
- **卡片点击**：漫射光响应——点击时该卡内部漫射光斑亮度 +10%（180ms）→ 500ms 指数回落。零缩放、零位移，边缘羽化环保持静止不抖。
- **弹窗弹出/收起**：弹窗 0.97 → 1 缩放伴随 620ms 淡入（ease-in-out）；scrim 用 500ms 淡入且终值 ≤0.28 不透明度（低对比，不压死底层晕染）；收起 380ms，反向但更快。
- **列表滚动**：卡片首次进入视口时 520ms / 90ms 错峰淡入（`rememberSaveable` 记忆，滚动往返不重播）；滚动过程不做视差；到顶/到底用 12dp 柔性回弹（`dampingRatio = 1.0`，无过冲）。
- **Tab 切换**：选中胶囊 420ms 色相 + 位置平滑迁移（连续无折点，对齐规格）；图标 220ms 交叉淡入；页面内容 300ms 淡入（不做横向滑动）。
- **下拉刷新**：顶部出现**羽化柔光环**（非箭头、非硬环），半径与浓度随下拉距离连续增大；松手后环心 900ms 内向内聚拢并减速旋转；完成 500ms 淡出。全程无硬边、无高饱和。
- **加载动画**：三点柔光呼吸——三个低 alpha 圆点依次亮度起伏，1800ms 一轮；或单圈羽化环慢转（角速度非线性、边缘羽化 ≥6dp）。替代 M3 圆弧。
- **空状态**：一次性 700ms 整体淡入 + 内部柔光斑单次 1.4s 呼吸后静止（不循环），静止后保留极微微光。
- **数据状态变更**：新增课程块从 0.4 浓度渐显至目标浓度（360ms）；删除时浓度降至 0 后回收（300ms）；同一课程改时间/节次时**仅重新晕染颜色（260ms）而不移动位置**，避免纸面「跳一下」。

## 书卷主题（CLAUDE）

- **页面转场**：「纸页层叠」——新页从右侧 14dp 处淡入（380ms，ease-out），旧页留在原位仅降 6% 不透明度并被轻微压暗；返回反向 300ms。禁止 1/3 视差与整屏横移（留白边距必须保持稳定）。
- **卡片点击**：描边与底色加深——0.5dp 描边与卡片底色在 160ms 内各加深一档，抬起 240ms 回落；不使用缩放位移（对称描边不能有厚度变化）。
- **弹窗弹出/收起**：面板从下方 24dp 升起 + 380ms 淡入（「抽出纸页」），scrim 320ms 淡入至 ≤0.32，收起 280ms 下落；对话框改为 0.98 → 1 + 280ms 淡入，**不使用缩放回弹**。
- **列表滚动**：仅首次进入时 400ms / 60ms 错峰淡入；滚动过程完全静态（阅读流不应被动效干扰）；到达顶部时分组卡做一次 ≤4dp 的微下沉。
- **Tab 切换**：选中胶囊 280ms ease-out 平移 + 底色渐变；图标 160ms 淡换；页面内容 260ms 淡入。节奏比柔绘紧一档（克制而非缓慢）。
- **下拉刷新**：顶部一道细横线（0.5dp、赤陶 60% alpha）从中心向两侧拉出，长度随下拉比例；松手后横线收成一点并 500ms 淡出。用「一条线」替代 spinner，贴合纸面克制的版式语言。
- **加载动画**：「墨点扩散」——一点低 alpha 圆自中心向外做 1200ms 浓度波（α 0.35 → 0.05），背景保持纸色，更贴近纸墨语境。
- **空状态**：静态分组卡式空态 + 一次性 1.2s 淡入；配 `AppHeroMotif`（课程格纸母题）单次呼吸后静止。
- **数据状态变更**：新增课程 → 色卡 200ms 淡入并伴随一次 1.0 → 1.02 → 1.0 极微缩放（`dampingRatio = 1.0`，仅作「已加入」提示）；删除 → 240ms 淡出，其余卡片 260ms 位移补位（gap closing）；拖拽改时间落位用无过冲弹簧（`stiffness = Medium`）。

## 通透主题（IOS · iOS 26）

- **页面转场**：真 iOS push/pop——新页位移**跟手**，松手后用 `.smooth` 弹簧（`dampingRatio = 1.0`、`StiffnessMediumLow`）收束；返回支持中途反向手势（按剩余距离决定完成或取消）；Tab 之间保持 `None`（iOS 不做 Tab 横滑）。
- **卡片点击**：HIG highlight——140ms 内 cell/卡底色加深一档 + 0.98 scale（`.snappy`），抬起 240ms 用 `.smooth` 回到 1.0。**无涟漪**。
- **弹窗弹出/收起**：sheet 从底部升起用 `.smooth` 弹簧，scrim 320ms 淡入；支持 mid-detent 停靠与拖拽超程阻尼；`Alert` 用 0.96 → 1 + 260ms 淡入（`.snappy`），退出 180ms。
- **列表滚动**：**不做**逐项错峰入场（iOS 列表默认无此项，逐项入场反而是伪 iOS）；改为滚动边缘橡皮筋拉伸/回弹（`dampingRatio ≈ 0.9`）、滚动条 240ms 淡入淡出、大标题折叠时导航栏**连续插值渐显**（替代当前的阈值切换）。
- **Tab 切换**：图标 SF 变体 morph（180ms `.snappy`）+ 选中色 `.smooth` 渐变；若有选中胶囊则弹簧滑动；点击已选中 Tab 回到该页顶部（`animateScrollToItem`）。
- **下拉刷新**：iOS `UIRefreshControl` 式——环形进度随下拉位移从 0 填充至 100%，松手后旋转加速 + 400ms `.smooth` 收束回顶；完成 200ms 淡出。
- **加载动画**：8 段线段依次明暗循环（1200ms，每段 150ms 相位差）；或 iOS 26 圆环进度（带 spring 起始）。替代 M3 圆弧。
- **空状态**：SF Symbol 式图标 + 文字，`scaleIn(0.92) + fadeIn` 400ms `.smooth`，图标先于文字 60ms 错峰；静止后无循环动画。
- **数据状态变更**：新增课程卡从 0.94 scale + 淡入（320ms `.bouncy`）；删除后列表用 `.smooth` 弹簧补位（260ms）；开关/分段控件用 `.snappy` 弹簧；数字与时长变化用 `AnimatedContent` 做 200ms 上下滑动数值滚动。

---

## 附：修复优先级建议

| 优先级 | 事项 | 影响主题 | 依据 |
|---|---|---|---|
| P0 | 周切换扫光按主题分流（柔绘/书卷移除、通透关闭） | 柔绘 · 书卷 | 材质矛盾的显性冲突 |
| P0 | sheet/dialog 接入 `LocalAppMotion` 并纳入分组开关 | 全部 | 用户关动画后仍弹跳，属功能缺陷 |
| P0 | 触摸反馈默认样式随主题走（柔绘 `BLEED` / 通透 `HIGHLIGHT`） | 柔绘 · 通透 | 柔绘无对应语言；通透为「伪 iOS」 |
| P1 | 为 `LocalAppMotion` 增加 themePreset 覆盖层（阻尼、时长、曲线按主题分档） | 全部 | 总纲性修复，一次解决多数错配 |
| P1 | Tab 选中态补齐过渡（位置 + 颜色 + 图标） | 全部 | 三套主题均缺，且柔绘规格已明文要求 |
| P1 | 导航转场按主题分档（通透改弹簧 + 交互式返回） | 通透 · 书卷 | 与主题核心主张不符 |
| P2 | 加载 / 空状态 / 下拉刷新三处补齐主题化动效 | 全部 | 场景缺口 |
| P2 | 数据状态变更（新增/删除/补位）补齐动画 | 全部 | 场景缺口 |

> 说明：上表仅为动效层面建议，不涉及配色、排版、字体与界面结构的任何改动。
