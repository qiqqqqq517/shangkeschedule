# 「通透」Apple HIG 全站视觉核验报告

- 核验日期：2026-09-09
- 核验对象：`shared/src/commonMain` 全部 UI 代码（91 个 UI 文件）+ `App.kt` / `Navigation.kt`
- 分支：`qoder/动画`
- 核验方式：纯代码静态核验（对照 token 体系逐文件比对），**未改动任何代码、业务逻辑、点击事件、页面结构与功能入口**
- 判定基准：HIG「通透」10 项核对清单（大圆角/留白/磨砂/低饱和/淡阴影/字阶/浅分割线/胶囊控件/深浅双主题/轻柔动画）

---

## 一、结论摘要（TL;DR）

**当前工作区代码并未整体达到「通透 HIG」标准。** 且存在比逐项样式问题更前置的阻断性问题——本分支的实际代码与《工作日志》中 v3.28～v3.30 的描述**不一致**：被日志声明为「已完成并验证」的通透主题基础设施（AiryGridStyle、airy 深浅色板、AppAlertDialog 封装等）在当前代码树中**不存在或未接通**；同时 `Theme.kt` 有一处**未提交且未完成**的改动，会让 `:shared` 编译失败。

在「4 个底部 Tab」口径上也需澄清：当前代码底部导航实际只有 **3 个 Tab**（今日课表 / 课表 / 我的），无第 4 个 Tab。

因此本次核验结论按真实代码给出，而非按工作日志叙述。

---

## 二、重大前置事实（优先级最高，先于一切逐项样式修复）

### 事实 1｜通透主题专属课表样式缺失（日志声称存在）
- `data/model/AppThemePreset.kt`：`AIRY` 枚举**存在**，但 `gridStyle = ScheduleGridStyle.DEFAULT`。
- 全库搜索 **`AiryGridStyle` 无任何定义**。
- `ScheduleGridStyle.kt:74-87` 的 `DEFAULT_COLOR_MAPS` 是 **M3 马卡龙高饱和 12 色**（红 `#FFCDD2/#D32F2F`、粉、紫、靛蓝、蓝、青、绿、黄、橙……），`courseBlockCornerRadiusDp = 8f`、`hideGridLines = false`。
- 日志声称的「AiryGridStyle：14dp 圆角 / 70f 节高 / 隐藏网格线 / 12 色 Apple 低饱和淡色双套」**在当前树不存在**。
- **后果**：切到「通透」主题后，课表网格实际渲染为 M3 高饱和马卡龙色块 + 8dp 圆角 + 可见网格线，直接违反「低饱和、无刺眼高饱和色块、柔和大圆角」。

### 事实 2｜通透主题专属色板缺失（日志声称存在）
- `AppStyle.kt` 仅有通用 `lightAppColorTokens()` / `darkAppColorTokens()` 两套，**没有** `airyAppColorTokens` / `airyDarkAppColorTokens`。
- 全库搜索上述标识符 0 命中。
- 日志声称的「深灰 `#1C1C1E` 深色基底、Apple System Blue `#007AFF`、label/secondaryLabel 文字层级、深浅独立课表 12 色」**未落地**。
- 当前深色基底为通用近黑蓝灰 `#0F1115 / #191C23`（非纯黑但**不是** Apple 深灰 `#1C1C1E`）。

### 事实 3｜AppAlertDialog 组件化封装不存在（日志声称已建并迁移 18 处）
- 全库搜索 **`AppAlertDialog` 0 命中**。
- `ui/components/AppBasicComponents.kt` 内 `AppDangerDialog` 仍**直用原生 M3 `AlertDialog`**。
- 日志声称的「18 个文件批量迁移为 `import …AppAlertDialog as AlertDialog`」**在当前树不存在**。
- **后果**：全站弹窗仍为 M3 默认圆角 / 默认底色 / 默认遮罩，与「通透」玻璃语言割裂。

### 事实 4｜工作区存在编译阻断（未提交的 Theme.kt 中间态）
- `ui/theme/Theme.kt:103`（工作区未提交改动）调用 **`appColorTokens(darkTheme, themePreset)`（双参）**。
- `AppStyle.kt:179` 仅定义 **`fun appColorTokens(isDark: Boolean)`（单参）**，全库无双参重载。
- **后果**：当前工作区直接编译 `:shared` 会报 unresolved reference。该改动本身是想把「形状/间距/字阶 token 按主题预设注入 + MaterialTheme Shapes 同步」接上，但引用的双参色板分发函数尚未补齐。
- 处置建议（二选一）：① 补齐 `appColorTokens(isDark, preset)` 并新增 airy 双色板（推荐，朝向日志目标）；② 回退该未提交改动（回到单参版本）。

### 事实 5｜绝大多数页面没有接入 AIRY 专属 token 通道
- 主题化访问函数 `appShapes()/appSpacing()/appType()/appColors()` 全库只有 **27 个文件**调用。
- 大量页面仍引用**旧静态常量** `AppShape.` / `AppSpacing.` / `AppType.` / `AppMaterialShapes`（例如 `TodayScheduleScreen`、`WeeklyScheduleScreen`、`SettingsScreen`、`StyleComponents.kt`、`NavigationComponents.kt` 等）。
- 这些静态常量 = v2 基线值（卡片 20dp、页边距 16dp、卡距 12dp、行高 52dp、标题 16~24sp），**AIRY 的放大值（22dp / 20dp / 16dp / 56dp / 更大字阶）永远到不了这些页面**——即使事实 4 修好也一样。
- 根因点：`SettingsScreen.kt` 顶层 `SETTING_PADDING / ITEM_SPACING`（161-162 行，顶层常量无法读 CompositionLocal）+ `SettingItem/SettingCard` 的静态默认值（426/597/614 行）被 CoupleSchedule / Semester / MoreOptions / Backup / AdvancedSettings 等**全站设置页共享**，波及面最大。

### 事实 6｜底部 Tab 口径澄清（与「4 个 Tab」核验前提不符）
- `Navigation.kt`：`MainDestination` 仅 3 个：`TodaySchedule`（今日课表）、`CourseSchedule`（课表）、`Settings`（我的）。
- `NavigationComponents.kt:114-132` 底部导航也仅 3 项。
- 没有「首页」与「日程」两个独立 Tab；`今日课表` 页（TodayScheduleScreen）承担了今日课程 + 时间轴 + 待办日程展示。核验按当前实际 3 Tab + 全部二级页执行。

---

## 三、HIG 10 项清单逐项核验

| # | HIG 检查项 | 判定 | 关键证据 |
|---|---|---|---|
| 1 | 统一柔和大圆角 | ❌ 未达标 | 大量卡片直用 M3 `Card`/`MaterialTheme.shapes.medium`（12dp，如 CourseInstanceList:258、TweakScheduleScreen:347、TimeSlotManagement:707、ImportPreviewComponents:107）；多处静态 `AppShape.card`=20dp（AIRY 目标 22dp 拿不到）；课表课程块 8dp（DEFAULT grid style） |
| 2 | 充足留白 | ❌ 未达标 | 全站列表页普遍硬编码 `padding(horizontal=16.dp)` + `spacedBy(8.dp)`（CoupleSchedule、Semester、AddEditCourse:206、import 全部 6 页、TimeSlot 等 20+ 处），未走 `appSpacing()`（AIRY 应为 20/16）；气泡卡内 `padding(8.dp)` 过挤（TodaySchedule:1219） |
| 3 | 磨砂半透明材质 | ◐ 部分达标 | ✅ 底部胶囊导航（liquidGlass 4dp 雾）、FloatingCourseBar、AppGlassBottomSheet（cardBg 0.86 tint + 20dp 雾）已具玻璃感；❌ 弹窗遮罩走 M3 默认、普通卡片无磨砂、无 AppAlertDialog 玻璃弹窗 |
| 4 | 低饱和配色、无高饱和刺眼色块 | ❌ 未达标 | 课表 `DEFAULT_COLOR_MAPS` 高饱和马卡龙 12 色；语义危险色大量直取 M3 `colorScheme.error`（未映射，如 import 各页、CourseSchemeCard:117）；DeveloperAndIconComponents:49 写死 `#73CAF8` 亮青；ColorPicker 等选色功能内高饱和 HSV 渐变属功能豁免 |
| 5 | 极淡柔和阴影 | ❌ 未达标 | 玻璃件 `shadowElevation` 8/10/14dp 三值分裂（回到本周圆钮 8dp、挂起条 10dp、底栏 14dp）；QuickDeleteScreen:169 `tonalElevation=8+shadowElevation=8`；M3 Card 默认投影；无统一淡阴影 token 通路 |
| 6 | 字体层级清晰 | ❌ 未达标 | 标题多写 `MaterialTheme.typography.titleLarge/…` + 静态 `AppType`（hero 24sp 而非 36、pageTitle 20 而非 24、rowTitle 16 而非 17）；TodaySchedule 大日期/下节课卡字阶全部静态；`appType()` 全站接入率低 |
| 7 | 分割线极浅 | ❌ 未达标 | `HorizontalDivider()` 普遍不传 color → 回落 M3 `outlineVariant`（偏深）；网格线 `outline.copy(0.2f)`（ScheduleGrid:87，深色下偏亮）；多处 `colorScheme.outline/outlineVariant` 作边框/次文字（outline 不在 withAppSurfaces 重映射内） |
| 8 | 胶囊按钮 / iOS 控件规范 | ❌ 未达标 | 已具备 AppSegmentedControl / AppDialogActions / 胶囊 FAB 等正面件；但原生 M3 控件仍大面积直用：`Button/TextButton/OutlinedButton`（各处 CTA、WebDialogHost ×3、SettingsScreen 3 个选择器对话框）、`RadioButton`（MoreOptionsDialogs ×2、AnimationSettings:391、BackupScreen:427、LanguageSetting:128、WebDialogHost:161）、`Checkbox`（TodaySchedule:575）、`DropdownMenu`（WebViewScreen:294、TweakSchedule:319）、M3 `SearchBar`（SchoolSelectionList:337）、M3 `NavigationRail`（NavigationComponents:181）、PrimaryTabRow 下划线 Tab（SchoolSelectionList:291） |
| 9 | 深浅双主题完整适配、深灰基底 | ❌ 未达标 | 结构性颜色经 `withAppSurfaces` 深浅两套 token 映射较好（无大面积纯黑/纯白底）；但深色基底是近黑蓝灰而非 Apple `#1C1C1E`；无 AIRY 独立深色色板；大量 `Color.White` 硬编码前景（AppDialogActions:599、AppFab:137、AddEditCourse:313、QuickDelete:178、MoreOptionsDialogs:186）；周切换扫光 `White.copy(0.14)` 深色未降档；StyleSettings 色池写死纯白/纯黑底（功能豁免但需注释边界）；ImageCropper 恒黑底（iOS 裁剪语义豁免） |
| 10 | 轻柔缓动动画 | ✅ 已达标 | 动效经 `LocalAppMotion` token 全局下发（AppMotion.kt），按压反馈/入场/周翻页扫光均按风格+分组开关解析；v3.26 已修复过冲负 padding 与非重播；未发现残留生硬动画路径 |

---

## 四、待修复问题清单（按页面/组件分组）

> 行号为当前工作区文件实际行号；路径前缀 `ui/` = `shared/src/commonMain/kotlin/com/shangkeschedule/ui/`。

### A. 弹窗族：原生 M3 AlertDialog / DatePickerDialog 未主题化（共约 25 处）
| 文件:行 | 说明 |
|---|---|
| `ui/components/AppBasicComponents.kt:202` | AppDangerDialog 直用原生 AlertDialog（无 AppAlertDialog 封装可复用） |
| `App.kt:285-302` | 学期设置引导弹窗（且渲染于主题外） |
| `ui/today/TodayScheduleScreen.kt:619` | 待办编辑弹窗 |
| `ui/components/CourseTablePickerDialog.kt:124,204` | 主弹窗 / 新增课表弹窗 |
| `ui/components/DatePickerModal.kt:34` | M3 DatePickerDialog 直用，确认/取消为 TextButton |
| `ui/components/ShareManager.kt:37` | 分享弹窗 |
| `ui/schoolselection/web/WebDialogHost.kt:85,115,152` | Alert/Prompt/SingleSelection 三弹窗 + 实心 Button 主次不分 |
| `ui/schoolselection/web/WebViewScreen.kt:500` | 学期起始提示弹窗 |
| `ui/settings/SettingsScreen.kt:516,668,724,766` | 选色 / 周次 / 星期 / 数字 4 个选择器弹窗（按钮区为原生 Button） |
| `ui/settings/appearance/AppearanceSettingsScreen.kt:314` | 主题切换确认弹窗 |
| `ui/settings/additional/MoreOptionsDialogs.kt:65,110,158,203` | 启动页 / 渠道 / 更新结果 4 弹窗 |
| `ui/settings/style/StyleSettingsComponents.kt:457` | 数值输入弹窗 |
| `ui/settings/backup/BackupScreen.kt:409,465` | 目标选择 / WebDAV 配置弹窗 |
| `ui/settings/coursetables/ManageCourseTablesScreen.kt:190,231` | 新增 / 编辑课表弹窗 |
| `ui/settings/course/AddEditCourseScreen.kt:417` | 退出拦截确认弹窗（含空 dismissButton 占位） |
| `ui/settings/import/ImportPreviewComponents.kt:237` | 预览条目编辑弹窗 |
| `ui/settings/conversion/CourseTableConversionDialogs.kt:87,136` | ICS 导出 / 崩溃导入弹窗 |
| `ui/settings/time/TimeSlotManagementScreen.kt:391,594,1117` | 退出确认 / 新建方案 / 日期范围弹窗（1117 建议改底部玻璃面板） |
| `ui/settings/course/CourseTimeDialogs.kt:303-322` | 裸 `Dialog`+`Surface(shapes.small)` 12dp 中置窗 |
| `ui/settings/quickactions/delete/QuickDeleteScreen.kt:436` | M3 DateRangePicker 直用 |

**修复方向**：在 `AppBasicComponents.kt` 落地 `AppAlertDialog`（glass 容器 = cardBg + 大圆角 + 自定义轻遮罩 scrim + AppDialogActions 胶囊操作区），上表全部改走该封装（与日志 v3.30 目标一致，但本分支需重建）。

### B. AIRY token 未接通（静态 AppShape/AppSpacing/AppType 引用 → 放大值不生效）
- **今日课表页**（`TodayScheduleScreen.kt`，问题最集中）：顶部标题 193、大日期 354、下节课卡 hero 909/977、时间轴行距 399-400、备注气泡 1212-1235、空态容器 1245-1253 等**全文件未接入任何 appShapes()/appSpacing()/appType()**；页边距/留白硬编码 8-16dp。
- **课表页**：`WeeklyScheduleScreen.kt:386`(胶囊标题)、264/769/780/883(圆钮避让几何)、373(胶囊)；`WeekSelectorBottomSheet.kt:77`。
- **设置页族**（波及全站）：`SettingsScreen.kt:161-162`(顶层常量)、218/224、239(hero)、426/597/614(SettingItem 标题/行高默认)；`StyleSettingsComponents.kt:185-186/312/329`；`CoupleScheduleSettingsScreen.kt:175-176`；`SemesterSettingsScreen.kt:134-135`；`PersonalizedDisplayScreen.kt:75-76`；`LanguageSettingScreen.kt:108`；`BackupScreen.kt:375`；`NotificationSettingsScreen.kt:54`；`CourseNameListScreen.kt:380/389`；`QuickDeleteScreen.kt:194`；`CourseSchemeCard.kt:76`（方案卡圆角 14dp 静态）。
- **通用组件**（默认参数，影响面大）：`StyleComponents.kt:87`(AppCard 20→22)、120/160(heroCard 24→32)、305/318(TelegramMenuItem 行高/字号)、538(AppTextField 输入框 14→10)、356/581/595/599；`AppBasicComponents.kt:92/137/230/284/294`；`NavigationComponents.kt:216/309-323/361`；`AlphabetIndexerList.kt:131`；`AdvancedColorPicker.kt:281`；`ImageCropper.kt:216/258`；`DeveloperAndIconComponents.kt:74`。
- 建议：静态常量引用统一改 `appShapes()/appSpacing()/appType()`；`SettingsScreen` 顶层常量须下沉为 @Composable 内读取（CompositionLocal 无法在顶层读取）；`listGap` 未入间距 token 的问题需补 token 字段。

### C. 直取 MaterialTheme.colorScheme.* 绕过 appColors()（重点：未映射的字段会真分叉）
- **会真分叉（withAppSurfaces 未重映射）**：`colorScheme.outline / outlineVariant / secondary / tertiary / error / errorContainer / secondaryContainer`——
  - 网格线 `ScheduleGrid.kt:87`（outline 0.2）；课程块边框 `CourseBlock.kt:184-186`（outline）；时间列/表头文字部分路径；
  - `CourseOtherSelectors.kt:113-117`（secondary）；删除/危险色 `error`：CourseSchemeCard:117、import 各页错误文案、QuickDeleteScreen:486-489（errorContainer 未映射成 dangerSoft）、ExcelImport:130 等；
  - `SchoolSelectionListScreen.kt:245-252`（outline/outlineVariant）、`StyleSettingsComponents.kt:616/650`、`CourseInstanceListScreen.kt:267`、`ImportPreviewComponents.kt:112`。
- **已被映射但仍建议收口到 appColors()**（语义正确性/可维护性）：`onSurface/onSurfaceVariant/primary/primaryContainer/…` 在 TodaySchedule、WeeklySchedule、CourseDetailBottomSheet、WeekSelectorBottomSheet、ScheduleGridComponents（今日/当前节高亮 246/338-343）、SettingsScreen:563、MoreOptionsScreen:127、AppearanceSettingsScreen:519/539-541、AnimationSettingsScreen:148/172/370/406、GlassBlurScreen:340-349、AdvancedSettingsCard:55 等大量出现。
- 建议：`outline→appColors().divider`、`onSurfaceVariant→appColors().textSecondary`、`error→appColors().danger`、`errorContainer→dangerSoft`、`secondaryContainer→primarySoft`、`primaryContainer→primarySoft`。

### D. 分割线 / 网格线过深
- `HorizontalDivider()` 未显式 `color` 回落 `outlineVariant`：AppearanceSettings:225/254/414、AnimationSettings:142/166/270、GlassBlur:153、DeveloperAndIconComponents:135-140、StyleSettingsComponents:225/232/256、TimeSlotManagement:275。
- 建议：全局给未着色 Divider 显式 `color = appColors().divider`、thickness 0.5dp。

### E. 硬编码颜色 / 深浅失效风险
- 白字前景（深色模式对比风险）：`StyleComponents.kt:599-601`（danger/亮紫底白字）、`AppBasicComponents.kt:137`（AppFab 深色亮紫上白图标）、`MoreOptionsDialogs.kt:181-186`、`AddEditCourseScreen.kt:313`、`QuickDeleteScreen.kt:178`、`ImageCropper`（黑底白字，豁免）。
- 大块高饱和：`DeveloperAndIconComponents.kt:49-50`（亮青 `#73CAF8` + 红 `#BD0000`）；课表默认色板见事实 1。
- 遮罩/扫光深色未适配：`CourseBlock.kt:335-352`（黑/白遮罩 + 0.618 固定降级 alpha）、`WeeklyScheduleScreen.kt:966-973`（白色扫光 0.14 深色偏亮）、`App.kt:99`（主题外加载占位 Surface 用 M3 默认底色）。
- 今日待办条固定青色 `TimetableDefaults.todoCourseColor`（与语义色脱钩，AIRY 低饱和原则下不随主题调）。
- `StyleSettingsComponents.kt:310/334` 色池纯白/纯黑底（功能豁免，需注释圈层）。

### F. 卡片形态不统一（M3 Card 12dp / 默认投影）
`SchoolSelectionListScreen.kt:422-430`、`AdapterSelectionScreen.kt:200-209`、`AppearanceSettingsScreen.kt:489-496`、`ImportPreviewComponents.kt:107-114`、`CourseInstanceListScreen.kt:258-270`、`TweakScheduleScreen.kt:347`、`TimeSlotManagementScreen.kt:707-744`、`CourseSchemeCard.kt:72-104`（Card + 裸 M3 TextField）。
- 建议：统一换 `AppCard/AppSelectableCard` + `appShapes().card`，内边距走 `appSpacing().cardInner`。

### G. 页边距 / 留白硬编码（20+ 处，均未走 appSpacing().pageHorizontal）
`AddEditCourseScreen:206`、`CourseInstanceListScreen:180`、`ManageCourseTablesScreen:149/159`、`QuickDeleteScreen:193`、`CourseTableConversionScreen:186`、import 各页（Excel:92、Json:119、TextFile:108、TextHub:68、TextImport:79）、`TweakScheduleScreen:166`、`TimeSlotManagement:271`、`TodayScheduleScreen:307` 等。

### H. 原生控件替换清单
- 单选 `RadioButton` → 胶囊选中/对勾行样式：上表 A/B 内 6 处。
- 多选 `Checkbox`（TodaySchedule:575）→ AppSwitch/胶囊勾选。
- `DropdownMenu`（WebViewScreen:294、TweakSchedule:319）→ TelegramMenu/共享 menu token（`appShapes().menu`+cardBg）。
- M3 `SearchBar`（SchoolSelectionList:337）→ 胶囊 `inputBg` 填充 + appColors 显式容器。
- PrimaryTabRow 下划线 Tab（SchoolSelectionList:291-313）→ AppSegmentedControl 胶囊分段。
- M3 `NavigationRail`（NavigationComponents:181-200）→ 主题化胶囊 rail 或自绘。
- M3 `TopAppBar`（TodaySchedule:197 等）→ 显式主题化容器色，避免默认 M3 观感。
- 输入框：`CourseSchemeCard.kt:94-104` 直用 M3 TextField → AppTextField。

---

## 五、已达标项（无需修改）

1. **底部液态玻璃导航**（NavigationComponents）：haze 模糊 + token 配色 + 胶囊选中 + 深浅区分，为全站玻璃语言样板（仅存少量静态常量小瑕疵）。
2. **玻璃系浮层组件**：`AppGlassBottomSheet`（cardBg 0.86 tint + 20dp 雾）、`FloatingCourseBar`、`AppSegmentedControl`、`AppSnackbarHost`（snackbarBg/snackbarFg 深浅两套），已符合磨砂/胶囊/深色适配。
3. **动效体系**：`LocalAppMotion` token 化下发，按压反馈、页面入场、周翻页扫光均支持「风格 + 分组开关」，动画参数轻柔、无生硬跳动（HIG 第 10 项唯一全达标项）。
4. **结构性颜色映射**：`withAppSurfaces` 把 M3 surface/background/surfaceContainer*/surfaceVariant 统一压到 token，深色下无大面积 M3 纯色崩坏；卡片三级背景层级概念已建立。
5. **基础 token 工程**：形状/间距/字阶 token 数据类 + AIRY 变体值已定义（`AppStyle.kt:342-524`），`Theme.kt` 未提交改动正是最后的装配步骤（补齐双参色板函数即可编译接通）。
6. **辅助组件正面项**：AppEmptyState、AppDangerDialog 按钮逻辑、AppBadge、Toast 走系统原生（Toast 样式由系统决定，非 App 可改范围）、导航栏悬浮隐藏（coerceIn 防负 padding）等。
7. 课程块/列表颜色**数据流已解耦**（`courseColorMaps` light/dark 双套 + `adaptiveTextColor`），未发现「时间列/表头写死浅色导致深色失效」的散落点；课程块圆角未在渲染层写死（由 gridStyle 驱动）。
8. 无障碍基础：`touchMin/rowMinHeight` 概念存在（需 token 化后生效）。

---

## 六、建议修复优先级

- **P0（前置阻断）**：① 补 `appColorTokens(isDark, preset)` 双参函数 + `airyAppColorTokens/airyDarkAppColorTokens`（深灰基底 `#1C1C1E`、label 文字层级），让当前未提交 `Theme.kt` 编译通过并真正装配 AIRY tokens；② 落地 `AppAlertDialog` 封装并批量替换 A 组约 25 处原生弹窗；③ 为 AIRY 补充 `AiryGridStyle`（14dp 圆角/低饱和双套 12 色/隐藏网格线）。
- **P1（AIRY 放大值到达页面）**：将 B 组静态 `AppShape/AppSpacing/AppType` 引用全部改接 `appShapes()/appSpacing()/appType()`；优先改 `SettingsScreen` 共享组件（SettingItem/SettingCard/顶层常量），一改全站设置页生效。
- **P2（HIG 观感收口）**：C/D 组 colorScheme 直取收敛到 appColors 语义色；分割线显式 divider；阴影统一淡档；白字前景改 token 前景。
- **P3（一致性打磨）**：H 组原生控件替换；F/G 组卡片与留白 token 化。

> 依核验规则，本轮未修改任何代码；建议按 P0 → P3 推进，每完成一批后重跑本核验清单。
