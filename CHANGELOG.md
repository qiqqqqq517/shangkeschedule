# 更新日志 · Changelog

> 本文件为「上课 · ShangKeSchedule」的**统一更新声明**，汇总各版本的发布说明。
> **约定**：每次发版在下方「最新版本」区**倒序追加**一条（最新在上）；记录格式见文末「记录规范」。

---

## 最新版本

### v3.53.0（2026-09-13）· 自定义时段顺延 + 组件/提醒每日自愈 + 学期管理重写

**功能**
- 自定义时间段：修改某一节的结束时间后，**后方节次自动整体顺延**（按设置的默认上课时长与下课休息时长逐节重排，至 23:59 为止），不再需要逐节手改；编辑弹窗同步放宽时间上限。
- 桌面组件与上课提醒**免开 App 每日自动更新**：新增每日 00:05 零点自愈任务（自动同步课表数据、重排未来 7 天上课提醒闹钟、勿扰时段、灵动岛并刷新全部小组件），开机广播与打开 App 时自动补锚，跨天/换周不再依赖手动打开。
- 学期管理重写：**点击任意学期卡即可把它设为当前学期**（停留本页，列表实时重排，「查看」按钮才跳转课表）；历史学期状态按日期自动判定（未开始 / 进行中 / 已完成，替代此前「非当前一律已完成」的误标）；同学年内按学期开始日期自动排序（第一学期在前）。
- 新建日程：**待办默认不带时间**（需要时关掉「全天」开关即可自定义具体时间）；活动 / 考试等类型仍默认带时间。

**修复 / 外观**
- 修复「回到本周」圆钮与通用圆钮按压时水波纹呈**方形**扩散的问题（现沿圆形轮廓扩散，玻璃阴影不再被裁切）。
- 周切换的玻璃扫光在跨多周快速返回时不再反复重启频闪，只保留一道完整扫光。
- 已完成的待办 / 日程不再出现在今日页「下一节课」卡片中。

**构建**
- versionCode 207，versionName 3.53.0。


### v3.52.0（2026-09-13）· 情侣课表独立化

**功能**
- 情侣课表升级为**独立课表**：拥有自己的课程、作息时间与学期配置，不再依附本人课表；学期管理页中并排展示在本人课表右侧（带 ❤「情侣」标记），左右滑动即可查看，支持添加 / 查看 / 重命名 / 删除。
- 情侣课表开放**完整编辑能力**：点击课程即可修改名称、教师、地点、周次、节次与颜色，也可手动添加课程。
- 情侣课表支持**多种导入方式**：教务系统导入、Excel / JSON / 文本文件导入（导入时选择目标课表即可导入到情侣课表），不再只有教务 + JSON 两种。
- 情侣课表**单独显示**：课表切换界面（顶栏切换弹窗与学期管理页）可直接切换到情侣课表，此时「双人同显」开关自动失效；设置页提供「返回本人课表」入口。
- 情侣课表**作息独立设置**：可在设置页进入「作息设置」单独修改 TA 课表的节次时间（含多作息方案）。
- **双人同显按各自作息排列**：叠加视图中本人与 TA 的课程分别按各自课表的作息时间换算位置；当双方作息不一样时，每个课程卡顶部显示课程起止时间（如 8:00-9:50）。

**修复 / 兼容**
- 旧版本已导入的情侣课表数据在升级时自动迁移为独立情侣课表（课程、作息、学期配置一并迁移，无感升级）。
- 备份与恢复完整支持情侣课表（备份格式 v2，旧备份仍可恢复）；删除本人课表时自动级联删除其配对情侣课表。

**构建**
- versionCode 206，versionName 3.52.0。

### v3.51.2（2026-09-13）· 根治课表切周闪退与卡顿（页内悬浮件玻璃架构修正）

**修复**
- **课表切周偶发闪退（根治）**：v3.51.1 的"录制期跳过"未覆盖全部路径——页内悬浮玻璃件（回到本周圆钮 / 课程挂起条 / AppFab）位于页面背景快照的录制子树内、又采样该快照，其渲染节点在特定重绘时序下与页面层互相引用成环，HWUI 渲染线程无限递归栈溢出。现**页内悬浮件整体改走 v3.44.0 同款 Haze 玻璃**（该结构已由 v3.44.0 在 vivo X200 等机型长期验证安全），底栏自研液态玻璃不受影响（位于录制范围之外），观感基本无差。
- **切周卡顿（主线程阻塞）**：切周时"上/本/下三周"课程缓存重建（数据库查询 + 课程合并算法）此前在主线程同步执行，造成 Skipped 36-59 帧的卡顿。现移至后台线程池，主线程只做状态赋值——真机复测同负载下 Skipped 告警归零。
- **debug 构建不启用崩溃自愈的 15 分钟窗口期**：避免自动降级在开发复测时静默掩盖真问题（release 行为不变）。

**构建信息**：versionCode 206 · 本地构建 · 支持 arm64-v8a / armeabi-v7a / x86_64（签名证书 SHA-256 `4ae49d8c…2475f` 与历史版本逐位一致，老用户可直接覆盖升级）

**验证**：三端编译通过；Redmi 真机完整玻璃路径下 48 次密集切周 + 6 轮切Tab组合压测 + 今日页滚动，零崩溃、pid 稳定（修复前同负载必现 `SIGSEGV / stack overflow`）

---

### v3.51.1（2026-09-13）· 修复课表「切到下一周」闪退（渲染树自引用环）

**修复**
- **课表页横向切到下一周偶发闪退**：玻璃背景快照在录制期间，页面内的悬浮玻璃件（回到本周圆钮 / 课程挂起条 / 玻璃 AppFab）会 `drawLayer` 回正在录制的同一层，形成渲染树自引用环，导致 HWUI 渲染线程 `prepareListAndChildren` 无限递归、栈溢出崩溃。现于录制期跳过「对自身层的背景采样」，切断环——该帧玻璃遮罩退一帧（视觉几乎无感），稳定态效果完整保留。
- 影响机型：Redmi / vivo X200 等已实测复现并通过修复验证；多套主题均修复。

**构建信息**：versionCode 205 · 本地构建 · 支持 arm64-v8a / armeabi-v7a / x86_64（签名证书与 v3.41.1~v3.51.0 逐位一致，老用户可覆盖升级）

**验证**：三端编译通过；Redmi 真机在「完整玻璃路径（无兜底降级）」下连续切周 10 + 密集 14 次零崩溃（修复前同机必现 `Fatal signal 11 (SIGSEGV)` / `stack overflow`）

---

### v3.51.0（2026-09-13）· 日程页与今日课表联动打通

**功能**
- **完成状态双向同步**：今日页勾选的日程/待办，在日程页现在以删除线 + 变淡呈现；日程页也可以直接勾选（点击勾选框或卡片），状态实时同步回今日页
- **今日待办并入日程页**：今日课表页 `+` 号添加的待办现在按日期显示在日程页时间轴中（无时间归入「全天」分组，有时间按上午/下午/晚上混排），与日程、课程同屏展示
- 日程页勾选框仅对「待办」类条目显示（活动 / 考试 / 作业不受影响）；长按删除按条目来源删对应数据，日历事件点在选中日同时计入待办

**修复**
- 修复日程页看不到今日页待办、勾选完成状态两边不同步的问题（此前两页各读一张表、日程页不渲染完成态）

**构建信息**：versionCode 204 · 本地构建 · 支持 arm64-v8a / armeabi-v7a / x86_64（签名证书与既往版本一致，老用户可直接覆盖升级）

---

### v3.50.11（2026-09-12）· 修正：不再按系统版本禁用玻璃效果（Android 15 机型恢复完整效果）

**修复**
- **不再按系统版本「一刀切」**：v3.50.9 / v3.50.10 曾以「Android 15+」为门槛关掉鲜艳度、液态折射与高光着色器，导致**实测能跑完整玻璃、长期不闪退的机型（如 vivo X200）也被误伤**（设置里「液态折射」显示不支持、高光描边变实线）。现在恢复完整效果：能力改由**本机是否真的崩溃过**决定——只有真正崩过的设备才回滚到 v3.44.0 方案，不再牵连正常机型

**构建信息**：versionCode 203 · 本地构建 · 支持 arm64-v8a / armeabi-v7a / x86_64（签名证书 SHA-256 `4ae49d8c…2475f` 与 v3.41.1~v3.50.10 逐位一致，老用户可直接覆盖升级）

**验证**：三端编译通过；v3.50.11 正式包在 API 35 模拟器实测——启动日志中「vibrancy 跳过」已消失（鲜艳度 / 高光着色器 / 折射全部恢复），玻璃底栏渲染正常；写锁存标记后底栏仍正确回滚到 v3.44.0 方案，回滚机制未受影响

---

### v3.50.10（2026-09-12）· 双方案底栏 + 崩溃后自动回滚到 v3.44.0 方案

**修复**
- **首次启动仍闪退的机型现在有第二道保险**：底栏改为**两套方案**——方案 A 为自研液态玻璃（现行），方案 B 为 **v3.44.0 的 Haze 毛玻璃底栏**（用户实测可在 OPPO Find X8 上正常运行）。若检测到上次进程崩溃退出，本次启动**底栏自动回滚到方案 B**，保证 App 一定可用；升级新版本后自动重试方案 A
- **Android 15+ 再收紧**：在 v3.50.9 已移除「色彩滤镜链」的基础上，进一步关闭该平台上的 **AGSL 运行时着色器**（折射不可用并在设置页如实提示、高光描边退化为实线，观感差异极小），使首帧渲染只剩「模糊」——与已验证安全的 v3.44.0 属同类操作
- 兜底模式下同时摘除高光 / 投影 / 内阴影装饰（其内部含 AGSL 与 BlurMaskFilter），避免兜底挡不住崩溃

**构建信息**：versionCode 202 · 本地构建 · 支持 arm64-v8a / armeabi-v7a / x86_64（签名证书 SHA-256 `4ae49d8c…2475f` 与 v3.41.1~v3.50.9 逐位一致，老用户可直接覆盖升级）

**验证**：三端编译通过（`:shared:compileKotlinJvm` / `:desktopApp:compileKotlin` / `:androidApp:assembleRelease`）；v3.50.10 正式包在 API 35 模拟器实测两套方案——方案 A 渲染正常、方案 B（强制锁存）呈 v3.44.0 形态的毛玻璃胶囊 + 共享胶囊指示器、锁存清除后自动回到方案 A

---

### v3.50.9（2026-09-12）· 修复部分机型启动闪退 · 今日页待办可勾选

> 本版一次性合并 v3.50.8 ~ v3.50.9 的全部变更（其间版本未单独发布）。

**修复**
- **部分手机启动闪退（OPPO Find X8 等 Android 15 机型）**：Android 15 起 targetSdk ≥ 35 的应用 HWUI 默认切 Vulkan 硬件渲染，部分机型的 GPU 驱动对液态玻璃的「色彩滤镜 + 模糊」**链式 RenderEffect** 原生崩溃（v3.44.0 等旧版的 Haze 模糊路径无此问题）；现 Android 15+ 上玻璃自动改走「单一模糊」路径（观感基本无差），并新增**自愈保护**——若检测到上次进程崩溃退出，本次启动玻璃自动降级为半透明面板，**保证 App 一定打得开**，升级新版本后自动重试完整玻璃
- 今日页日程区段左上角标签按当天日程类型显示（如「待办 · 活动 · 考试」），不再固定写「今日课表」（v3.50.8）

**功能**
- 今日页「待办」分类的日程支持点击勾选完成（数据库 v12，旧数据自动迁移，勾选状态跨设备随备份迁移）

**构建信息**：versionCode 201 · 本地构建 · 支持 arm64-v8a / armeabi-v7a / x86_64（签名证书 SHA-256 `4ae49d8c…2475f` 与 v3.41.1~v3.50.7 逐位一致，老用户可直接覆盖升级）

**验证**：三端编译通过（`:shared:compileKotlinJvm` / `:desktopApp:compileKotlin` / `:androidApp:assembleRelease`）；v3.50.9 正式包在 API 35 模拟器三态实测（完整玻璃正常渲染 / 崩溃降级锁存 / 锁存清除恢复，日志 + 截图）；桌面与 iOS 路径无行为变化

---

### v3.50.7（2026-09-12）· 液态玻璃底栏（定稿）· 我的信息页 · 下节课卡

> 本版一次性合并 v3.46.0 ~ v3.50.7 的全部变更（其间版本未单独发布）。

**功能**
- **液态玻璃底栏**：按 Kyant0/AndroidLiquidGlass（Apache-2.0）原版逐行复刻的三层结构 —— 64dp 玻璃条 + 隐形 Tab 录制层 + 56dp 可拖拽玻璃指示器；按住指示器可拖动换 Tab，带按压缩放 / 高光 / 投影 / 内阴影 / 速度挤压回弹（v3.46.0 ~ v3.48.1）
- **「我的信息」页**：头像（系统选择器 + 1:1 裁剪）/ 昵称 / 个性签名 / 学校 / 学院 / 专业 / 年级；「我的」页头合并、身份卡增大（v3.49.0）
- **下节课卡三选一**：今日课程结束后「自动显示下一次（含跨天日程）/ 已结束变淡 / 消失」（v3.47.0）

**修复**
- **液态玻璃三效果（模糊 / 折射 / 色散）全部不生效的根因修复**：Compose 1.11.1 的 `GraphicsLayer.record` 不像 1.12 那样把调用方画布切到录制画布，背板录制层恒为空 ⇒ 所有玻璃件都在采样空层；现于录制块内手工交换 `drawContext.canvas`，三效果全部生效（离屏探针像素级差分 + 真机 A/B 验证）（v3.50.2）
- 日程页日期轴：滑动只浏览、不再改选中日，蓝色高亮钉在选中日上（v3.46.1）
- 折射开关 / 档位对底栏零可见变化的参数等价问题；模糊滑杆量程扩到 0~24（v3.49.1 / v3.49.3）
- 备份文件补入折射 5 键（可空迁移：旧备份恢复不覆盖设备现值）（v3.50.0）

**外观**
- **底栏最终形态**：玻璃条与 Tab 录制层**只做模糊**（不折射不色散，任何状态无扭曲）；折射 / 色散仅保留为**按住指示器时**的反馈（静止态为 0）（v3.50.6 / v3.50.7）
- Tab 图标-文字间距收紧至 0.5dp + 文字行高修剪（去掉字体上下留白），图标与文字整体居中（v3.50.3 ~ v3.50.5）
- 开启折射时切换「参考实现光学」：描边环减弱、镜面高光增强、标准档 24/24（v3.47.0）

**构建信息**：versionCode 199 · 本地构建 · 支持 arm64-v8a / armeabi-v7a / x86_64（各约 5.85MB，签名证书 SHA-256 `4ae49d8c…2475f` 与 v3.41.1~v3.45.1 一致，老用户可直接覆盖升级）

**验证**：三端编译通过（`:shared:compileKotlinJvm` / `:desktopApp:compileKotlin` / `:androidApp:assembleRelease`）；真机 Redmi 22041216UC 装机验证（versionCode 199）：液态玻璃三效果离屏探针像素级差分 + 真机 A/B、底栏点击 / 按住 / 拖动全量交互冒烟、无 FATAL

---

### v3.45.1（2026-09-11）· 柔绘动效提速 · 「动效速度」重新标定

**修复**
- **柔绘的进入 / 退出不再磨蹭**：导航入场 375 → **210ms**、退场 238 → **110ms**；底部面板 388/238 → 210/110ms；弹窗 388/238 → 200/110ms；Tab 胶囊 / 图标 263/138 → 160/100ms；触摸指示 313/200 → 190/120ms；卡片抬起 313 → 160ms；状态渐变 225 → 150ms（均为默认档实际值）
- **柔绘入场曲线改为「陡起步 + 长收尾」**（新增 `SoftEnterEase`）：原曲线两端等速、前 30% 几乎不动，观感像「先愣一下再化开」；现在触发即响应、落定仍柔和，依旧单调零过冲、不产生折点

**优化**
- **「动效速度」三档重新标定**：由 1.30 / 1.60 / 2.00 改为 **1.45 / 2.00 / 2.80** —— 默认档比基线**快一倍**（全站时长再减半，通透 / 书卷等主题同步 −20%），档间相差 1.38~1.4 倍，来回切换手感差别明确（以柔绘导航入场为例：舒缓 290ms / 标准 210ms / 快 150ms）
- **柔绘周切换「整屏换气」跟随设置**：此前时长写死 600ms、完全不受「动效速度」控制，现随速度档变化（默认约 320ms）

**构建信息**：versionCode 182 · 本地构建 · 支持 arm64-v8a / armeabi-v7a / x86_64（各约 5.78MB，签名证书 SHA-256 `4ae49d8c…2475f`，老用户可直接覆盖升级）

**验证**：编译通过（`:androidApp:assembleDebug` / `:androidApp:assembleRelease`）；真机 Redmi 22041216UC（versionName 3.45.1，主题切到「柔绘 + 琉璃轻弹 + 标准速度」）：功能性冒烟 10/10（四个 Tab 切换 / 二级页 push-pop / 日程页底部面板开合 / 进程未重启 / FATAL 0）；日程页日期面板回归 27/27；`run-as` 读 DataStore 核验持久化为 `SOFT` / `GLASS` / `STANDARD`；录屏差分 A/B（通透档）退场 161/172 → 110ms、入场 183/197 → 167ms（该机 screenrecord 仅 7~15fps，毫秒级结论以令牌换算表为准）

**涉及文件**：`ui/theme/AppMotion.kt`、`ui/schedule/WeeklyScheduleScreen.kt`

**说明**：弹簧类动效（按压缩放、悬浮件回弹）由阻尼比 / 刚度决定，不随时长倍率变化 —— 这是有意保留的边界

---

### v3.45.0（2026-09-11）· 日程页日期面板：左右滑动切日期 · 切月联动下方日期 · 下拉展开整月日历

**功能**
- **顶部日期可左右滑动**：折叠态前后翻一周（周条整体后移 / 前移 7 天），展开态前后翻一月，带滑入转场；时长与曲线跟随「周翻页」动效分组与「动效设置」，关掉分组或开启「减弱动态效果」即瞬切
- **切换月份时下面的日期一起对应过去**：月份标题、当日标题、周条三者同步 —— 例如选「12月」后周条即为 12 月 7–13 日、当日标题为「12月11日 周五」（日期按「同月同日」平移，目标月没有该日则落到月末，如 10/31 → 9/30）。此前只改月份、下面的日期仍停在上个月
- **日期下拉直接展开整月日历**：在日期面板内下拉即展开整月（固定 6 行 × 7 列 = 42 格，31 天的月份完整可见），上收即收起；月外日期淡化显示但仍可点选，有日程的日子带圆点
- **点击月份直接选月份**：点顶部月份标题打开月份选择器（年份左右切换 + 1–12 月网格，当前月高亮），一步跳到任意年月
- 月份左右箭头同样联动下方日期；点选任意日期后自动收起月历

**外观**
- 月份标题右侧新增下拉箭头示意可点；面板底部新增展开把手（下拉 / 上收 / 点击均可切换），展开态箭头翻转
- 整月日历沿用日程页既有语言：星期表头按「每周起始日」轮转、月外日期淡化、选中日主色实底、有日程圆点

**其他**
- 同时包含原 v3.44.1（未发布内容）：移除「动画效果预览」卡片
- 四语言（简体中文 / 繁体中文 / 英文）新增 9 条文案与无障碍标签

**构建信息**：versionCode 181 · 本地构建 · 支持 arm64-v8a / armeabi-v7a / x86_64

**验证**：编译通过（`:androidApp:assembleDebug` / `:androidApp:assembleRelease`）；真机 Redmi 22041216UC（versionName 3.45.0）装机交互专项 **27/27 通过**：初始「9月」+ 周条 7–13 + 当日标题「9月11日 周五」→ 点月份标题弹出选择器（「选择月份」+「2026年」+ 1–12 月）→ 选「12月」后月份标题 / 当日标题 / 周条一起落到 12 月 → 下拉展开整月 42 格含 31 号 → 展开态左滑进 1 月（「1月11日 周一」）、右滑回 12 月 → 上收收起恢复周条 → 折叠态左滑「12月18日」（周条 14–20）、右滑回到「12月11日」→「回到今天」回 9 月；全程进程未重启、**FATAL EXCEPTION 0 条**（脚本 `tools/verify_agenda_panel_v345.py`）

**涉及文件**：`ui/agenda/AgendaScreen.kt`、`ui/agenda/AgendaViewModel.kt`、`strings.xml`×4

---

### v3.44.1（2026-09-11）· 删除动画效果预览

**移除**
- **删除「动画效果预览」卡片**：移除「实时预览」（课程块入场错峰 + 按压缩放演示）和「转场与滚动预览」（导航转场 / Tab 胶囊迁移 / 底栏隐藏演示）两个预览子卡片及其关联私有函数与四语言字符串资源

**构建信息**：versionCode 180 · 本地构建

---

### v3.44.0（2026-09-11）· 新增「动效速度」可调倍率 + 退场提速

**新增**
- **「动效速度」三档可调**：舒缓 1.3× / **标准 1.6×（默认）** / 快 2.0× —— 倍率是「速度」，数值越大越快；
  统一作用于页面转场、列表与卡片入场、面板与弹窗、触摸反馈、状态渐变等全部动画时长
- **退场独立令牌（慢进快出）**：页面退场不再复用入场时长 —— 柔绘退场基线 600ms → 380ms，
  叠加默认倍率后实际 **238ms**（此前 600ms 全程半透明，是「淡出磨蹭」的根因）

**效果**（柔绘 · 标准档实际时长）
- 导航入场 600 → 375ms；**退场 600 → 238ms**
- 面板 620 → 388ms / 380 → 238ms；弹窗 620 → 388ms / 380 → 238ms
- 触摸反馈 500 → 312ms / 320 → 200ms；按压抬起 500 → 312ms；Tab 胶囊 420 → 262ms

**构建信息**：versionCode 179 · 本地构建

**验证**：真机 Redmi 22041216UC —— 「动画效果」页新增分区、三档可切换、退出重进持久化生效、全程无 FATAL；`shared` 编译通过

---

### v3.43.0（2026-09-11）· 交互动效全面分档：三套主题各说各的动效语言

**新增**
- **动效语言按主题分档**（此前三套主题共用一套 iOS 26 + Material 混合动效，动效级主题分支全库只有 1 处）：
  - **柔绘**：最慢、零缩放零位移、对称缓动；按压 = 软边浓度「化开」；导航 = 淡入上浮（不横移，避免打断晕染画面连续性）
  - **书卷**：临界阻尼零过冲；按压 = 底色均匀加深（静态纸面上不「游动」）；导航 = 纸页自右缘短距层叠
  - **通透（iOS 26）**：保留 iOS 弹簧语义；按压 = 轻微缩放；导航 = 整屏横推 + 旧页 1/3 尾随视差
- **触摸反馈全站覆盖**：自定义主题化触摸指示经全站注入，**110 处可点元素统一生效**（此前 110 处用 Material 默认涟漪，自研反馈只覆盖 1 处）
- **新增 3 个可独立开关的动画分组**：底部面板 / 对话框 / 切换标签（分组总数 6 → 9）
- **新增「减弱动态效果」无障碍开关**：开启后位移 / 缩放 / 错峰全部归零，只保留淡入淡出，减少眩晕感
- **动画设置页新增「转场 / 滚动预览」**：可视化演示导航转场三形态、Tab 选中胶囊迁移、底栏隐藏下滑淡出
- **今日页支持下拉刷新**，指示器随主题分形（柔绘三点呼吸 / 书卷墨点晕开 / 通透八段旋转）
- **加载态按主题分形**：柔绘三点呼吸 / 书卷墨点晕开 / 通透八段旋转（此前三套主题统一是 Material 旋转环）

**修复**
- 今日页课程卡恢复按压 + 错峰入场（此前动效落在不可达分支，等于今日页丢失全部课程卡反馈）
- 「已结束课程降透明」由一帧硬跳改为渐变（三处）
- 空态 / 加载态补入场；提示条（Snackbar）补上浮入场
- 当前节次高亮跨节次时由瞬切改为渐变（底色 / 数字色 / 字重 / 竖条浓度同步补间）
- 列表删除后由「瞬间消失」改为淡出 + 平滑补位
- 拖拽课程块落位改为临界阻尼弹簧吸附（此前直接赋值，无落定感）
- 玻璃顶栏滚动时现在会折叠（此前该分支直接丢弃滚动状态，永不折叠）
- 柔绘周切换「整屏换气」修正：去掉双重缓动叠加、深色模式不再叠白
- 底栏 Tab 选中胶囊由瞬移改为位置 / 颜色 / 图标同步过渡
- 入场错峰总量加上限：满周 30 块不再排队 1.2–2.1s 才全部出现，且入场起始即可见轮廓
- 动效设置页新增的「转场与滚动预览」卡标题此前与「动效风格」分区重名，改为独立文案（四语言）

**构建信息**：versionCode 177 · 本地构建 · 支持 arm64-v8a / armeabi-v7a / x86_64

**验证**：编译三端通过（shared JVM / Android Debug / Desktop）；产出 debug 包三 ABI。
**真机装机验证已完成**（Redmi 22041216UC，versionCode 177）：
- 交互动效专项 19 项全过：下拉刷新、课程详情面板、动效设置页新预览 + 3 个新分组 + 减弱动效开关、主题切换确认框（30 处对话框迁移后）均可正常交互；**全程无 FATAL、进程未重启**
- 掉帧实测（同一滚动负载，`dumpsys gfxinfo`）：柔绘 课表 6.31% / 今日 4.15%，书卷 4.55% / 2.76%，通透 4.63% / 1.99% —— **柔绘已回到与另两套主题同量级**
- 三主题静态像素互斥复核通过，跨主题签名色 ≈ 0，**无视觉回归**

**涉及文件**：新增 `ui/theme/ThemeIndication.kt`；修改 24 个源码文件（`AppMotion.kt` / `Theme.kt` / `StyleComponents.kt` / `AppBasicComponents.kt` / `AppTopAppBar.kt` / `NavigationComponents.kt` / `ScheduleGrid.kt` / `ScheduleGridComponents.kt` / `WeeklyScheduleScreen.kt` / `App.kt` / `TodayScheduleScreen.kt` / `TodayScheduleViewModel.kt` / `AnimationSettingsScreen.kt` / `AppSettingsModel|Repository|ViewModel` / `QuickDeleteScreen.kt` / 16 个含对话框的文件）+ `strings.xml` ×4 语言

---

### v3.42.0（2026-09-11）· 新增第三主题「柔绘」：柔和晕染 · 薄涂通透 · 漫射柔光

**功能**
- **新增「柔绘」主题**：功能面完全参照书卷/通透——同一批页面、同一批分组与条目、同一批字段与位置，只替换 UI 材质
  - 设计语言：柔和晕染渐变、虚化圆角（卡片 24dp / 课程块 16dp）、**无锐利硬边缘**（全站 1dp 实色描边 → 羽化渐变描边环）、低对比雾紫配色（主色 `#7C86C9`）、薄涂通透卡 + 漫射柔光 + 轻量手绘纹理 + 双层软模糊投影
  - 今日页：双色晕染时间轴卡（雾紫 / 暖杏交替）、便签胶囊、下节课卡、明日预览、日程行、课程详情薄涂面板
  - 我的页：4 组 inset 分组列表（课表 / 课程 / 偏好 / 关于），条目与开关位置与书卷一一对应；30dp 晕染图标容器 + 同色图标；所有行等宽等高（970×144px，与通透一致）
  - 课表页：12 对低饱和马卡龙色池、隐藏网格线、课程块无描边 + 软投影（静置 6dp / 浮起 12dp）、当前节次圆角薄涂高亮块 + 内侧渐变竖条
  - 悬浮件：底栏 / 悬浮课程条 / FAB / 底部弹层（模糊 14dp、噪点 0.06、遮罩 26%）统一薄涂质感
  - 深色模式完整适配：雾紫提亮档 `#9AA3DC` + 暖灰深底 `#1F1E24` / `#2A2830`
- **周切换扫光按主题分流**：柔绘档移除方向性玻璃扫光（镜面语言与雾面薄涂材质冲突），改为 600ms 无方向「整屏换气」明度起伏；书卷/通透行为不变
- **书卷与通透零改动**：Material `outline` / `outlineVariant` 角色新增主题 token，默认值即 Material 基线原值，两套既有主题视觉不变（`git diff '*Claude*'` 为空）

**修复**
- 柔绘日程事件分类色不再使用 Material 500 档高饱和色，改走主题语义色（低饱和马卡龙档）
- 柔绘暖调时间轴描边残留的 iOS 系统橙 `#FF9500` → 柔绘警示色 `#D9A97E`
- 柔绘今日页移除多余的「今日课表」顶栏与「添加待办」FAB（书卷/通透均无此结构）

**构建信息**：versionCode 175 · 本地构建 · 支持 arm64-v8a / armeabi-v7a / x86_64

**验证**：编译三端通过；真机三主题互斥像素核验通过（各主题签名色命中、互串≈0）；我的页 15 界面 IA 走查标签集合一致；柔绘深色模式像素核验通过；硬边线检出 0 段、柔绘高饱和像素占比 0.05%

**涉及文件**：新增 `SoftStyle.kt`、`SoftSettingsComponents.kt`；修改 `AppThemePreset.kt`、`ScheduleGridStyle.kt`、`AppStyle.kt`、`Theme.kt`、`TodayScheduleScreen.kt`、`SettingsScreen.kt`、`StyleComponents.kt`、`AppBasicComponents.kt`、`AppTopAppBar.kt`、`CourseBlock.kt`、`WeeklyScheduleScreen.kt`、`ScheduleGridComponents.kt`、`FloatingCourseBar.kt`、`ManageCourseTablesScreen.kt`、`TimeSlotManagementScreen.kt`、strings.xml（×4 语言）

---

### v3.41.1（2026-09-11）· 修复通透「我的」页开关行与其它设置行高度不一致

**修复**
- **「我的」页所有设置行现在完全等宽等高**：修复前「显示非本周课程 / 是否显示周末」两行被行尾开关撑开（行距 189px vs 其它行 144px，行宽 628px vs 725px），现在 12 行统一为 **宽 612px × 高 57px、行距 145px**
- 根因是行尾插槽按内容自适应：导航行的箭头只有 15dp，而开关自带 48dp 触控扩张，两类行的宽度、高度、点击热区都被拉得不一致。改为**固定尺寸尾部插槽**（宽 56dp × 高 32dp），箭头与开关在同一盒内居中，行高统一由图标/文字决定
- 顺带统一了两类行的点击热区（此前开关行的可点范围明显窄于导航行）

**构建信息**：versionCode 174 · 本地构建 · 支持 arm64-v8a / armeabi-v7a / x86_64

**验证**：编译三端全部通过；真机装机逐行量测 12 行宽度/高度/行距唯一值一致；两套主题（通透 / 书卷）像素核验仍互不串色

**涉及文件**：`IosSettingsComponents.kt`

---

### v3.41.0（2026-09-11）· 通透主题重做为 iOS 26 Liquid Glass ｜ 书卷主题完整保留

**功能**
- **主题精简为两套**：删除「经典」与「云舒」；保留「书卷」并新增/重做「通透」。主题页只展示这两项
- **通透主题（iOS 26 / Liquid Glass）**：功能面**完全参照书卷**——同一批页面、同一批分组与条目、同一批字段与位置，只替换 UI 与动画组件
  - 今日页：周次胶囊 + 日期大字页头 → 下节课卡 → 今日课程时间轴（时间列 / 节点圆点 / 连线 / 节数徽标 / 信息胶囊）→ 日程事件 → 明日预览 → 课程详情玻璃面板
  - 设置页：「我的」4 组 inset grouped 列表【课表 / 课程 / 偏好 / 关于】，条目与开关位置与书卷一一对应
  - 课表页：左侧色条 + 极淡同色底 + 隐藏网格线 + 文字左对齐 + 70dp 节高 + 12dp 连续圆角
- **书卷主题零改动**：配色（暖砂纸底 / 赤陶主色）、字体（Poppins / Newsreader / Lora）、inset grouped 暖米色分组卡、今日页设计稿形态全部原样保留
- 老用户主题偏好自动迁移：`ORIGINAL` / `SLEEPY` / `TIMETABLE` / `AIRY` 统一落到「通透」，历史配色池（书卷 12/20 色、经典马卡龙、云舒 Material）首次启动平滑升级为系统色池

**外观**
- 通透色板严格取 Apple 系统色：分组列表底 `#F2F2F7`、卡片白 `#FFFFFF`、systemBlue `#007AFF`、深色 `#0A84FF`；深浅两套完整 systemGrouped 层级
- Liquid Glass 材质：不透明卡片改用「高光 + 反射 + 厚度」三层玻璃描边（`iosGlassRim`），悬浮件（底栏胶囊 / 圆钮 / 挂起条 / FAB / 详情面板）共用 haze 背景模糊 + 边缘光学
- iOS 26 连续圆角阶梯（卡片 16 / hero 22 / 控件 12 / chip 10）、8pt 网格间距、SF 字阶（Large Title 34 / Body 17 / Footnote 13 / Caption 2 11 + HIG 字距）
- 导航栏：44pt 高 + 居中 17pt SemiBold 标题 + 玻璃底衬 + 底部发丝线
- 今日页时间轴卡片按系统蓝 / 系统橙交替渐变；周次胶囊、今天列、进行中圆点统一系统蓝

**动效**
- 三档动效重写为 iOS 26 物理弹簧：`柔和顺滑`（无过冲、丝滑收束，默认）、`轻盈舒缓`、`灵动跟手`（短促弹回），对应 SwiftUI `.smooth` / `.snappy` / `.bouncy` 语义
- 时带重排：导航 350ms / 底栏隐藏 240ms / 页面入场 320ms（40ms 错峰）/ 展开 280ms

**修复**
- 删除「云舒」主题后清理其遗留的课程块投影分支（`isSleepyPreset`），避免死分支继续影响课程块渲染

**构建信息**：versionCode 173 · 本地构建 · 支持 arm64-v8a / armeabi-v7a / x86_64

**验证**：`:shared:compileKotlinJvm` / `:androidApp:assembleDebug` / `:desktopApp:compileKotlin` 全部通过；真机（22041216UC 同型号 arm64 设备）装机运行无崩溃；像素级核验确认两套主题互不串色（脚本 `tools/verify_dual_themes.py`）

**涉及文件**：`IosStyle.kt`(新增) · `IosSettingsComponents.kt`(新增) · `IosPreview.kt`(新增) · `AppThemePreset.kt` · `ScheduleGridStyle.kt` · `StyleSettingsRepository.kt` · `Theme.kt` · `AppStyle.kt` · `AppMotion.kt` · `LiquidGlass.kt` · `TodayScheduleScreen.kt` · `SettingsScreen.kt` · `AppTopAppBar.kt` · `AppBasicComponents.kt` · `StyleComponents.kt` · `FloatingCourseBar.kt` · `ScheduleGridComponents.kt` · `WeeklyScheduleScreen.kt` · `CourseBlock.kt` · `ManageCourseTablesScreen.kt` · `TimeSlotManagementScreen.kt` · `strings.xml`×4

---

### v3.40.0（2026-09-10）· 学期管理页 + 时间段管理页全新设计

**功能**
- **「管理课表」页重构为「学期管理」**：页头（学期档案 eyebrow + 大标题 + 共 N 个学期概览 + 新建按钮）→ 当前学期高亮卡（主色描边 + 顶部主色条 + 当前学期徽标 + 日期区间/课程数 + **学期进度条**「第 N 周 / 共 M 周 · 已过 P%」+ 查看课表/学期设置按钮）→ 历史学期按学年分组（已完成徽标 + 课程数/教学周统计 + 查看/删除）→「查看更早的学期」折叠展开 → 新建学期方式卡（**教务导入 / 手动创建 / 备份恢复**）
- **点击历史学期卡即切换**并直达课表页；**长按学期卡可重命名**
- **时间段管理页新增「恢复默认」**：一键载入默认 13 节作息模板，保存后生效
- 新增 30+ 文案，覆盖简体中文 / 繁体中文 / 英文

**外观**
- 学期管理页：Lora 衬线 eyebrow / Newsreader 28sp 页标题与 22sp 学期名、success 进度条、outline 已完成徽标、虚线折叠按钮，全面对齐 Claude 设计语言
- 时间段管理页：作息方案胶囊 +「当前使用」提示 + 右箭头；自动切换行改为标题 + 副标题 + 开关；默认时长改为无框双列大数字输入（聚焦变主色）；节次卡左侧「第 N 节 + 时间」两行 + 右侧圆角删除钮
- 两页均支持宽屏适配（内容限宽 640dp 居中），圆角遵循主题 token

**构建信息**：versionCode 172 · 本地构建 · 支持 arm64-v8a / armeabi-v7a / x86_64（各 5.46MB）

**涉及文件**：`ManageCourseTablesScreen.kt` · `ManageCourseTablesViewModel.kt` · `TimeSlotManagementScreen.kt` · `CourseTableRepository.kt` · `strings.xml`×4

---

### v3.39.4（2026-09-10）· 今日课表周次胶囊恢复原位 + 日期避让状态栏

**修复**
- **今日课表页头布局修正**：「第几周」方形胶囊从日期下方移回日期上方（原位置），保持 `日期 → 周次 → 课程列表` 视觉层级；页头列添加 `statusBarsPadding()`，日期不再触碰手机状态栏
- 周次胶囊：`fontSize=13sp`、`RoundedCornerShape(8.dp)`，居中对齐，与日期间距 6dp

**涉及文件**：`TodayScheduleScreen.kt` · versionCode 171

---

### v3.39.3（2026-09-10）· 今日课表周次胶囊移至日期下方并放大

**外观**
- **今日课表页头调整**：「第几周」方形胶囊从日期上方移至日期下方（日期 → 周次 → 下一节课/课程列表），视觉层级更清晰；字体从 11sp 提升至 13sp，胶囊内边距扩大（`12dp×5dp`），辨识度更高

**涉及文件**：`TodayScheduleScreen.kt` · versionCode 170

---

### v3.39.2（2026-09-10）· 汕头大学教务适配重做（强制电脑版 + 一次点击完成导入）

**修复**
- **汕头大学教务导入重做**：修复在教务门户首页点击「执行导入」时**只跳转、不导入**的问题。原逻辑先跳转到课表页再等待，而页面一旦跳转，当前脚本上下文即被销毁，后续代码永远不会执行，用户必须再点一次。现改为**同源读取课表页**后直接解析导入，**在门户首页一次点击即可完成导入**
- **强制电脑版进入**：汕头大学教务门户是**固定宽度的桌面布局**，却自带 `width=device-width` 视口声明，在手机版下被压扁，左侧菜单点不开、登录后进不去课表。现该校统一以**电脑版（桌面 UA + 1280px 视口）**进入，页面恢复正常的桌面排版

**功能**
- **「一键导航到课表」对汕头大学精确生效**：新增该校专用的课表页导航入口，直接跳转「学期理论课表」，不再依赖菜单文案模糊探测（门户里含「课表」字样的菜单有 5 处以上，容易点错）

**说明**
- 课表解析规则**未改动**（上一版已按实测结构验证正确）：仍正确处理表头 `th` 列、大节→小节映射（01,02 / 03,04,05 / 06,07 / 08,09,10 / 11,12,13）、多段周次（如 `3-4,6-11`）、教室精确到教室号
- 实测该校 2026 年秋季学期解析出 **11 门课程**，与页面课程格逐条一致（含古代汉语A 第 11-12 节、公卫与生命安全多段周次）
- 该校 5 个大节共 13 个小节，若 App 内默认时间段不足，请到「设置 → 自定义时间段」按学校作息补足
- 适配脚本已同步至私有适配仓库（哈希逐字节一致，首启不会重复下载）

**涉及文件**：`stu.js`（汕头大学适配器 v4）、`AdapterSelectionScreen.kt`、`androidApp/build.gradle.kts` · versionCode 169

---

### v3.39.1（2026-09-10）· 书卷主题浅色配色修复 + 非本周显示还原

**修复**
- **书卷主题浅色模式「撞色」修复**：书卷主题浅色模式下 20 个课程配色过于接近、不同课程难以区分（原浅色池直接把深色值套 25% 透明度，所有色相被同向淡化成近似的米白）。现重新调整浅色池的**明度与饱和度**，在保持「淡彩纸底 + 暖砂色调」观感不变的前提下，课程块之间的最近色差从 23.8 提升到 40.9（约 +72%），相邻课程一眼可辨
- **非本周课程块显示还原**：按用户要求，非本周（非当前周）课程块恢复为原有的整体压字式淡化遮罩，本次不改动其观感
- **修复 v3.39.0 遗留的源码编译错误**：v3.39.0 的 `CourseBlock.kt` 处于半合并状态（新增了降级渲染字段却未清理旧引用，并残留已删除的 import），导致源码无法编译；本次一并修复，`main` 已恢复可正常构建

**说明**
- 深色模式配色池**未做任何改动**（课程块最近色差仍为 89.2），仅浅色模式生效
- 书卷浅色课程块正文对比度 7.16:1，满足 WCAG AA 4.5:1
- 非本周课程块对比度偏低（1.6–1.9:1）为**已知遗留问题**，本次按用户要求保留不修

**涉及文件**：`AppThemePreset.kt`、`CourseBlock.kt`、`androidApp/build.gradle.kts` · versionCode 168

---

### v3.39.0（2026-09-10）· 今日课表 UI 调整 + 日程事件同步 + 教务导入入口

**功能**
- **今日课表页头重做**：左上角周次胶囊从圆角胶囊（含左右箭头）改为方形块，视觉更简洁；日期文字上移 6dp 与周次胶囊更紧凑；删除右上角设置按钮
- **课表页三点菜单新增教务导入**：溢出菜单（⋮）顶部新增「教务系统导入」条目，使用学校图标，直接跳转学校选择列表
- **日程页周条左右滑动**：在周日期条上支持水平拖拽手势，左滑切换下一天、右滑切换前一天，拖拽期间不响应格子点击
- **教务导入页底部栏改为并排**：「执行导入」与「一键导航到课表」由上下堆叠改为左右并排等宽，减少底部占位

**数据**
- **今日课表同步日程事件**：今日课表页（Claude 主题）在课程时间轴之后展示来自「日程」页新建的自建日程，含分类彩色圆点（待办绿/活动橙/考试红/作业蓝）、地点、备注

**其他**
- 移除「利落」（TIMETABLE）主题预设，主题列表仅保留经典 / 云舒 / 书卷（Claude）等既有主题

**修复**
- 修复关闭「显示周末」时会静默把「每周起始日」重置为周一、覆盖用户已选起始日的问题
- 修复教务导入成功后「学期起始日」提示弹窗重复（主界面与导入页各维护一份）的问题
- 修复新建课程页在未收到预设数据时可能卡死的问题（CONFLATED 通道 `first()` 改为 `firstOrNull()`）

**涉及文件**：`TodayScheduleScreen.kt`、`TodayScheduleViewModel.kt`、`WeeklyScheduleScreen.kt`、`AgendaScreen.kt`、`WebViewScreen.kt`、`AppThemePreset.kt`、`AppStyle.kt`、`ClaudeStyle.kt`、`CourseBlock.kt`、`ScheduleGridComponents.kt`、`AppearanceSettingsScreen.kt`、`AppSettingsViewModel.kt`、`AddEditCourseViewModel.kt` · versionCode 167

---

### v3.38.0（2026-09-10）· 教务适配私有化 + 导入页一键导航到课表

**功能**
- **教务适配私有化 + 远程安全更新**：适配配置不再随公开源码分发。新增私有适配仓库与鉴权网关，APP 启动后在后台拉取最新适配，**逐个文件强制 sha256 校验**，校验通过才覆盖本地；校验失败即丢弃并提示「适配更新失败」；网络不通时**静默回退内置适配**，教务导入、课表解析不受任何影响
- 教务导入页底部栏重做：移除冗长引导文案，「执行导入」改为全宽主按钮，其下方新增**「一键导航到课表」**——在教务系统网页内自动寻找课表入口并跳转

**说明**
- 密钥管理：仓库读取 Token 只存于服务端环境变量，绝不进入代码或 APP；APP 侧鉴权密钥改为**构建期注入**，公开源码与公开仓库均不含密钥
- 鉴权网关仅放行「适配清单」与「适配文件」两个路径，其余（含根路径）一律 `403`，并做常量时间鉴权、5 分钟缓存与单 IP 限流——浏览器与爬虫无法直接获取适配
- 适配更新全程在后台进行，失败不影响现有功能，**未新增任何界面**
- 「一键导航到课表」优先使用适配脚本显式声明的入口，否则按页面菜单文案探测（用 iframe / 特殊控件渲染的教务系统可能命中不到）

**构建**
- versionCode 163 · arm64-v8a / armeabi-v7a / x86_64

### v3.36.2（2026-09-10）· 重写汕头大学教务适配器

**修复**
- 修复汕头大学教务导入失败。原适配器按「每大节固定 2 小节」推算节次、且用 `getElementsByTagName('td')` 遍历表格，而该校课表的表头行与大节标签列实际是 `<th>`（少一列会导致**星期整体错位**），节次映射也是 `01,02 / 03,04,05 / 06,07 / 08,09,10 / 11,12,13`——因此导入结果必然错误
- 修复课程数据字段名：导入 JSON 曾使用 `room`，而客户端要求的是 `position`，会导致解析成功后**保存阶段直接失败**
- 同一课程格内有「简版 / 详版 / 整大节占位」三档内容，现按课程归组后取详版解析，不再重复导入或混入空格占位块

**功能**
- 导入时支持**学期选择**：识别到多个学期会弹出选择（默认当前页学期），可直接导入其它学期课表
- 停在学校统一身份认证（CAS）登录页时给出明确提示；导入失败的诊断信息改为真实结构计数，便于定位

**说明**
- 解析规则按实测站点结构重写：星期以表头「星期一…星期日」建映射，节次优先取单元格 `[xx节]`、缺失时用大节标签兜底，周次支持多段（`3-4,6-11(周)`）与单双周，教室优先精确到教室号
- 该校 5 个大节共 13 个小节，若客户端默认时间段不足，请到「设置 → 自定义时间段」按学校作息补足

**构建**
- versionCode 161 · arm64-v8a / armeabi-v7a / x86_64

### v3.36.1（2026-09-10）· 修复「周课表颜色与颜色池不一致」

**修复**
- 修复周课表（网格视图）课程块颜色与颜色池对不上：颜色池里的颜色**自带透明度**，且这个透明度是设计令牌（书卷浅色池 `0x40` 淡底、通透浅色池 `0x1F` 淡底）。此前网格渲染会用「课程块不透明度」**直接覆盖**它（默认 100%），淡底令牌失效，浅色池在课表上被渲染成**饱和实色**——既与颜色池里显示的颜色不一致，也与列表视图、今日页课程条、课程色卡不一致
- 现在「课程块不透明度」改为在颜色自带透明度**基础上缩放**：颜色池里的颜色原样呈现，透明度滑杆依然有效
- 顺带修复「课程块不透明度」对色条主题（通透 / 利落）**完全无效**的问题，网格视图与列表视图取色口径统一

**外观**
- 书卷主题浅色模式课程块恢复设计约定的 `0x40` 淡底（块内文字自动转为深色，保持可读）；经典 / 云舒 / 利落 / 通透 默认观感不变

**构建**
- versionCode 160 · arm64-v8a / armeabi-v7a / x86_64

### v3.36.0（2026-09-10）· 课程配色独立成页 + 一键重置配色

**功能**
- 新增三级页「外观与样式 → 个性化显示 → 个性化配色」：把原先散落在「自定义课表页」里的**课程颜色池（浅色 / 深色）、课程块文字颜色、课表页面文字颜色**整块迁入，配色设置不再与圆角 / 间距等布局项混排
- 新增**一键重置配色**按钮（带二次确认）：把课程颜色池恢复为当前主题预设的配色。用于主题预设配色更新后、老用户配色被历史快照"冻住"的情况——预设配色在切换主题时会被快照写入本地，之后预设更新不会自动生效

**外观**
- 「自定义课表页」移除配色相关分组，只保留壁纸 / 网格尺寸 / 课程块外观，页面更聚焦
- 「个性化显示」由两张卡扩为三张卡：玻璃模糊 / 动画效果 / 个性化配色

**构建**
- versionCode 159 · arm64-v8a / armeabi-v7a / x86_64

### v3.35.6（2026-09-10）· 修复「预设配色改动对老用户不生效」

**修复**
- 修复「重做完预设调色板，老用户看到的还是旧配色」：选择主题时会把整套样式（含 `course_color_maps`）**快照**写入 DataStore，而读取时 `toCompose()` 优先采用持久化值，导致后续修改预设调色板**对已选过该主题的用户完全无效**——v3.35.5 的书卷 20 色配色因此根本没生效
- 新增一次性迁移：**仅当**持久化配色与某个「历史预设调色板」逐色完全一致时，才自动升级为当前预设配色；用户手动改过颜色的情况原样保留，不会覆盖个人选择
- 升级到本版后，v3.35.5 的 20 色书卷配色会**自动生效，无需手动重选主题**

**构建**
- versionCode 158 · arm64-v8a / armeabi-v7a / x86_64

### v3.35.5（2026-09-10）· 书卷主题课表配色重做，彻底解决撞色

**外观**
- 书卷主题课程配色由 12 色扩为 **20 色**，按「18° 等距色相 + 相邻明度交替（0.54 / 0.38）」重新设计。全池最近色对距离由 **22.8 提升到 88.5**（约 4 倍），前 12 色为 89.2
  - 这是「撞色」的真正根因：旧的 12 色池里 赤陶 `#C96442` / 珊瑚 `#E88672` / 红 `#D65450` / 玫瑰 `#D86485` / 棕 `#9A6B4E` 五个暖色几乎同色，橄榄 `#788C5D` / 苔绿 `#7A8A4F` / 青绿 `#5F9A5A` 三个绿色几乎同色——索引虽然不同，肉眼根本分不开

**修复**
- 颜色去重改为以**整张课表**为基准：原实现跑在「可视周缓存」上，而渲染前的 `filterNonActiveCourseOverlaps` 会丢掉「本周不上、只在其它周上」的课程，导致统计不完整、跨周滑动时配色不一致

**构建**
- versionCode 157 · arm64-v8a / armeabi-v7a / x86_64

### v3.35.4（2026-09-10）· 课表撞色自动修正

**修复**
- 彻底修复课表内多门课程撞色：此前只修复「越界」的颜色索引，重复颜色被当作合法，所以历史随机分配留下的撞色一直残留。现改为按**课程名**自动归一——同名课次统一为一个颜色（与教务导入的 `getOrAssignColorByName` 语义一致），不同课程名优先分配颜色池中尚未被占用的颜色，12 色用尽后才复用占用次数最少的颜色
- 打开课表即自动完成修正并写回本地，结果稳定、可反复执行不抖动；越界索引（换主题 / 颜色池变小）也一并修正
- 新建课程的默认取色与上述规则保持一致（原为随机取色，必然撞色）

**构建**
- versionCode 156 · arm64-v8a / armeabi-v7a / x86_64

### v3.35.3（2026-09-10）· 修复 CI 插件解析失败 + 课表默认配色撞色

**修复**
- 修复 CI 构建长期失败（KSP 插件报 `was not found in any of the following sources`）：真因为 GitHub Actions 海外运行器访问阿里云镜像返回 502 Bad Gateway，而 Gradle 命中 5xx 时**不会**回退到后续仓库、直接判定该构件解析失败；现 `settings.gradle.kts` 的阿里云镜像改为由 `-PuseMirror` 开关控制——本地默认开启提速，CI 传 `-PuseMirror=false` 仅走 Google / Maven Central / Gradle Plugin Portal 官方源
- 修复课表默认配色撞色：新建课程、以及修复越界颜色索引时，原先逐个随机分配（`Random.nextInt`），常把不同课程分到同一颜色；现改为确定性的「全局占用最少」分配——优先使用尚未被占用的颜色，颜色池用尽后才复用被占用次数最少的颜色（并列取最小索引），12 门课程可依次拿到 12 个互不相同的颜色

**构建**
- versionCode 155 · arm64-v8a / armeabi-v7a / x86_64

### v3.35.2（2026-09-10）· 修复 CI 正式版发布链路

**构建**
- 修复 CI 构建在缺少 `keystore.properties` 时崩溃：`androidApp/build.gradle.kts` 原先无条件创建 release 签名配置并读取该文件，CI 环境下取到 null 抛 `null cannot be cast to non-null type kotlin.String`；现改为仅在存在 `keystore.properties` 时创建本地签名配置，CI 沿用 AGP 注入签名（`-Pandroid.injected.signing.*`），本地与线上签名产物不变
- CI 构建步骤加入失败重试（最多 3 次，重试时附加 `--refresh-dependencies`），抵御 Gradle Plugin Portal / Maven Central 偶发抖动导致的 KSP 插件解析失败
- versionCode 154 · arm64-v8a / armeabi-v7a / x86_64

### v3.35.1（2026-09-10）· 修复「我的」页无法滑动 + 课表课程配色撞色

**修复**
- 修复书卷主题下「我的（设置）」页无法滑动：该页为书卷主题时没有 TopAppBar，却仍挂载了 `exitUntilCollapsedScrollBehavior` 的 nestedScroll 连接，滚动量被整段吞掉；现改为仅在有顶栏的主题下挂载该连接（离屏场景实测：挂载时滚动位移 0，移除后正常）
- 修复书卷主题课表页多门课程同色难区分：原 Claude 课表样式只有 5 个 chart 色循环填充 12 个槽位，且 chart-1 与 chart-5 几乎同色、chart-3/4 过淡；现改为 12 个可区分的色相（赤陶 / 丁香紫 / 橄榄 / 珊瑚 / 金 / 钢蓝 / 红 / 苔绿 / 青绿 / 玫瑰 / 棕 / 松石）

**构建**
- versionCode 153 · arm64-v8a / armeabi-v7a / x86_64

### v3.35.0（2026-09-10）· 今日课表页对齐设计稿 today.html

**外观**
- 书卷主题「今日课表」页按 `claude-style-page.design/pages/today.html` 重绘：
  - 页头改为「周次胶囊 + 居中日期大字」（18sp 700），设置入口保留在右上
  - 新增「下一节课」hero 卡：丁香紫渐变（lilac-100 → lilac-50）、28dp 圆角、左侧「下一节课」小标签 + 圆点 + 22sp 课程名 + 地点/教师元信息；右侧时间徽标（15sp 700 + 时钟图标）与 26sp 倒计时数字 + 「分钟后开始」
  - 今日课程改为时间轴：左侧 56dp 时间列（起始时间 13sp 600 + 圆点 + 连接线），右侧课程卡为 20dp 圆角渐变卡，按序交替丁香紫 / 蜜桃两套配色，含「N 节」徽标、地点/教师元信息与「课程类型 / 学时」信息胶囊
  - 进行中课程圆点填充，已结束课程整卡降透明度并给标题加删除线
- 保留「明日预览」区块；原独立周次条并入页头，以胶囊形态呈现

**功能**
- 新增 7 条三语文案：下一节课 / 课程类型 / 学时 / 理论教学 / 实验教学 / %s 节 / 分钟后开始

**构建**
- versionCode 152 · arm64-v8a / armeabi-v7a / x86_64

### v3.34.0（2026-09-10）· 书卷主题全站铺开 · 多端适配

**功能**
- 新增预设感知顶部栏 `AppTopAppBar`：书卷主题下渲染 48dp 居中标题导航条（17sp 600 / 字距 -0.01em），其余主题保持 M3 原行为；已接入全部设置二级页（时间段 / 学期 / 备份 / 导入导出 / 课程管理 / 更多 / 外观 / 通知 / 转换 / 快捷操作等）
- 时间段管理页书卷化：分组标签改用琥珀色 13sp 小标题，作息方案 / 自动切换作息 / 默认时长设置三组内容改为 inset 分组卡，并新增「节次列表」分组标签
- 书卷主题卡片统一为 14dp 圆角 + 暖米底 `#f3efe4` + 0.5dp 浅描边 + 极轻投影（`AppCard` / `AppSelectableCard` 同步生效）
- 设置行书卷化：行高 48dp、标题 15sp Medium（`SettingItem`），与「我的」页 inset 列表观感一致
- 悬浮窗（课程挂起胶囊）书卷化：改用暖米实底 + 浅描边 + 柔和投影，图标取赤陶主色，替代液态玻璃形态

**外观**
- 分组标签 `AppSectionHeader` 在书卷主题下改为 13sp 600 + 字距 -0.01em
- 新增分组卡底色 / 描边主题 token（`claudeGroupBg` / `claudeGroupBorder`），深浅色分别取值

**优化**
- 书卷 Typography 构建结果按字族 `remember` 缓存，避免主题重组时重复构建 30+ 文本样式
- 书卷顶部栏触控区由 44dp 提升至 48dp，满足无障碍最小触控目标
- 学期设置 / 备份恢复 / 文件导入中心宽屏内容限宽 640dp 居中

**构建**
- versionCode 151 · arm64-v8a / armeabi-v7a / x86_64

### v3.33.0（2026-09-10）· 书卷主题落地 + 日程事件模块

**功能**
- 新增第 5 套主题预设「书卷」（CLAUDE，Anthropic 设计系统）：暖砂纸底 `#faf9f5` + 浅米色卡片 + 赤陶主色 `#c96442`，深色为暖炭底 `#262624` + 亮赤陶 `#d97757`；主色与动态取色 / 自定义主色解耦，像素级锁定
- 书卷主题字体族：标题 Newsreader（衬线）、UI Poppins、正文 Lora，随主题自动切换
- 书卷课表样式：Anthropic chart-1~chart-5 五色循环配色（浅色约 25% 透明淡底 / 深色实底）、8dp 圆角、44dp 时间列、76dp 节次行高
- 「今日课表」页按设计稿重绘：日期 eyebrow（Lora 大字距）+ 28sp Newsreader 大标题 + 课程数徽标 + 周次条 + 课程卡片流（色条 / 类型徽标 / 时间地点教师三行元信息，进行中呼吸圆点、已结束删除线）+ 明日预览 + 课程详情弹层
- 「我的 / 设置」页改为 inset grouped 列表：用户信息行 + 课表 / 课程 / 偏好 / 关于四组；新增 `ClaudeSettingsComponents` 组件库（`ClaudeInsetGroup` / `ClaudeListItem` / `ClaudeGroupLabel` / `ClaudePageHeader` / `ClaudeUserRow`，含多色调图标容器）
- 主题预设「Claude」显示名正式定名「书卷」（三语：书卷 / 書卷 / Shujuan），枚举值仍为 `CLAUDE`
- 新增「日程」模块：日程事件表 `schedule_events`（数据库迁移 10 → 11，含 date 索引）+ 日程页（月历 + 日程列表），支持待办 / 活动 / 考试 / 作业 / 其他分类，全天或定时日程
- 新增农历工具 `LunarCalendar`，供日期展示使用
- 底部导航调整为 4 个 Tab：今日 / 课表 / 日程 / 我的（新增「日程」，首项短标签「今日」避免换行）
- 学校资源更新：刷新索引 `school_index.pb`，新增 GZUTCM / STU 离线教务适配资源

**外观**
- 设计包重组：`ios-style-page.design` → `apple-style-page.design`，新增 `claude-style-page.design`（13 个页面）与 `claude-schedule-page.design`
- 新增 Newsreader / Poppins / Lora / Geist Mono 字体资源

**修复**
- 修复设置页书卷分支新增字符串（`settings_group_timetable` 等）因缺失扩展属性 import 导致的 `Unresolved reference` 编译错误

**构建**
- versionCode 150 · arm64-v8a / armeabi-v7a / x86_64

### v3.32.0（2026-09-10）· Claude 主题「今日课表」页对齐设计稿

**功能**
- Claude 主题下「今日课表」页按设计包 1:1 重绘：日期 eyebrow（Lora 11sp 大字距）+ 28sp Newsreader 大标题 + 课程数徽标 + 圆形设置钮，取代原居中标题栏
- 新增周次条：周次 + 日期区间（`9月8日 – 9月14日`），左右翻页钮按设计稿样式预留
- 课程卡片流改为设计稿形态：5dp 色条 + 18sp 课程名 + 类型徽标 + 时间/地点/教师三行元信息；已结束态降透明度并加删除线，进行中态主色描边 + 顶部 2dp 主色线 + 呼吸圆点
- 明日预览改为凹陷卡 + 4dp 色条 + 「时间 · 地点」单行，右侧「查看全部」跳转周课表
- 点按课程卡片打开详情弹层（状态 / 时间 / 地点 / 教师 / 学分 + 关闭 / 编辑课程）
- Claude 主题下隐藏待办悬浮按钮，保持与设计稿一致（待办仍可通过「日程」页访问）

**外观**
- 课程色条改用 CLAUDE 课表样式的 chart 色板，浅色模式为 25% 透明度（与设计稿实色叠白卡观感一致），深色模式为 chart 实色
- 修复色条在 Row 中因高度约束为 Infinity 而塌成 0 高度不显示的问题（改用 `IntrinsicSize.Min`）

**构建**
- versionCode 149 · arm64-v8a / armeabi-v7a / x86_64

### v3.31.0（2026-09-10）· Claude 设计系统 · 全新「Claude」主题 + 设计包

**功能**
- 新增第 5 套主题预设「Claude」（Anthropic / Claude 设计系统）：暖砂纸底 `#faf9f5` + 白卡 + 赤陶主色 `#c96442`，深色为暖炭底 `#262624` + 亮赤陶 `#d97757`，主色与动态取色解耦（与「通透」同策略，像素级锁定）
- 字体族内置：Poppins（UI 正文）/ Newsreader（页面大标题）/ Lora（阅读正文）/ Geist Mono（token 与代码），7 个字体文件打包进 APK，中文字形沿用系统字体（与设计稿一致）
- 课表网格新增 Claude 配色：以设计系统 chart 色板（`#b05730` / `#9c87f5` / `#ded8c4` / `#dbd3f0` / `#b4552d`）循环成 12 组课程色
- 新增设计包 `claude-schedule-page.design/`：今日日程 / 周课表 / 多学期管理 / 教务导入 / 组件预览共 5 个高保真页面，含跨页导航、周次切换、学期复制删除、四步导入向导、深浅色切换与键盘可达性

**外观**
- 「Claude」主题圆角与留白按设计系统收敛：卡片 16dp、胶囊 12dp、页边距 20dp、页标题 28sp、卡片标题 18sp
- 设计包补齐多端适配：手机（≤380 / 381–767px）底栏导航、平板（768–1199px）左侧栏、桌面（≥1200px）侧栏 + 详情栏；并适配安全区、横屏矮屏与 `prefers-reduced-motion`
- 无障碍：设计包 20 项关键前景/背景对比度全部满足 WCAG 2.2 AA（实底白字按钮与成功徽章改用同色相降明度变体，品牌色本身不变）

**修复**
- 修复外观设置主题选择器在预设增至 5 项后，窄屏等分排列导致标签被挤压换行的问题，改为横向可滚动 + 固定项宽

**构建**
- versionCode 148 · arm64-v8a / armeabi-v7a / x86_64

### v3.28.0（2026-09-09）· iOS 风格全面改版 · 「通透」主题

**功能**
- 全新「通透」主题：严格对齐 Apple 设计规范的 iOS 原生风格——systemGroupedBackground 分组底色、systemBlue 主色、Apple 语义色系、19dp 大圆角卡片、SF 风格字阶（largeTitle 34 / headline 17 / body 15）
- 今日课表页 iOS 化重绘：分区大标题 + 左侧色条扁平课程列表（时间右上等宽字 + 地点/教师带图标行）+ 本周概览三列统计卡 + 明日课程区块
- 「我的」页 iOS 化重绘：大标题导航 + 渐变头像资料卡 + insetGrouped 分组列表（课程设置/工具/其他）+ 28dp 彩色圆角图标徽章 + iOS 风格开关
- 课表网格 iOS 样式：左侧 4dp 色条 + 12 色 Apple 系统色 12% 透明底 + 隐藏网格线 + 文字左对齐
- 多端适配：通透主题下平板/桌面宽屏内容限宽 640dp 居中

**外观**
- 约 60 个组件文件统一迁移至主题化 token：按钮、输入框、对话框、底部面板、列表行全部对齐 iOS 视觉
- 保留悬浮按钮与导航栏液态玻璃效果，其余图形元素全部更换
- 原「通透」(AIRY) 预设与新 iOS 预设合并为一个「通透」，主题列表收敛为 4 项，老数据自动迁移

**修复**
- 修复通透主题主色被 MaterialKolor Expressive 色相旋转染绿的问题，systemBlue 现已像素级锁定（今日高亮/Tab 选中/选中边框全部回归 Apple 蓝系）

**构建**
- versionCode 143 · arm64-v8a / armeabi-v7a / x86_64

### v3.26.1（2026-09-08）· 真机修复

**修复**
- 修复弹性动效风格（琉璃轻弹）下，滚动触发底栏隐藏时应用闪退的问题（过冲缓动导致负内边距）
- 同类过冲数值统一钳制，覆盖底栏淡出、圆钮淡出、页面入场等所有 fraction 驱动的透明度

**构建**
- versionCode 136 · arm64-v8a / armeabi-v7a / x86_64

### v3.26.0（2026-09-08）· 全局动效系统

**功能**
- 新增「个性化显示 → 动画效果」页：三档全局动效风格（**琉璃轻弹** 液态玻璃弹性·默认 / **舒缓轻移** 克制精致 / **灵动跟手** 短促跟手）+ 六个可独立开关的动画分组（课程格反馈 / 周翻页过渡 / 玻璃悬浮件 / 页面入场 / 导航转场 / 底栏隐藏），关掉即瞬切无动效
- 全新课程格点按反馈：周页与今日页课程块按压缩放 + 微抬起（此前完全无反馈）
- 全新周切换玻璃滑动过渡：切周时一道玻璃光泽扫过课程区
- 全新页面入场：进入周页 / 今日页时课程块错峰淡入（仅首次触发，不随数据刷新重播）
- 「个性化显示」升级为两卡 hub：玻璃模糊（原页内容下沉）与动画效果两个三级入口

**外观**
- 全应用硬编码动画（导航转场、底栏隐藏、悬浮件出入场、按压缩放、呼吸节奏、展开/变色等）统一收口为动效令牌，随所选风格整体变化手感

**构建**
- versionCode 135 · arm64-v8a / armeabi-v7a / x86_64

### v3.22.9（2026-09-08）· 外观与主题颜色修复

**修复**
- 修复外观与样式页自定义主色调无法生效的问题，浅色和深色模式现在分别使用对应的自定义主色
- 自定义取色不再被动态取色入口锁定；选择自定义颜色后会自动关闭动态取色，避免新颜色被覆盖
- 修复「我的」页顶部「上课」渐变卡右侧课程格纸插画贴边并被圆角切断的问题，插画现在完整显示并保留微倾动画
- 修复设置卡片图标与卡片边缘贴合的问题，统一补齐内容水平间距
- 学期设置入口描述改为准确概括开学日期、总周数和当前周数

**外观**
- 保留 Telegram 风格毛玻璃底栏、页面吸顶栏和底部面板的一致视觉语言
- 课程格纸插画改为卡片内侧布局，避免装饰元素被裁切

**国际化**
- 补齐自定义主色和学期设置描述的简体中文、繁体中文、英文资源

**构建**
- versionCode 114 · arm64-v8a / armeabi-v7a / x86_64
- 正式版签名 APK 三 ABI 构建并上传至 GitHub Release

### v3.22.8（2026-09-08）· 品牌文案清理

**修复**
- 统一应用商店元数据和离线教务适配注释中的旧项目名称，改为「上课 / Attend Class」
- 修复课表分享口令签名与解析器不一致导致的自导出文本无法自动解析问题

**构建**
- versionCode 113 · arm64-v8a / armeabi-v7a / x86_64


### v3.22.4（2026-09-07）· 课表页底栏形态统一

**修复**
- 课表页网格现在与「我的」页一致：内容延伸到底部导航条之下并从玻璃后透出，去除底栏下方的空白死区
- 底部导航条隐藏时滚动余量自动收回，末节课程可完整滚动到屏幕底部

**构建**
- versionCode 109 · arm64-v8a / armeabi-v7a / x86_64

### v3.22.3（2026-09-07）· 底栏形态修复

**修复**
- 修复底部导航栏玻璃层未被裁剪导致显示为直角矩形的问题，现恢复为完整圆角胶囊形态
- 修复课表页底部出现的白色空块：页面背景不再透出窗口白色，底部留白高度不再重复计算系统手势区
- 修复课表页顶栏收起后玻璃导航条遮挡课程网格末行的问题

**构建**
- versionCode 108 · arm64-v8a / armeabi-v7a / x86_64

### v3.22.2（2026-09-07）· 导入可修正与桌面端修复

**功能**
- 导入预览支持逐条编辑与删除：可修正识别错误的课程名、教师、地点、星期、节次和周次，导入内容与预览严格一致
- 设置页新增「备份与恢复」独立入口，不再需要逐级进入
- 周次选择器在非常规起始页选择周次时也能准确跳转

**修复**
- WebDAV 恢复时若个别数据模块缺失，完成后会明确列出缺失项，不再静默忽略
- 修复周课表课程块无法被屏幕阅读器朗读和操作的问题，现按课程名、时间、地点、教师整体播报
- 修复设置页标题与底栏名称不一致的问题
- 清理两个从未接入导航的死页面入口（快捷操作中心、旧版文本导入）

**构建**
- versionCode 107 · 补齐桌面端缺失的 8 处平台实现，桌面目标首次可完整编译 · arm64-v8a / armeabi-v7a / x86_64

### v3.22.1（2026-09-07）· 数据安全与导入可靠性修复

**功能**
- 系统日历同步首次使用会请求日历写入权限，授权后继续同步，拒绝时明确提示
- WebView 教务导入新增加载失败页：网络、SSL、主页面 HTTP 错误或长时间无响应时可直接重试或返回
- 底部导航切换保留已访问主页面状态，底栏隐藏后内容会自动回收底部留白

**修复**
- 恢复备份、断开 WebDAV、覆盖导入、删除课程/作息方案、批量删除和调课等不可逆操作统一增加二次确认
- 作息时间段重编号后自动迁移普通课程对应节次，避免课程落到错误时间段
- JSON 文件只保留一条专用导入路径，避免与自动文本导入产生行为差异
- 修复教务 WebView 302 地址插入脚本时的转义漏洞
- 修复底栏滚动隐藏时的闪烁、重复淡出与 Today 页底部 inset 叠加

**国际化与无障碍**
- 补齐英文、简体和繁体中文的设置分区、确认和 WebView 错误文案
- 底栏使用 Tab 语义并避免图标与标签重复朗读；批量删除入口补齐无障碍描述

**构建**
- versionCode 106 · arm64-v8a / armeabi-v7a / x86_64

### v3.22.0（2026-09-06）· 毛玻璃与全局一致性改版

**功能**
- 全新毛玻璃质感（Telegram 形态）：底部悬浮导航栏、设置页吸顶栏与全部底部弹窗（周次选择、时间选择、时间段编辑、快速删除筛选、颜色调整）支持真实背板模糊——滚动与拖动时内容从玻璃后柔和透出；深浅色自动适配，不支持模糊的旧设备自动回退实色

**外观**
- 全局组件收敛到单一来源：设置行、分区标题、空状态、加载态、悬浮按钮、危险确认弹窗、输入框、分段控件、提示条、滚轮分隔线等全部统一，消除各页面混杂形态
- 悬浮按钮全部为主色圆形 + 按压缩放；列表空状态统一为淡灰胶囊提示；危险操作（删除课表/情侣课表/快速删除/重置样式）确认按钮统一红色胶囊
- 课程块长按悬浮态与拖拽手柄改语义强调蓝（跟随主题）；周次选择胶囊触控区加大；底部导航恢复按压反馈并提高触控区高度
- 彩色课程卡片文字按背景明暗自动切换黑/白，深色课程底上的可读性提升
- 风格预览演示课程与全部页面骨架细节（间距/字阶/圆角/分隔线）向统一基线收敛，整体更轻盈

**国际化**
- 导入链路（文件/Excel/JSON/文本/ICS/CSV）、教务 WebView 桥接提示、外观与转换弹窗等全部文案接入多语言（简体中文/繁体中文/英文）；「←」字符返回键统一为标准返回图标并补充无障碍描述

**修复**
- 深色模式：提示条底色、利落主题课表文字、外观页滑杆旋钮描边等处硬编码颜色导致的可读性问题
- 时间段管理、备份、转换等页面弹窗按钮与输入框观感与全局统一

**构建**
- versionCode 105 · arm64-v8a / armeabi-v7a / x86_64

### v3.21.0（2026-09-06）· 外观改版

**外观**
- 全局风格向「日程 / 我的」基线收敛：页面底色统一为淡灰白，全部内容装入白色大圆角（20dp）无边框卡片；卡片圆角、投影、间距、字阶（大数字 28sp / 页标题 20sp / 行标题 16sp / 辅助 13sp）全 App 统一
- 「我的」页新增紫渐变头部卡；全部设置列表行统一为「语义色图标 chip + 行标题 + 副标题 + 灰色箭头」，开关统一为大号胶囊样式（开启为绿色）
- 底部导航改为悬浮白色胶囊条，选中项浅紫胶囊高亮 + 图文加粗
- 课表页新增 ⋮ 溢出菜单（Telegram 形态：白圆角卡 + 图标文字行），承载添加课程 / 管理课表 / 外观样式 / 自定义时间段既有入口；周次切换入口胶囊化
- 今日页悬浮按钮改为主色圆形大投影样式并支持按压缩放；备注采用聊天气泡形；空态/加载态/提示条样式统一
- 学校选择页搜索框换肤为全圆角胶囊样式；课表数量、跳过日期等计数统一为灰底白字胶囊徽标
- 首轮验收反馈后排版紧凑化：列表图标 chip 收敛至 48dp、行高统一 64dp、卡片间距与内边距收紧，「我的」头部卡增加图标锚点与文案层次，通知页区标题对齐字阶规范
- 二轮验收反馈后弹窗观感软化：全部对话框标题由 24sp 常规体收软为 20sp 半粗体，页面/分区标题统一 20sp 粗体，观感更柔和统一
- 三轮验收反馈后输入类弹窗重做：添加/编辑课表名称、今日待办编辑等弹窗换用填充式圆角输入框（无描边）与「灰字取消 + 主色胶囊确认」操作区，观感与基线控件统一
- 深浅色两套完整主题同步适配；外观样式之外无任何功能变更

**构建**
- versionCode 104 · arm64-v8a / armeabi-v7a / x86_64

### v3.20.0（2026-09-06）· 功能更新

**功能**
- 新增河北医科大学课表导入适配：支持乘方教务新版系统（jwweb.hebmu.edu.cn），账号密码+验证码登录后自动获取当前学期课表，解析课程名/教师/教室/星期/节次/周次（含周六周日课程）

**构建**
- versionCode 103 · arm64-v8a / armeabi-v7a / x86_64

### v3.19.0（2026-09-06）· 功能更新

**功能**
- 中国科学院大学新增「研究生教务」分类入口：复用同一套 SEP 平台课表适配器，在研究生分类下可搜索并导入；原本科分类入口保留不变

**构建**
- versionCode 102 · arm64-v8a / armeabi-v7a / x86_64

### v3.18.7（2026-09-06）· 修复

**修复**
- 滁州学院教务系统在 App 内 WebView 页面布局错乱、无法看到课表网格：改为以电脑版（桌面 UA + 1280px 视口）打开，与西安医学院、国科大金智系学校保持一致

**构建**
- versionCode 101 · arm64-v8a / armeabi-v7a / x86_64

### v3.18.6（2026-09-06）· 修复

**修复**
- 修复滁州学院课表导入完全失败：浏览器登录查课正常但 App 内导入中断，共修复 6 项问题——学期列表接口（dataQuery）返回 500 改为从课表页内嵌的当前学期 ID 直接获取；课程数据解析正则与实际页面格式不匹配（`activity = new TaskActivity` 无 `var`、教师数组与课程行之间有中间代码）；节次索引表达式跨行导致匹配失败；每日节次硬编码 12 与实际 10 不符导致节次错位；课程参数顺序偏移导致教室/周次取错字段；教师名为 JS 表达式而非字符串，改为从教师数组中提取姓名（支持多教师）
- 滁州学院导入流程简化：登录后自动定位当前学期，无需手动选择学期

**构建**
- versionCode 100 · arm64-v8a / armeabi-v7a / x86_64

### v3.18.5（2026-09-06）· 修复

**修复**
- 修复中国科学院大学课表导入失败：个人课表网格解析正常，但课程详情（上课周次/地点）因网格页（xkgo）与详情页（xkcts）跨域，WebView 内 fetch 被浏览器 CORS 拦截导致全部获取失败；现跨域请求改经应用内置拦截器转发并回填跨域许可头，一次点击即可完整导入
- 导入流程简化为单页完成：识别个人课表 9 门课 → 逐门获取上课时间/周次/地点 → 直接保存，无需页面跳转
- 国科大选课系统默认以电脑版进入（桌面 UA + 1280px 视口），跨域详情转发更稳定

**构建**
- versionCode 99 · arm64-v8a / armeabi-v7a / x86_64

### v3.17.0（2026-09-06）· 功能更新

**功能**
- 西安医学院适配升级为「API 直取」：真实测试账号端到端打通金智 ehall 统一认证 → 教务系统（青果）→ 学生个人课表全链路；适配器在青果教务页内同源请求「我的课表」数据接口（学期列表 + 个人课表列表），解析课程名/教师/周次/星期/节次/教室/学分，比页面 DOM 解析更稳更全
- 大厅页未进教务时点导入可一键跳转「教务管理系统」应用；会话失效给出明确提示
- 周次解析支持「1,3,5,7周」列举式（单双周课不再误并为连周）

**构建**
- versionCode 91 · arm64-v8a / armeabi-v7a / x86_64

### v3.16.0（2026-09-05）· 功能更新

**功能**
- 新增西安医学院专用适配：经金智网上办事大厅（ehall）统一认证入口进入，自动识别登录页/首页/课表页三种状态，课表采用 DOM 矩阵解析（支持 rowspan/colspan 与 iframe）

**修复**
- 修复正方教务系统班级课表录入错误：导航到班级课表页面后，API 兜底仍调用个人课表接口导致只导入个人课表；现自动识别页面类型，班级课表调用 `bjkbdy_cxBjKb.html` 接口并传递班级参数
- 增强湖南财政经济学院适配：增加 CAS 登录页与主框架页检测，按页面状态给出精准导入引导

**构建**
- versionCode 90 · arm64-v8a / armeabi-v7a / x86_64

### v3.15.1（2026-09-05）· 正式版

**修复**
- 修复灵动岛窗口闹钟的 PendingIntent 匹配缺陷：改提前量/课程变动导致窗口变化时，新旧闹钟可能并存，旧结束闹钟会在新窗口内误停正在运行的灵动岛；现已常量化 Intent 并修正取消匹配
- 修复 ProgressStyle 进度刻度：多节课的日子进度点现准确落在「正在上的课」分段上（原只走到整条 1/N 处）
- 修复分段配色：「已完成=绿色」此前会被官方样式渲染覆盖，现完整呈现（已完成绿 / 进行中课程色 / 未开始灰）

**功能**
- 真机（vivo V2458A · OriginOS 6 / Android 16 QPR1）验证：通知已携带 Android 16 Live Updates 全部官方准入标志（requestPromotedOngoing + shortCriticalText + ProgressStyle 分段 + 追踪图标），原生 Pixel 等 AOSP QPR1 机型可直接呈现状态栏胶囊；vivo 机型胶囊呈现取决于 vivo 系统更新/原子岛开放接入

**构建**
- versionCode 89 · arm64-v8a / armeabi-v7a / x86_64

### v3.15.0（2026-09-04）· 功能更新

**功能**
- 灵动岛升级为 Android 16 官方「以进度为中心的通知」（ProgressStyle）
  - 按 vivo「Android 16 开发者适配文档」与 Google 官方规范重写：课程进度以官方分段样式呈现
  - 今天的每节课对应一个进度分段（已完成 / 进行中 / 未开始），一眼看清当天课程进度
  - 上课中显示实时进度百分比 + 剩余分钟，状态芯片同步更新（如「高等数学 50%」）
  - 继续支持 Android 16 实时芯片（Promoted Ongoing）；低版本自动降级为水平进度条

**构建**
- versionCode 88 · arm64-v8a / armeabi-v7a / x86_64

### v3.14.1（2026-09-04）· 功能更新

**功能**
- 灵动岛改为「时间窗口启停」：仅在当天第一节课开始前 → 最后一节课结束的时段内运行，其余时间零常驻、更省电
  - 由系统闹钟精确调度窗口开始 / 结束，周末或无课日自动跳过
  - 打开「状态栏灵动岛」开关时，若未授予「闹钟与提醒」权限会主动引导授权，确保准时启停
  - 实时倒计时、上课进度条等显示效果保持不变

**构建**
- versionCode 87 · arm64-v8a / armeabi-v7a / x86_64

### v3.14.0（2026-09-04）· 功能更新

**功能**
- 新增「状态栏灵动岛」（Android 16 实时更新 / Promoted Ongoing）
  - 在状态栏常驻显示当前 / 下一节课状态，实时刷新上课进度与剩余时间
  - 支持四种状态：正在上课（进度条 + 剩余分钟）/ 下一节课倒计时 / 今日无课 / 今日课程已结束
  - Android 16（API 36）及以上呈现实时状态芯片 + 进度样式；低版本自动降级为持续通知
  - 与「兼容穿戴设备同步通知」联动：开启兼容模式后停用实时更新效果
  - 入口：设置 → 课程提醒设置 → 「状态栏灵动岛」

**构建**
- versionCode 86 · arm64-v8a / armeabi-v7a / x86_64

### v3.13.1（2026-09-04）· 正式版

**功能**
- 今日课表新增「今日待办」：右下角悬浮「+」号一键添加
  - 待办条与课程条同款样式：左侧时间列 + 圆角色块 + 复选框置前
  - 时间改为选择式（应用同款滚轮底部弹窗），支持添加 / 编辑 / 勾选完成 / 删除
  - 排序：填了时间按时间排序，未填时间自动排至最后，已勾选排最后；待办整体排在所有课程列表之后

**修复**
- 待办自动排到课程列表之后（页面最底部），不再插在课程之间
- 无时间的待办不再显示「--:--」占位，时间列留空

**构建**
- versionCode 85 · arm64-v8a / armeabi-v7a / x86_64

### v3.11.0（2026-09-03）· 正式版

**功能**
- 文件导入按格式拆分独立入口：Excel / JSON / ICS 日历 / CSV / 文本文件（HTML/TXT）
  - ICS 日历导入支持 .ics/.ical/.ifb 文件直接选择导入
  - 各入口按格式过滤文件类型并强制使用对应解析器
- ICS 日历导入全面改进：修复 WakeUp 导出文件解析失败/数据错乱
  - 跳过 VALARM 块，避免覆盖真实课程信息
  - 节次优先从 DESCRIPTION「第X-Y节」提取；周次按绝对日期映射（支持 UNTIL/COUNT）
  - LOCATION「地点 教师」自动拆分、ICS 转义还原

**构建**
- versionCode 82 · arm64-v8a / armeabi-v7a / x86_64

### v3.10.1（2026-09-03）· 正式版

**修复**
- ICS 日历导入：修复 WakeUp 导出的 ICS 文件解析失败/数据错乱问题
  - 跳过 VEVENT 内 VALARM 块，避免其 DESCRIPTION 覆盖真实课程信息（节次/地点/教师丢失根因）
  - 节次优先从 DESCRIPTION「第X - Y节」提取，不再仅依赖固定作息时间推算
  - 周次按事件绝对日期映射（RRULE 支持 UNTIL 与 COUNT），解决所有课程被当作第1周起 16 周的错误
  - LOCATION「地点 教师」自动拆分，ICS 转义（\, \n 等）正确还原
- 新增 ICS 解析回归测试（真实 WakeUp 文件 26 条 + 旧格式兼容）

**构建**
- versionCode 81 · arm64-v8a / armeabi-v7a / x86_64

### v3.10.0（2026-09-03）· 正式版

**教务导入增强**
- 沈阳农业大学：强制电脑版进入教务（桌面 UA + 1280px 视口修复），保证课表页渲染与解析稳定
- 三校适配（湖南财政经济学院 / 西南交通大学希望学院 / 沈阳农业大学）真机导入实测全部通过

**构建**
- versionCode 80 · arm64-v8a / armeabi-v7a / x86_64

### v3.9.2（2026-09-03）· 正式版

**教务导入新增**
- 湖南财政经济学院：新增适配，强智教务系统课表导入（CAS 统一认证登录）
- 西南交通大学希望学院：修正登记与适配，URP 教务系统课表导入（含验证码登录）
- 沈阳农业大学：修正原登记（原入口有误），新增专用适配，支持校内统一认证（tpass）与校外 WebVPN 双入口
- 沈阳农业课表以课表网格导入（理论课 + 实验课一并排入，实验课无需重复解析）

**修复**
- 修复学校「最近访问」历史记录导致「导入脚本文件不存在」的问题（历史记录优先使用最新适配数据）
- 沈阳农业移除实验课、实习课重复解析

**构建**
- versionCode 79 · arm64-v8a / armeabi-v7a / x86_64 · 学校清单 1784 所 / 专属适配 138 个

### v3.9.1（2026-09-03）· 发布说明

**修复**
- 修复从「最近访问」进入学校时因历史记录的旧资源目录导致「导入脚本文件不存在」的问题

**构建**
- versionCode 78 · arm64-v8a / armeabi-v7a / x86_64

### v3.9.0（2026-09-02）· 发布说明

**教务导入新增与修正（3 校适配）**
- 湖南财政经济学院：新增适配，强智教务系统课表导入（CAS 统一认证登录）
- 西南交通大学希望学院：修正登记与适配，URP 教务系统课表导入（含验证码登录）
- 沈阳农业大学：修正原登记（原入口有误），新增专用适配，支持校内统一认证（tpass）与校外 WebVPN 双入口
- 沈阳农业课表支持「理论课 + 实验课 + 实习课」三类安排一并导入，实验课按作息时间推算节次
- 学校索引重建（1784 所）与离线资源包更新

**构建**
- versionCode 77 · arm64-v8a / armeabi-v7a / x86_64

### v3.8.1（2026-09-01）· 正式版

**教务导入增强**
- 武汉纺织大学外经贸学院：强制桌面版进入教务，解决手机端教务菜单无法打开课表的问题
- 湖北工程学院：经 ehall 办事大厅进入教务的完整适配（无法直连教务时自动走办事大厅）
- 正方通用适配器 filter 修复，覆盖旧库覆盖 `Array.prototype.filter` 的兼容问题

**导入体验重构（收官）**
- 导入新课表后若未设置开学日期：自动弹窗「请设置开学日期」并跳转学期设置
- 文件导入目标选择：Excel / 文本文件 / 文本粘贴导入时，可选「另存为新课表」或「覆盖已有课表」
- Excel 课表文件解析导入
- 文件导入六分类 Hub
- 文本导入多格式回退链
- 文件选择器闪退根治（GET_CONTENT 强路由 + MediaStore 兜底导出）

**外观与稳定性**
- 外观默认值统一：字体比例 1.2 / 内部填充 2 / 圆角 8 / 外部间距 1（三种模式一致）
- 课表字体跳变收敛：开启/关闭周末时字号稳定

**构建**
- versionCode 76 · arm64-v8a / armeabi-v7a / x86_64

### v3.4.0（2026-09-01）

- 设置页 UI 收尾：主页 9 项导航标题全部加粗、字号统一（18sp）；导航卡片高度微增
- 更多页新增「联系作者反馈」入口（邮箱 hhixingchen520@163.com），支持反馈新功能或请求适配教务（适配需附：学校名称、教务系统账号、教务系统密码）
- 二级页统一为逐项独立卡片、分区大卡只做分割
- 主页新增「显示非本周课程 / 是否显示周末」双开关
- 情侣课表导入/删除入口迁至情侣课表页
- 120Hz 高刷

### v2.13.12（2026-08-31）

- 修复：江苏医药职业学院教务课表导入失败
- 该校教务仅校内可访问，新增 WebVPN「智慧苏医」入口（统一身份认证登录 → 智慧苏医门户 → 教务系统 → 课表导入）
- 修复 enlink WebVPN 根地址被跳转到客户端下载页的问题，改为直连智慧苏医门户深层链接

### v2.13.11（2026-08-30）

- **修复 CAS 统一认证 / 门户类学校导入问题**：登录后点击「教务系统」不再出现空白页或闪退，内置浏览器支持多窗口（window.open / 新标签页）并自动转回当前页继续导入
- **重写强智教务系统课表解析**：支持 frameset / `<frame>` 及多层 `<iframe>` 嵌套的课表

### v2.13.10（2026-08-30）

- 作息方案自动切换：过滤不存在的日期（如 9-31），含无效日期的方案不参与自动切换
- 作息方案日期设置弹窗：日期滚轮跟随月份动态显示
- 放开明文 HTTP 访问并精简网络安全配置

### v2.13.7（2026-08-29）

- 盐城师范学院改为双入口：正方教务系统（内网）+ X-Web 校外直连
- 正方教务适配器复用页面真实接口地址，修复 WebVPN 反代下 404
- 正方教务接口增加请求 URL 与返回体诊断日志

### v2.13.6（2026-08-29）

- 修复除南通大学外其他学校课程导入失败的问题
- 重写通用适配器（强智 / 青果 / 金智 / URP / 南软）为 DOM 矩阵解析，支持 rowspan/colspan 与 iframe
- 金智(Wisedu)新增内嵌 kbxx / kcbxx JSON 解析路径
- 正方教务自动读取 gnmkdm、增加页面渲染重试并优化导入流程

### v2.13.5（2026-08-27）

- 修复课表显示、导入、备份、WebView 等一批遗留问题
- 收紧网络策略：默认禁止 HTTP 明文，仅放行学校教务 / SSO 相关域名
- 重写 README，突出「上课 · ShangKeSchedule」自身特色

### v2.12.0（2026-08-25）

- **课表排版体验优化**：课程块字号自适应缩放、地点智能换行、课程信息优先级重排
- **多作息方案（夏令时 / 冬令时）**：同一课表可维护多套作息时间，支持生效日期范围和跨年区间，可手动或按日期自动切换

---

## 记录规范

- 每次发布在「最新版本」区**最顶部**追加一条，格式：`### vX.Y.Z（YYYY-MM-DD）· 发布说明`。
- 内容按「功能 / 修复 / 外观 / 构建」分类，写明关键变更与构建信息（versionCode、支持 ABI）。
- 与仓库 `工作日志.md`（开发过程记录）互为补充：本文件面向用户发布说明，工作日志面向开发过程。
