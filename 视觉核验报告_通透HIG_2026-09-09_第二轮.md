# 「通透」Apple HIG 全站视觉核验报告 · 第二轮（Trae 修复后复查）

- 核验日期：2026-09-09（第二轮，Trae 修复后复查）
- 核验对象（Trae 工作区确认）：`D:\01课程表\shangkeschedule` 主仓库（HEAD `970f791` = v3.26.2 + 未提交改动 7 个文件）
  - 排除项：`shangkeschedule--worktrees/` 下两个 worktree（`bug` 分支停在 cf081fd、`main` 分支停在 6ed6ea0，均为 v3.23 前旧代码，无任何通透主题改动，非 Trae 工作区）
- 核验方式：纯代码静态核验 + `:shared:compileAndroidMain` 编译验证，**未改动任何代码**
- 判定基准：HIG「通透」10 项清单

---

## 一、结论摘要（TL;DR）

**全站仍未达到「通透 HIG」标准。Trae 仅完成了 P0-1（AIRY 色板/token 基础设施）一项，P0-2 / P0-3 / P1 / P2 / P3 全部未执行。**

- ✅ **P0-1 已完成且可编译**：`AppStyle.kt` 新增双参 `appColorTokens(isDark, preset)` + `airyAppColorTokens()` / `airyDarkAppColorTokens()`（深色基底 Apple 深灰 `#1C1C1E`、主色 `#007AFF`/`#0A84FF`、Apple 全套语义色）；`Theme.kt` 接入 `themePreset` 并注入形状/间距/字阶 CompositionLocal；`AppThemePreset.kt` 注册 `AIRY` 枚举 + 4 语言字符串。**`:shared:compileAndroidMain` 编译通过 ✅（55s，零报错）**——上一轮报告的编译阻断已解除。
- ❌ **P0-2 未做**：`AppAlertDialog` 全库 **0 命中**，约 25 处原生 M3 `AlertDialog`/`DatePickerDialog` 原样残留（18 个文件）。
- ❌ **P0-3 未做**：`AiryGridStyle` 全库 0 命中，`AppThemePreset.AIRY.gridStyle` 仍为 `ScheduleGridStyle.DEFAULT` → 通透主题下课表仍是 **M3 高饱和马卡龙 12 色 + 8dp 小圆角 + 可见网格线**，直接违反「低饱和 / 柔和大圆角」。
- ❌ **P1 未做**：旧静态常量 `AppShape.` / `AppSpacing.` / `AppType.` 仍有 **23 个文件**在引用；主题化访问函数 `appShapes()/appSpacing()/appType()` 除定义处外**业务代码 0 接入** → AIRY 放大值（22dp 卡片 / 20dp 留白 / 56dp 行高 / 17sp 正文）**到不了任何一个页面**。
- ❌ **P2/P3 未做**：原生控件（RadioButton ×6、Checkbox、DropdownMenu ×4、SearchBar、PrimaryTabRow、NavigationRail）、未着色 `HorizontalDivider()` ×4、阴影三值分裂（8/10/14dp）、写死亮青 `#73CAF8`、硬编码白字等全部残留。

**底部 Tab 口径再次确认：3 个 Tab**（今日课表 / 课表 / 我的），无「首页」「日程」独立 Tab，核验按 3 Tab + 全部二级页执行。

---

## 二、Trae 实际改动范围（git diff --stat 证实）

| 文件 | 改动量 | 内容 | 判定 |
|---|---|---|---|
| `ui/theme/AppStyle.kt` | +323 行 | 双参色板分发 + AIRY 深浅色板 + 形状/间距/字阶 token 类与 AIRY 变体 + CompositionLocal | ✅ 质量合格 |
| `ui/theme/Theme.kt` | +35 行 | `themePreset` 参数透传 + 双参取色 + token 注入 + Material Shapes 同步 | ✅ |
| `data/model/AppThemePreset.kt` | +9 行 | `AIRY` 枚举（种子色 `#007AFF`）——但 `gridStyle` 仍指向 `DEFAULT` | ⚠️ 半成品 |
| `strings.xml` ×4 | — | `theme_preset_airy`（通透/Airy） | ✅ |

**注意**：工作日志中 v3.30.0 的三条记录（AppAlertDialog 迁移 18 处、20+ 文件接入 appShapes()、全局弹窗玻璃化）**与当前代码树不符**——`AppAlertDialog` 与业务文件的 `appShapes()` 引用均为 0 命中，日志描述未落地，存在「日志先行于代码」问题。

---

## 三、HIG 10 项清单逐项判定（本轮）

| # | HIG 检查项 | 判定 | 关键证据 |
|---|---|---|---|
| 1 | 统一柔和大圆角 | ❌ 未达标 | AIRY 形状 token 已定义但业务代码 0 接入；卡片仍 20dp 静态值；课程块 8dp（DEFAULT gridStyle）；M3 Card 12dp 散落 |
| 2 | 充足留白 | ❌ 未达标 | `appSpacing()` 业务文件 0 接入；全站硬编码 16dp 页边距 / 8dp 间距原样 |
| 3 | 磨砂半透明材质 | ◐ 部分达标 | 底部胶囊导航 / AppGlassBottomSheet / FloatingCourseBar 已有玻璃感（前版成果）；❌ 约 25 处弹窗仍 M3 默认样式，无玻璃弹窗 |
| 4 | 低饱和配色 | ❌ 未达标 | 课表 DEFAULT_COLOR_MAPS 高饱和马卡龙 12 色（ScheduleGridStyle.kt:75 起原样）；DeveloperAndIconComponents.kt:49-50 写死 `#73CAF8`/`#BD0000`；error 色未映射 danger |
| 5 | 极淡柔和阴影 | ❌ 未达标 | 8/10/14dp 三值分裂仍在（WeeklyScheduleScreen:896、StyleComponents:283、AppBasicComponents:169、FloatingCourseBar:89、GlassBlurScreen:223）；QuickDeleteScreen:169 tonal 8 + shadow 8 叠加仍在 |
| 6 | 字体层级清晰 | ❌ 未达标 | `appType()` 业务文件 0 接入；标题仍走 M3 typography + 静态 AppType |
| 7 | 分割线极浅 | ❌ 未达标 | 未着色 `HorizontalDivider()` 4 处（AppearanceSettings:225/414、TimeSlotManagement:275/565）回落 outlineVariant；网格线 outline 0.2 原样 |
| 8 | 胶囊按钮 / iOS 控件 | ❌ 未达标 | RadioButton ×6（MoreOptionsDialogs:78/123、BackupScreen:427、AnimationSettings:391、LanguageSetting:128、WebDialogHost:161）、Checkbox（TodaySchedule:575）、DropdownMenu ×4（WebViewScreen:294、TweakSchedule:319、TimeSlotManagement:505、StyleComponents:277）、SearchBar（SchoolSelectionList:334）、PrimaryTabRow（SchoolSelectionList:291）、NavigationRail（NavigationComponents:181）全部原样 |
| 9 | 深浅双主题、深灰基底 | ◐ 半达标 | ✅ AIRY 深色色板已就位（`#1C1C1E` 基底 / `#2C2C2E` 卡片 / 深色语义变体 / 对比度达标）——**但只在切到通透主题时生效**；❌ 经典等其余主题深色仍是近黑蓝灰；硬编码白字前景未处理 |
| 10 | 轻柔缓动动画 | ✅ 已达标 | `LocalAppMotion` token 体系延续前版成果，无残留生硬动画 |

---

## 四、待修复问题清单（按优先级）

### P0-A｜落地 AppAlertDialog 并替换 ~25 处原生弹窗（未动）
`AppAlertDialog` 全库 0 命中。残留原生弹窗文件（18 个）：`App.kt`、`TodayScheduleScreen`、`NotificationDialogs`、`BackupScreen`、`AppearanceSettingsScreen`、`MoreOptionsDialogs`、`WebViewScreen`、`WebDialogHost`、`TimeSlotManagementScreen`、`AddEditCourseScreen`、`ImportPreviewComponents`、`CourseTableConversionDialogs`、`StyleSettingsComponents`、`SettingsScreen`、`ManageCourseTablesScreen`、`CourseTablePickerDialog`、`AppBasicComponents`(AppDangerDialog)、`ShareManager`。

### P0-B｜AiryGridStyle 缺失，通透主题课表仍是高饱和马卡龙（未动）
`AppThemePreset.kt:28`：`AIRY.gridStyle = ScheduleGridStyle.DEFAULT`；`ScheduleGridStyle.kt:74-87` M3 高饱和 12 色原样，`courseBlockCornerRadiusDp = 8f`、`hideGridLines = false`。**这是用户切到「通透」后视觉冲击最大的违规点。**

### P1｜静态常量引用 → appShapes()/appSpacing()/appType()（未动，23 文件 ~70 处）
重灾区：`TodayScheduleScreen`(11)、`SettingsScreen`(12)、`StyleComponents`(12)、`NavigationComponents`(9)、`WeeklyScheduleScreen`(7)、`GlassBlurScreen`(6)、`AppBasicComponents`(6) 及 17 个其余文件。`SettingsScreen` 顶层常量 `SETTING_PADDING/ITEM_SPACING` 需下沉为 @Composable 读取。

### P2｜颜色收口 / 分割线 / 阴影 / 深色专项（未动）
- 直取 `colorScheme.error/outline/secondary` 未映射：CourseSchemeCard、import 各页、QuickDeleteScreen:486-489、SchoolSelectionListScreen、ScheduleGrid:87 等
- 未着色 Divider ×4（见上表 #7）
- 阴影 8/10/14dp 分裂 6 处（见上表 #5）
- 写死色：`DeveloperAndIconComponents.kt:49-50`
- 白字前景硬编码：StyleComponents:599-601、AppBasicComponents:137、MoreOptionsDialogs:181-186、AddEditCourse:313、QuickDelete:178

### P3｜原生控件替换 / 卡片统一 / 留白 token 化（未动）
清单同上表 #8，另加 M3 Card 散落 8 处（SchoolSelectionList、AdapterSelection、AppearanceSettings、ImportPreviewComponents、CourseInstanceList、TweakSchedule、TimeSlotManagement、CourseSchemeCard）与 20+ 处硬编码页边距。

---

## 五、已达标项（无需修改）

1. **AIRY 色板与 token 基础设施**（本轮 Trae 新增）：浅色 `#F2F2F7` 分组底 / Apple 全套语义色 / label 文字层级；深色 `#1C1C1E` 深灰基底（非纯黑）/ `#2C2C2E` 卡片三级层级 / 深色语义变体——设计值完全符合 Apple HIG。
2. **编译链路**：上一轮的 Theme.kt 双参调用编译阻断已解除，`:shared:compileAndroidMain` ✅。
3. **底部液态玻璃导航 / AppGlassBottomSheet / FloatingCourseBar / AppSegmentedControl / AppSnackbarHost**：前版玻璃样板保持完好。
4. **动效体系**（`LocalAppMotion`）：HIG 第 10 项唯一全达标项，延续。
5. **结构性颜色映射**（`withAppSurfaces`）：无大面积纯黑/纯白崩坏。
6. **主题隔离设计**：AIRY 走 CompositionLocal 分发，经典/云舒/利落零回归的架构基础已具备——只差把 token 接到页面。

---

## 六、下一步建议

Trae 实际完成度 ≈ **核验清单的 15%**（仅 P0-1）。建议把本报告第四、五节作为追加提示词发回 Trae，明确要求：

1. **P0-A + P0-B 优先**（弹窗玻璃化 + AiryGridStyle 低饱和课表色）——这两项是用户切主题后第一眼看到的违规点；
2. **P1 一改全站**：优先 `SettingsScreen` 共享组件（SettingItem/SettingCard/顶层常量下沉），其次 `StyleComponents`/`AppBasicComponents`/`NavigationComponents` 默认参数；
3. 每完成一批跑一次 `:shared:compileAndroidMain`；
4. **工作日志必须与代码同步**——v3.30.0 三条记录描述的迁移在代码树中不存在，需 Trae 补做或更正日志，不得「日志先行」。
